<!-- META: P44 | module:subsystems/模拟服务层 | status:ready | last:2026-06-13 -->
# P44 池子批次抽卡（十连强制）实施计划

> 日期：2026-06-13 | 状态：就绪 ✓
> 依赖：无（独立可执行）

**Goal:** 支持池子级配置 `batch_size`（默认 1），当 `batch_size > 1` 时策略检查 N 发可负担性、服务层原子预检查 + 逐发抽取（保底逐发更新、多资源优先级自动切换）。

**Architecture:** 池子驱动——`batch_size` 是 `Pool` 的静态属性。策略返回普通 `DrawAction`，通过 `ctx.state.can_afford_batch()` 检查 N 发可负担性。服务层预检查 → `for i in range(batch_size)` 逐发 `spend()` + `draw()`。不改 `DrawAction` 接口、不改收集器/统计/保底引擎接口。

**Tech Stack:** Python 3.10+, pytest, 现有项目基础设施

---

## 设计摘要

### 数据流

```
schedule.txt 第 9 列 (可选, batch_size, 默认 1)
        │
        ▼
PoolConfig.batch_size  ──→  PoolEntry.batch_size  ──→  Pool.batch_size
                                                             │
        ┌────────────────────────────────────────────────────┤
        ▼                                                    ▼
StrategyContext                                    GachaService.run_simulation()
(pool.batch_size 可读)                             (读取 pool.batch_size)
        │                                                    │
        ▼                                                    ▼
策略调用                                          if not can_afford_batch(cost, batch_size):
  can_afford_batch(cost, batch_size)                   continue   # 静默跳过
        │                                                    │
        ├─ True  → 返回 DrawAction(pool_id)                  ├─ for i in range(batch_size):
        └─ False → 跳过该池子                                │      spent = state.spend(cost)  # 逐发扣费
                                                             │      pity.before_draw()
                                                             │      reward = pool.draw()
                                                             │      pity.after_draw()
                                                             │      collector.on_draw(spent=...)
                                                             └─ 下一轮迭代
```

### 向后兼容性

| 场景 | batch_size | can_afford_batch | 服务层 | 策略 |
|------|-----------|-----------------|--------|------|
| 现有配置无第 9 列 | 1 (默认) | 退化到 `can_afford` | `range(1)` → 单发 | 无感知 |
| 新建十连池 | 10 | 模拟 10 次独立费用选择 | `range(10)` → 逐发 | 检查 10 发可负担性 |
| 未来 N 连池 | N | 模拟 N 次 | `range(N)` → 逐发 | 检查 N 发可负担性 |

- 不改任何现有 `.txt` 配置文件
- 不改 `DrawAction` 接口
- 不改收集器 / 统计 / 保底引擎接口

---

## 一、文件结构

| 操作 | 文件 | 职责 |
|------|------|------|
| 修改 | `core/pool_config.py:95-109` | `PoolConfig` 新增 `batch_size` 字段 + `parse_schedule_file()` 解析第 9 列 |
| 修改 | `core/pool_config.py:345-355` | `load_config_from_directory()` Pool 构造传 `batch_size` |
| 修改 | `core/config_store.py:21-34` | `PoolEntry` 新增 `batch_size` 字段 |
| 修改 | `core/pool.py:176-188` | `Pool` 新增 `batch_size` 字段 |
| 修改 | `core/state.py` | 新增 `can_afford_batch()` + `_choose_option_from()` |
| 修改 | `core/strategy.py` | 10 处 `can_afford` → `can_afford_batch` |
| 修改 | `core/worst_impact.py:85` | 1 处 `can_afford` → `can_afford_batch` |
| 修改 | `core/config_io.py` | `_save_schedule()` 写第 9 列 + `_load_schedule()` 读第 9 列 |
| 修改 | `service/batch_simulator.py:559` | `from_config_store()` 传 `batch_size` |
| 修改 | `service/gacha_service.py:161-254` | DrawAction 段：预检查 + 批次循环 |
| 修改 | `service/config_service.py:25-60` | `export_pool_to_config()` / `import_pool_from_config()` 涵盖 `batch_size` |
| 修改 | `gui/config_panel.py` | pool_table 新增第 10 列「批次大小」——表头、渲染、编辑、序列化 |
| 修改 | `gui/about_dialog.py:146` | 帮助文本——schedule.txt 格式说明追加 `[批次大小]` 列 |
| 修改 | `config/schedule.txt:2` | 默认配置文件格式注释追加 `[batch_size]` |
| 新建 | `tests/core/test_batch_draw.py` | TDD 测试 |

---

## 二、测试策略

| 阶段 | 测试方式 | 测试文件 |
|------|---------|---------|
| Task 1-2 数据模型 | 单元测试——字段默认值 + 解析 | `tests/core/test_batch_draw.py` |
| Task 3 资源检查 | **TDD**——先写测试再实现 | `tests/core/test_batch_draw.py` |
| Task 4 策略适配 | 更新现有测试断言（若涉及）+ batch_size>1 mock 验证 + 回归 | `pytest -q` |
| Task 5a 批次循环骨架 | 集成测试——N 发执行 + 资源不足跳过 | `tests/core/test_batch_draw.py` |
| Task 5b 保底逐发调用 | 单元测试——mock 保底引擎逐发 before_draw/after_draw 调用次数 | `tests/core/test_batch_draw.py` |
| Task 5c wait_gains 归因 | 确认测试——现有逻辑无需修改，仅首击承载等待收益 | `tests/core/test_batch_draw.py` |
| Task 6 端到端 | CLI/GUI 回归 + 手动目视 | `python -m gacha_simulator.cli -n 100 -s 42` + GUI 池子表格验证 |
| Task 7a 表格列基础设施 | 手动目视——表格列显示、表头、背景色 | GUI 启动 → Config 面板 → 池子 Tab |
| Task 7b 数据流路径 | 手动目视——编辑、保存、重载一致性 | GUI 启动 → 编辑池子 → 保存 → 重载验证 |
| Task 7c 模板与复制路径 | 手动目视——模板批量创建 + 复制 | GUI 启动 → 模板创建池子 → 复制验证 |

---

## 三、任务分解

### Task 1: 数据模型——PoolConfig / PoolEntry / Pool 新增 batch_size

**Files:**
- Modify: `core/pool_config.py:95-109`
- Modify: `core/config_store.py:21-34`
- Modify: `core/pool.py:176-188`
- Modify: `service/batch_simulator.py:555-569`
- New: `tests/core/test_batch_draw.py`

- [ ] **Step 1: 写测试——PoolConfig 默认 batch_size=1**

```python
# tests/core/test_batch_draw.py
import pytest
from gacha_simulator.core.pool_config import PoolConfig

def test_pool_config_batch_size_default():
    """PoolConfig.batch_size 默认值为 1。"""
    pc = PoolConfig(
        pool_id='test', name='test', start_day=0, end_day=21,
        cost_str='draw_resource:160', distribution_file='pools/test.txt',
    )
    assert pc.batch_size == 1
```

- [ ] **Step 2: 跑测试确认失败**

```bash
pytest tests/core/test_batch_draw.py::test_pool_config_batch_size_default -v
```
预期：`AttributeError: 'PoolConfig' object has no attribute 'batch_size'`

- [ ] **Step 3: PoolConfig 加字段**

```python
# core/pool_config.py:95
@dataclass
class PoolConfig:
    pool_id: str
    name: str
    start_day: float
    end_day: float
    cost_str: str
    distribution_file: str
    bindings: Dict[str, str] = field(default_factory=dict)
    target_specs: List[Tuple[str, int]] = field(default_factory=list)
    rerun_of: Optional[str] = None
    exchange_card_id: Optional[str] = None
    batch_size: int = 1  # ← 新增
```

- [ ] **Step 4: 写测试——Pool 默认 batch_size=1**

```python
def test_pool_batch_size_default():
    """运行时 Pool 对象 batch_size 默认为 1。"""
    from gacha_simulator.core.pool import Pool
    pool = Pool(id='test', name='test', cost=[], rewards=[])
    assert pool.batch_size == 1
```

- [ ] **Step 5: Pool 加字段**

```python
# core/pool.py:176
@dataclass
class Pool:
    id: str
    name: str
    cost: PoolCost
    rewards: List[Tuple[Reward, float]]
    available_from: Optional[float] = None
    available_until: Optional[float] = None
    is_exchange: bool = False
    is_rerun: bool = False
    original_pool_id: Optional[str] = None
    exchange_card_id: Optional[str] = None
    pool_type: str = ''
    batch_size: int = 1  # ← 新增
```

- [ ] **Step 6: PoolEntry 加字段**

```python
# core/config_store.py:21
@dataclass
class PoolEntry:
    enabled: bool = True
    pool_id: str = ''
    name: str = ''
    pool_type: str = ''
    start_day: int = 0
    end_day: int = 21
    cost: str = 'draw_resource:160'
    distribution_file: str = ''
    bindings: Dict[str, str] = field(default_factory=dict)
    target_specs: List[tuple] = field(default_factory=list)
    rerun_of: Optional[str] = None
    exchange_card_id: Optional[str] = None
    distribution: List[PoolDistEntry] = field(default_factory=list)
    batch_size: int = 1  # ← 新增
```

- [ ] **Step 7: from_config_store 传 batch_size**

```python
# service/batch_simulator.py:559 附近
pool = Pool(
    id=pid,
    name=getattr(pe, 'name', pid),
    cost=parsed_cost,
    rewards=rewards,
    available_from=start_day * DAY,
    available_until=end_day * DAY,
    is_exchange=bool(exchange_cid),
    exchange_card_id=exchange_cid,
    pool_type=ptype,
    batch_size=getattr(pe, 'batch_size', 1),  # ← 新增
)
```

- [ ] **Step 7.5: load_config_from_directory 传 batch_size** <!-- REVIEW-R1-FIX: ISSUE-001 -->

> `load_config_from_directory()`（`pool_config.py:345-355`）是 `Pool()` 的第二构造点，`core/__init__.py` 公开导出。当前代码库内无直接调用方（仅旧 CLI 路径 `parse_schedule_file` + `load_config_from_directory`），但属公开接口，需保持一致。

```python
# core/pool_config.py:345-355 —— Pool() 构造追加 batch_size
pool = Pool(
    id=pc.pool_id, name=pc.name,
    cost=pc.cost,
    rewards=rewards,
    available_from=pc.start_day * DAY,
    available_until=pc.end_day * DAY,
    is_rerun=pc.rerun_of is not None,
    original_pool_id=pc.rerun_of,
    is_exchange=pc.exchange_card_id is not None,
    exchange_card_id=pc.exchange_card_id,
    batch_size=getattr(pc, 'batch_size', 1),  # ← 新增
)
```

- [ ] **Step 8: 跑测试确认通过**

```bash
pytest tests/core/test_batch_draw.py::test_pool_config_batch_size_default tests/core/test_batch_draw.py::test_pool_batch_size_default -v
```
预期：2 PASS

- [ ] **Step 9: 跑全量回归确认无破坏**

```bash
pytest -q
```
预期：全部通过（现有测试不使用 batch_size，无影响）

- [ ] **Step 10: 提交**

```bash
git add core/pool_config.py core/config_store.py core/pool.py service/batch_simulator.py tests/core/test_batch_draw.py
git commit -m "feat: Pool/PoolConfig/PoolEntry 新增 batch_size 字段（默认 1）"
```

---

### Task 2: 配置读写——schedule.txt 第 9 列 batch_size

**Files:**
- Modify: `core/pool_config.py:146-166` (`parse_schedule_file`)
- Modify: `core/config_io.py:414` (`_save_schedule`)
- Modify: `core/config_io.py:86` (`_load_schedule`)

- [ ] **Step 1: 写解析测试**

```python
def test_parse_schedule_with_batch_size():
    """schedule.txt 第 9 列 batch_size 被正确解析。"""
    from gacha_simulator.core.pool_config import parse_schedule_file
    import tempfile, os
    content = (
        "pool_b10 | 十连池 | 0 | 21 | draw_resource:160 | pools/test.txt | ssr=c1 | | 10\n"
        "pool_s1  | 单抽池 | 0 | 21 | draw_resource:160 | pools/test.txt | ssr=c1 | | 1\n"
        "pool_def | 默认池 | 0 | 21 | draw_resource:160 | pools/test.txt | ssr=c1\n"
    )
    tmp = tempfile.NamedTemporaryFile(mode='w', suffix='.txt', delete=False, encoding='utf-8')
    try:
        tmp.write(content)
        tmp.close()
        configs, _ = parse_schedule_file(tmp.name)
        assert configs[0].batch_size == 10
        assert configs[1].batch_size == 1
        assert configs[2].batch_size == 1   # 缺省
    finally:
        os.unlink(tmp.name)
```

- [ ] **Step 2: 跑测试确认失败**

```bash
pytest tests/core/test_batch_draw.py::test_parse_schedule_with_batch_size -v
```
预期：FAIL（`batch_size` 未从 schedule.txt 解析）

- [ ] **Step 3: 修改 parse_schedule_file 解析第 9 列**

```python
# core/pool_config.py:146 附近，target_specs 解析之后、PoolConfig 构造之前
batch_size = 1
if len(parts) > 8 and parts[8].strip():
    try:
        batch_size = int(parts[8].strip())
    except ValueError:
        batch_size = 1

# PoolConfig 构造加上 batch_size
pc = PoolConfig(
    pool_id=pool_id, name=name,
    start_day=start_day, end_day=end_day,
    cost_str=cost_str,
    distribution_file=dist_file, bindings=bindings,
    target_specs=target_specs,
    rerun_of=rerun_of,
    exchange_card_id=exchange_card_id,
    batch_size=batch_size,  # ← 新增
)
```

- [ ] **Step 4: 修改 _save_schedule 写第 9 列 + 注释**

```python
# core/config_io.py:418 —— 更新注释
f.write("# Format: pool_id | name | start_day | end_day | cost | template | bindings(key=val) | target_card_ids | [batch_size]\n")

# core/config_io.py:441 —— 修改写入行
batch_str = f" | {pool.batch_size}" if getattr(pool, 'batch_size', 1) > 1 else ""
f.write(f"{pool.pool_id} | {pool.name} | {pool.start_day} | {pool.end_day} | {pool.cost} | {dist_file} | {bindings_str} | {target_str}{batch_str}\n")
```

- [ ] **Step 5: 修改 _load_schedule 传递 batch_size**

```python
# core/config_io.py:154-167 —— PoolEntry 构造加 batch_size
pool_entry = PoolEntry(
    enabled=True,
    pool_id=pc.pool_id,
    name=pc.name,
    start_day=int(pc.start_day),
    end_day=int(pc.end_day),
    cost=pc.cost_str,
    distribution_file=dist_file,
    bindings=dict(pc.bindings),
    target_specs=list(pc.target_specs),
    rerun_of=pc.rerun_of,
    exchange_card_id=pc.exchange_card_id,
    distribution=distribution,
    batch_size=getattr(pc, 'batch_size', 1),  # ← 新增
)
```

- [ ] **Step 6: 添加 batch_size ≥ 1 值域验证（含防御性上界）** <!-- REVIEW-R1-FIX: ISSUE-002 -->

```python
# core/pool_config.py parse_schedule_file 中，int() 转换之后
batch_size = 1
if len(parts) > 8 and parts[8].strip():
    try:
        batch_size = int(parts[8].strip())
        if batch_size < 1:
            batch_size = 1  # 静默修正非法值
        elif batch_size > 1000:
            batch_size = 1000  # 防御性上界：防止 can_afford_batch O(N) 阻塞
    except (ValueError, TypeError):
        batch_size = 1
```

- [ ] **Step 6.5: 更新仓库内预置 schedule.txt 格式注释** <!-- REVIEW-R1-FIX: ISSUE-006 -->

> `gacha_simulator/config/schedule.txt` 第 2 行当前格式注释不含 `[batch_size]`。虽然 `_save_schedule()` 下次保存时会自动写入新注释，但新用户首次使用此默认文件时注释不反映实际支持的 9 列格式，需同步更新。

```python
# gacha_simulator/config/schedule.txt:2 —— 注释末尾追加
# Format: pool_id | name | start_day | end_day | cost | template | bindings(key=val) | target_card_ids | [batch_size]
```

- [ ] **Step 6.6: 更新 about_dialog.py 帮助文本** <!-- REVIEW-R1-FIX: ISSUE-004 -->

> `about_dialog.py` 第 146 行内嵌 HTML 帮助文本是用户通过 GUI（帮助→配置文件指南）查阅格式的唯一入口，当前缺少 `[批次大小]` 列说明。

```python
# gui/about_dialog.py:146 —— <pre> 标签末尾追加
# 改前：
# <pre>pool_id | 名称 | 开始天 | 结束天 | 费用 | 分布文件 | 绑定(k=v;k=v) | 目标卡(逗号分隔,可选:数量)</pre>
# 改后：
# <pre>pool_id | 名称 | 开始天 | 结束天 | 费用 | 分布文件 | 绑定(k=v;k=v) | 目标卡(逗号分隔,可选:数量) | [批次大小]</pre>
# 并在 <pre> 下方追加一行说明：
# <p><b>批次大小</b>：单次动作的抽卡发数（十连=10），默认 1。可选列，省略时默认单抽。</p>
```

- [ ] **Step 7: 跑测试确认通过**

```bash
pytest tests/core/test_batch_draw.py::test_parse_schedule_with_batch_size -v
```
预期：PASS

- [ ] **Step 8: 提交**

```bash
git add core/pool_config.py core/config_io.py gacha_simulator/config/schedule.txt gui/about_dialog.py
git commit -m "feat: schedule.txt 第 9 列 batch_size 读写支持 + 帮助/注释同步"
```

---

### Task 3: 资源检查——can_afford_batch + _choose_option_from

**Files:**
- Modify: `core/state.py`
- Modify: `tests/core/test_batch_draw.py`（追加）

- [ ] **Step 1: 写 TDD 测试——can_afford_batch 单资源**

```python
class TestCanAffordBatch:
    def test_single_resource_sufficient(self):
        """单一资源足够 batch_size 发。"""
        from gacha_simulator.core.state import GachaState
        state = GachaState(resources={'draw_resource': 2000})
        cost = {'draw_resource': 160}
        assert state.can_afford_batch(cost, 10) is True

    def test_single_resource_insufficient(self):
        """单一资源不够 batch_size 发。"""
        state = GachaState(resources={'draw_resource': 1500})
        cost = {'draw_resource': 160}
        assert state.can_afford_batch(cost, 10) is False

    def test_batch_size_1_delegates_to_can_afford(self):
        """batch_size=1 退化为 can_afford。"""
        state = GachaState(resources={'draw_resource': 200})
        cost = {'draw_resource': 160}
        assert state.can_afford_batch(cost, 1) == state.can_afford(cost)
```

- [ ] **Step 2: 跑测试确认失败**

```bash
pytest tests/core/test_batch_draw.py::TestCanAffordBatch -v
```
预期：3 FAIL（`can_afford_batch` 未定义）

- [ ] **Step 3: 实现 can_afford_batch + _choose_option_from**

```python
# core/state.py GachaState 类中新增

def _choose_option_from(self, cost, resources: dict) -> Optional[CostOption]:
    """从给定资源快照中选择最优 cost option（不修改真实资源）。"""
    if isinstance(cost, dict):
        if all(resources.get(r, 0) >= a for r, a in cost.items()):
            return cost
        return None
    if isinstance(cost, list):
        for option in cost:
            if all(resources.get(r, 0) >= a for r, a in option.items()):
                return option
        return None
    return None

def can_afford_batch(self, cost, batch_size: int = 1) -> bool:
    """
    模拟 batch_size 次独立抽卡的费用选择。

    每次独立选择最优 cost option，资源快照逐次递减。
    支持 free_draw:1 > draw_resource:160 等 OR 优先级资源自动切换。

    batch_size=1 退化到 can_afford()。
    """
    if batch_size <= 1:
        return self.can_afford(cost)
    remaining = dict(self.resources)
    for _ in range(batch_size):
        option = self._choose_option_from(cost, remaining)
        if option is None:
            return False
        for res, amt in option.items():
            remaining[res] -= amt
    return True
```

- [ ] **Step 4: 跑测试确认通过**

```bash
pytest tests/core/test_batch_draw.py::TestCanAffordBatch -v
```
预期：3 PASS

- [ ] **Step 5: 追加多资源优先级切换测试**

```python
def test_multi_resource_priority_switching(self):
    """免费抽耗尽后自动切换到付费资源。"""
    state = GachaState(resources={
        'free_draw': 5,
        'draw_resource': 2000,
    })
    cost = [{'free_draw': 1}, {'draw_resource': 160}]
    # 5 发免费 + 5 发付费 = 需要 free_draw:5 + draw_resource:800
    assert state.can_afford_batch(cost, 10) is True

def test_multi_resource_insufficient_after_priority_exhaustion(self):
    """免费抽耗尽后付费资源不足 → False。"""
    state = GachaState(resources={
        'free_draw': 5,
        'draw_resource': 400,  # 只够 2 发付费
    })
    cost = [{'free_draw': 1}, {'draw_resource': 160}]
    # 5 发免费 + 5 发付费 = 需要 draw_resource:800，只有 400
    assert state.can_afford_batch(cost, 10) is False

def test_batch_size_1_multi_resource(self):
    """batch_size=1 多资源退化测试。"""
    state = GachaState(resources={'free_draw': 1})
    cost = [{'free_draw': 1}, {'draw_resource': 160}]
    assert state.can_afford_batch(cost, 1) is True
    state2 = GachaState(resources={'draw_resource': 200})
    assert state2.can_afford_batch(cost, 1) is True
    state3 = GachaState(resources={})
    assert state3.can_afford_batch(cost, 1) is False
```

- [ ] **Step 6: 跑全部测试**

```bash
pytest tests/core/test_batch_draw.py::TestCanAffordBatch -v
```
预期：6 PASS

- [ ] **Step 7: 提交**

```bash
git add core/state.py tests/core/test_batch_draw.py
git commit -m "feat: GachaState 新增 can_afford_batch——模拟 N 发独立费用选择"
```

---

### Task 4: 策略适配——11 处 can_afford → can_afford_batch

**Files:**
- Modify: `core/strategy.py`（10 处）
- Modify: `core/worst_impact.py`（1 处）

- [ ] **Step 1: 定位所有替换点**

当前 11 处 `ctx.state.can_afford(pool.cost)` 调用点（已逐行审计 line-level 确认）：

| # | 文件:行号 | 所属策略 | 上下文 |
|---|----------|---------|--------|
| 1 | strategy.py:105 | SmartStrategy._get_needed_card_exchange | 兑换池可负担性 |
| 2 | strategy.py:117 | SmartStrategy.select_action | 目标池可负担性 |
| 3 | strategy.py:153 | PoolQuotaStrategy.select_action | 兑换池可负担性 |
| 4 | strategy.py:157 | PoolQuotaStrategy.select_action | 目标池跳过条件 |
| 5 | strategy.py:167 | PoolQuotaStrategy.select_action | 目标池可负担性 |
| 6 | strategy.py:207 | **PityReserveStrategy**.select_action | 兑换池可负担性 |
| 7 | strategy.py:211 | **PityReserveStrategy**.select_action | 目标池跳过条件 |
| 8 | strategy.py:265 | **StopOnTargetStrategy**.select_action | 兑换池可负担性 |
| 9 | strategy.py:269 | **StopOnTargetStrategy**.select_action | 目标池可负担性 |
| 10 | strategy.py:310 | **TargetHuntingStrategy**.select_action | 目标池可负担性 |
| 11 | worst_impact.py:85 | **DrawTargetStrategy**.select_action | 类定义在 worst_impact.py:72 |

> **注意：`FixedCountStrategy`（strategy.py:281-295）不调用 `can_afford`——它无条件从 `current_pools[0]` 抽卡，无需修改。** 实际 8 个注册策略中 6 个使用 can_afford（`smart`/`pool_quota`/`pity_reserve`/`stop_on_target`/`target_hunting`/`draw_target`），`fixed_count` 和 `no_draw` 不使用。

- [ ] **Step 2: 逐处替换**

每处替换模式相同——`ctx.state.can_afford(pool.cost)` → `ctx.state.can_afford_batch(pool.cost, pool.batch_size)`。

以 `strategy.py:105` 为例：
```python
# 改前
if pool.is_available_at(ctx.state.real_time) and ctx.state.can_afford(pool.cost):

# 改后
if pool.is_available_at(ctx.state.real_time) and ctx.state.can_afford_batch(pool.cost, pool.batch_size):
```

以 `strategy.py:157` 为例（单独一行的 can_afford 调用）：
```python
# 改前
if pool.is_exchange or not ctx.state.can_afford(pool.cost):

# 改后
if pool.is_exchange or not ctx.state.can_afford_batch(pool.cost, pool.batch_size):
```

- [ ] **Step 2.5: 写 batch_size>1 策略行为测试** <!-- REVIEW-R1-FIX: ISSUE-006 -->

> GATE-check-6 发现：Task 4 仅依赖 batch_size=1 退化等价性验证，缺少 batch_size>1 的针对性测试。若替换时误写成 `can_afford_batch(pool.cost, 1)`（硬编码而非 `pool.batch_size`），回归测试无法发现。以下测试构造 batch_size=10 的 Pool，mock 策略返回 DrawAction，断言 `can_afford_batch` 第二个参数为 `pool.batch_size`（10，非硬编码 1）。

```python
def test_strategy_can_afford_batch_uses_pool_batch_size():
    """策略调用 can_afford_batch 时第二个参数为 pool.batch_size（非硬编码 1）。"""
    from unittest.mock import MagicMock
    from gacha_simulator.core.pool import Pool, Reward
    from gacha_simulator.core.state import GachaState
    from gacha_simulator.core.strategy import SmartStrategy, StrategyContext
    from gacha_simulator.core.target_card import TargetCardSet

    pool = Pool(
        id='test_b10', name='十连池', cost=[{'coin': 10}],
        rewards=[(Reward(id='card_a', name='A'), 1.0)],
        batch_size=10
    )
    state = GachaState(resources={'coin': 100})
    original_can_afford_batch = state.can_afford_batch
    call_args = []
    def spy(cost, batch_size):
        call_args.append((cost, batch_size))
        return original_can_afford_batch(cost, batch_size)
    state.can_afford_batch = spy

    ctx = StrategyContext(
        state=state, current_pools=[pool], all_pools=[pool],
        target_cards=TargetCardSet([]), acquired={},
        pool_draw_counts={'test_b10': 0}, total_draws=0,
        ssr_ids=set(),
    )
    strategy = SmartStrategy()
    action = strategy.select_action(ctx)
    # 关键断言：can_afford_batch 的第二个参数为 pool.batch_size (10)，非硬编码 1
    assert len(call_args) >= 1
    assert call_args[0][1] == pool.batch_size
    assert call_args[0][1] == 10
```

```bash
pytest tests/core/test_batch_draw.py::test_strategy_can_afford_batch_uses_pool_batch_size -v
```
预期：PASS（验证 `can_afford_batch` 第二个参数为 `pool.batch_size=10`）

- [ ] **Step 3: 跑全量回归确认无破坏**

```bash
pytest -q
```
预期：全部通过（batch_size 默认为 1 时 `can_afford_batch` 退化到 `can_afford`，行为完全一致）

- [ ] **Step 3.5: 确认已排除的构造点** <!-- REVIEW-R1-FIX: ISSUE-008 -->

> `worst_impact.py:226-233` `prepare_simulation_config()` 中为最差影响分析创建 `MAX_POOLS` 个虚拟 `Pool` 对象，不含 `batch_size` 参数。**此行为正确——默认 `batch_size=1` 恰好符合分析预期**（分析中用 `ConsecutivePoolTargetCondition` 逐池判断，不需要批次语义）。无需修改代码，仅在此标注供未来维护者参考。
>
> 同时确认 `worst_impact.py:85` 的 `can_afford`→`can_afford_batch` 替换已在 Step 2 正确覆盖。

- [ ] **Step 4: 提交**

```bash
git add core/strategy.py core/worst_impact.py
git commit -m "feat: 策略层 11 处 can_afford 替换为 can_afford_batch——支持 batch_size 感知"
```

---

### Task 5a: 服务层——批次循环骨架 <!-- REVIEW-R1-FIX: ISSUE-001 -->

> **估计工时：** 45min | **依赖：** Task 3 (can_afford_batch)
> 
> **职责：** 原子预检查 + `for i in range(batch_size)` + `spend()` 移入循环 + 防御性 `break`。
> 不涉及保底缓存处理（→5b）和 `_pending_wait_gains` 归因（→5c）。
> 
> **被替换原始代码行范围：** `gacha_service.py:161-254`（单发 DrawAction 段约 94 行）。本子任务引入批次循环骨架，5b/5c 在此基础上追加保底和等待收益修改。

**Files:**
- Modify: `service/gacha_service.py:161-254`
- Modify: `tests/core/test_batch_draw.py`（追加）

- [ ] **Step 1: 写集成测试——批次循环骨架**

```python
class TestGachaServiceBatchDrawSkeleton:
    """Task 5a: 批次循环骨架——预检查 + 循环 + 逐发 spend + break。"""

    def test_batch_draw_executes_n_draws(self):
        """batch_size=3 的池子一次 DrawAction 执行 3 发。"""
        from gacha_simulator.core.pool import Pool, Reward
        from gacha_simulator.core.state import GachaState
        from gacha_simulator.core.action import DrawAction
        from gacha_simulator.core.strategy import Strategy, StrategyContext
        from gacha_simulator.core.stop_condition import StopCondition
        from gacha_simulator.core.target_card import TargetCardSet
        from gacha_simulator.service.gacha_service import GachaService

        pool = Pool(
            id='test_batch', name='batch池', cost=[{'coin': 10}],
            rewards=[(Reward(id='card_a', name='A'), 1.0)],
            batch_size=3,
        )
        state = GachaState(resources={'coin': 100})

        class FixedDraw(Strategy):
            def select_action(self, ctx):
                return DrawAction(pool_id='test_batch')

        stop_hit = []
        class OneActionStop(StopCondition):
            def check(self, state, pools, stats):
                if stats.total_draws >= 3:
                    stop_hit.append(True)
                    return True
                return False

        service = GachaService(
            pools=[pool], strategy=FixedDraw(),
            stop_condition=OneActionStop(),
            target_cards=TargetCardSet([]),
            pity_engine=None, resource_gain=None,
            schedule_manager=None, card_defs=[], ssr_ids=set(),
        )
        result = service.run_simulation_compact(state, max_iterations=100)
        assert result.total_draws == 3
        assert result.total_consumed.get('coin', 0) == 30

    def test_batch_draw_insufficient_skips(self):
        """资源不足 batch_size 发时静默跳过。"""
        pool = Pool(
            id='test_batch', name='batch池', cost=[{'coin': 10}],
            rewards=[(Reward(id='card_a', name='A'), 1.0)],
            batch_size=10,
        )
        state = GachaState(resources={'coin': 50})

        class FixedDraw(Strategy):
            def select_action(self, ctx):
                return DrawAction(pool_id='test_batch')

        class NeverStop(StopCondition):
            def check(self, state, pools, stats):
                return stats.total_draws > 0

        service = GachaService(
            pools=[pool], strategy=FixedDraw(),
            stop_condition=NeverStop(),
            target_cards=TargetCardSet([]),
            pity_engine=None, resource_gain=None,
            schedule_manager=None, card_defs=[], ssr_ids=set(),
        )
        result = service.run_simulation_compact(state, max_iterations=100)
        assert result.total_draws == 0
```

- [ ] **Step 2: 跑测试确认失败 (2 个新测试)**

```bash
pytest tests/core/test_batch_draw.py::TestGachaServiceBatchDrawSkeleton -v
```
预期：FAIL（服务层尚未修改）

- [ ] **Step 3: 实现批次循环骨架**

```python
# service/gacha_service.py:161 附近
# 将现有单发 DrawAction 段替换为以下批次循环骨架

if _isinstance(action, _DrawAction):
    pool = _pools.get(action.pool_id)
    if not pool:
        raise ValueError(f"Pool not found: {action.pool_id}")

    batch_size = max(getattr(pool, 'batch_size', 1), 1)

    # ── 原子预检查 ──
    if not state.can_afford_batch(pool.cost, batch_size):
        continue

    # ── 逐发执行 ──
    for _ in range(batch_size):
        # 每发独立扣费（spend 根据当前剩余资源动态选择最优方式）
        spent = state.spend(pool.cost)
        if spent is None:
            # 防御性：can_afford_batch 已预检查，理论上不应发生
            # 极端情况（如奖励扣减资源导致中途枯竭）→ 停止本批次剩余发
            break

        probabilities = {r.id: p for r, p in pool.rewards}
        # 注意：当前阶段保底仍沿用 ctx._pity_cache（与单发一致），下一子任务 5b 中移除缓存依赖
        original_probs = probabilities.copy() if _pity_engine else None
        if _pity_engine:
            cached_probs = ctx._pity_cache.get(pool.id)
            if cached_probs is not None:
                probabilities = cached_probs
            else:
                probabilities = _pity_engine.before_draw(pool.id, pity_state, probabilities)
            pool._apply_probabilities(probabilities)

        reward = pool.draw()

        # [保底触发检测逻辑——与现有单发代码一致，暂不变]
        pity_triggered = False
        triggered_pity_name = None
        if original_probs is not None:
            for card_id, orig_prob in original_probs.items():
                new_prob = probabilities.get(card_id, 0)
                if new_prob > orig_prob * 1.01:
                    pity_triggered = True
                    break
        if _pity_engine and pity_triggered:
            spec = _pity_engine.get_spec(pool.id)
            if spec:
                triggered_names = []
                for pname in spec.pity_names:
                    behavior = _pity_engine.behaviors.get(pname)
                    if behavior is None: continue
                    cv = pity_state.get(pname)
                    if behavior.is_active(cv): triggered_names.append(pname)
                triggered_pity_name = ','.join(triggered_names) if triggered_names else None

        pool_spec = _pity_engine.get_spec(pool.id) if _pity_engine else None
        pool_counter_max = 0
        if pool_spec:
            for pname in pool_spec.pity_names:
                cv = pity_state.get(pname)
                pool_counter_max = max(pool_counter_max, cv)

        if _pity_engine:
            _pity_engine.after_draw(pool.id, pity_state, reward.id)

        stats.on_draw(reward.id, pool.id, pity_triggered)
        if pity_triggered:
            stats.pity_triggers += 1

        # [奖励资源逻辑——与现有单发代码一致，不变]
        rg = dict(reward.resources_gained or {})
        if reward.first_time_bonus or reward.nth_time_bonus or reward.excess_bonus:
            ac_new = stats.acquired_counts.get(reward.id, 0)
            init = _initial_counts.get(reward.id, 0)
            total_before = init + ac_new - 1
            total_after = init + ac_new
            bonus = compute_bonus_resources(reward, total_before, total_after)
            for k, v in bonus.items():
                rg[k] = rg.get(k, 0) + v
        if rg:
            for k, v in rg.items():
                resources[k] = resources.get(k, 0) + v

        # [Compact 记录——保持现有 _pending_wait_gains 行为，5c 确认无需修改]
        if _is_compact:
            for k, v in spent.items():
                total_consumed[k] = total_consumed.get(k, 0) + v
            if rg:
                for k, v in rg.items():
                    total_gained[k] = total_gained.get(k, 0) + v
            combined_gained = dict(rg)
            for k, v in _pending_wait_gains.items():
                combined_gained[k] = combined_gained.get(k, 0) + v
            _pending_wait_gains.clear()
        else:
            combined_gained = rg.copy() if rg else _EMPTY_DICT

        collector.on_draw(
            card_id=reward.id, pool=pool, spent=spent,
            resources_gained=rg, pity_triggered=pity_triggered,
            triggered_pity_name=triggered_pity_name,
            pity_counter_max=pool_counter_max,
            real_time=real_time, pity_state=pity_state,
            combined_gained=combined_gained,
        )
```

- [ ] **Step 4: 跑集成测试确认通过**

```bash
pytest tests/core/test_batch_draw.py::TestGachaServiceBatchDrawSkeleton -v
```
预期：2 PASS（N 发执行 + 资源不足跳过）

- [ ] **Step 5: 提交**

```bash
git add service/gacha_service.py tests/core/test_batch_draw.py
git commit -m "feat(5a): GachaService 批次循环骨架——预检查 + for range(batch_size) + 防御性 break"
```

---

### Task 5b: 保底逐发调用——移除 ctx._pity_cache 依赖 <!-- REVIEW-R1-FIX: ISSUE-001 -->

> **估计工时：** 30min | **依赖：** Task 5a（批次循环骨架已就位）
> 
> **职责：** 批次第 2 发起 `ctx._pity_cache` 已过期（第 1 发保底状态变化后缓存不再有效），必须改为逐发直调 `before_draw()` / `after_draw()`。

**Files:**
- Modify: `service/gacha_service.py`（5a 产物中保底相关行）
- Modify: `tests/core/test_batch_draw.py`（追加保底逐发测试）

- [ ] **Step 1: 写保底逐发调用测试**

```python
def test_batch_draw_pity_called_per_draw(self):
    """保底引擎逐发调用 before_draw / after_draw——不使用过期缓存。"""
    from unittest.mock import MagicMock
    pool = Pool(
        id='test_batch', name='batch池', cost=[{'coin': 10}],
        rewards=[(Reward(id='card_a', name='A'), 1.0)],
        batch_size=3,
    )
    state = GachaState(resources={'coin': 100})

    class FixedDraw(Strategy):
        def select_action(self, ctx):
            return DrawAction(pool_id='test_batch')

    class ThreeDrawStop(StopCondition):
        def check(self, state, pools, stats):
            return stats.total_draws >= 3

    mock_pity = MagicMock()
    mock_pity.get_spec.return_value = None
    mock_pity.before_draw.return_value = {'card_a': 1.0}

    service = GachaService(
        pools=[pool], strategy=FixedDraw(),
        stop_condition=ThreeDrawStop(),
        target_cards=TargetCardSet([]),
        pity_engine=mock_pity, resource_gain=None,
        schedule_manager=None, card_defs=[], ssr_ids=set(),
    )
    result = service.run_simulation_compact(state, max_iterations=100)
    assert result.total_draws == 3
    # 关键断言：batch_size=3 → before_draw 调用 3 次（非 1 次缓存复用）
    assert mock_pity.before_draw.call_count == 3
    assert mock_pity.after_draw.call_count == 3
```

- [ ] **Step 2: 跑测试确认失败**

```bash
pytest tests/core/test_batch_draw.py::test_batch_draw_pity_called_per_draw -v
```
预期：FAIL（`before_draw` 仍使用 `ctx._pity_cache`，调用次数少于 3）

- [ ] **Step 3: 移除 ctx._pity_cache 依赖——逐发直调 before_draw**

对 5a 产物中保底段的精确修改（仅改 5 行）：

```python
# ─── 改前（5a 产物，沿用 ctx._pity_cache）───
        if _pity_engine:
            cached_probs = ctx._pity_cache.get(pool.id)
            if cached_probs is not None:
                probabilities = cached_probs
            else:
                probabilities = _pity_engine.before_draw(
                    pool.id, pity_state, probabilities
                )
            pool._apply_probabilities(probabilities)

# ─── 改后（5b 产物，逐发直调）───
        if _pity_engine:
            # 不使用 ctx._pity_cache——批次中保底状态逐发变化，第 2 发起缓存已过期
            probabilities = _pity_engine.before_draw(
                pool.id, pity_state, probabilities
            )
            pool._apply_probabilities(probabilities)
```

- [ ] **Step 4: 跑保底测试确认通过**

```bash
pytest tests/core/test_batch_draw.py::test_batch_draw_pity_called_per_draw -v
```
预期：PASS（`before_draw` / `after_draw` 各调用 3 次）

- [ ] **Step 5: 提交**

```bash
git add service/gacha_service.py tests/core/test_batch_draw.py
git commit -m "feat(5b): 批次抽卡保底逐发调用——移除 ctx._pity_cache 依赖"
```

---

### Task 5c: _pending_wait_gains 批次归因——仅首击承载等待收益

> **估计工时：** 15min | **依赖：** Task 5a + 5b（批次循环 + 保底逐发已就位）
>
> **关键发现：不需要改代码。** 等待收益在 WaitAction 时已入账 `resources`/`total_gained`，`combined_gained` 只是给分析面板的「资金来源」标签。`streaming.py` 逐发**累加** `combined_gained` 重建 `cum_gained`——若每发都附会导致 N× 虚增。现有单发逻辑「第一发合并 + 立即 `clear()`」在批次循环中天然表现正确：仅第一发含等待收益，后续发为空，累加不会虚增。

**验证：不必修改，确认现有逻辑在批次中行为正确即可**

```python
# 5a/5b 产物中 Compact 记录段——保持不变
    for _ in range(batch_size):
        # ... spend / pity / draw / reward ...

        if _is_compact:
            for k, v in spent.items():
                total_consumed[k] = total_consumed.get(k, 0) + v
            if rg:
                for k, v in rg.items():
                    total_gained[k] = total_gained.get(k, 0) + v
            combined_gained = dict(rg)
            for k, v in _pending_wait_gains.items():  # 仅第 1 发有值
                combined_gained[k] = combined_gained.get(k, 0) + v
            _pending_wait_gains.clear()               # 第 1 发后清空

# streaming.py 累加验证（不会虚增）：
#   第 1 发：combined_gained={coin:100} → cum_gained={coin:100}
#   第 2 发：combined_gained={}         → cum_gained={coin:100} 不变
#   第 N 发：combined_gained={}         → cum_gained={coin:100} 不变
```

- [ ] **Step 1: 写显式回归测试——_pending_wait_gains 批次归因不虚增** <!-- REVIEW-R2-FIX: OPT-004 -->

> 5a/5b 的集成测试验证了 `total_draws` 和资源消耗，但未显式锁定「`_pending_wait_gains` 不会因批次循环导致 N× 虚增」这一结构约束。以下测试 spy `collector.on_draw` 记录每发的 `combined_gained`，确保批次每发独立归因、无残留。真正的等待收益注入需 WaitAction→DrawAction 联动，属更广的集成测试范畴——此处先锁定 `on_draw` 调用次数 = batch_size 这一结构性前提。

```python
def test_batch_on_draw_called_per_draw_not_per_action(self):
    """批次中 collector.on_draw 逐发调用 batch_size 次（非 1 次），确保归因粒度正确。"""
    from gacha_simulator.core.pool import Pool, Reward
    from gacha_simulator.core.state import GachaState
    from gacha_simulator.core.action import DrawAction
    from gacha_simulator.core.strategy import Strategy
    from gacha_simulator.core.stop_condition import StopCondition
    from gacha_simulator.core.target_card import TargetCardSet
    from gacha_simulator.service.gacha_service import GachaService

    pool = Pool(
        id='test_batch', name='batch池', cost=[{'coin': 10}],
        rewards=[(Reward(id='card_a', name='A'), 1.0)],
        batch_size=3,
    )
    state = GachaState(resources={'coin': 100})

    class FixedDraw(Strategy):
        def select_action(self, ctx):
            return DrawAction(pool_id='test_batch')

    class ThreeDrawStop(StopCondition):
        def check(self, state, pools, stats):
            return stats.total_draws >= 3

    on_draw_calls = []
    service = GachaService(
        pools=[pool], strategy=FixedDraw(),
        stop_condition=ThreeDrawStop(),
        target_cards=TargetCardSet([]),
        pity_engine=None, resource_gain=None,
        schedule_manager=None, card_defs=[], ssr_ids=set(),
    )
    # spy collector.on_draw 记录每发调用
    _orig_on_draw = service._collector.on_draw
    def _spy(**kwargs):
        on_draw_calls.append(kwargs)
        _orig_on_draw(**kwargs)
    service._collector.on_draw = _spy

    result = service.run_simulation_compact(state, max_iterations=100)
    assert result.total_draws == 3
    assert len(on_draw_calls) == 3  # ← 核心断言：每发一次 on_draw，非每动作一次
    # 若未来有人误将 collector.on_draw 移到 for 循环外，此断言直接 FAIL
```

```bash
pytest tests/core/test_batch_draw.py::TestGachaServiceBatchDrawSkeleton::test_batch_on_draw_called_per_draw_not_per_action -v
```
预期：PASS（`on_draw` 调用 3 次 = batch_size）

- [ ] **Step 2: 确认现有单元/集成测试覆盖此路径**

```bash
pytest tests/core/test_batch_draw.py -v
```
预期：全部 PASS——5a（循环骨架）+ 5b（保底逐发）+ 5c Step 1（等待收益）的测试覆盖 `_pending_wait_gains` 不为空时的完整路径

- [ ] **Step 3: 跑全量回归**

```bash
pytest -q
```
预期：全部通过

---

### Task 6: 端到端验证 + 收尾

**Files:**
- 无新文件

- [ ] **Step 1: CLI 回归——默认 batch_size=1 行为不变**

```bash
python -m gacha_simulator.cli -n 100 -s 42
```
预期：正常运行，输出统计摘要，无报错

- [ ] **Step 2: 确认现有全部测试绿色**

```bash
pytest -q --cov=gacha_simulator --cov-report=term
```
预期：全部通过，覆盖率不下降

- [ ] **Step 3: 设计文档提交**

```bash
git add docs/01-活跃/subsystems/模拟服务层/P44\ 池子批次抽卡（十连强制）实施计划.md
git commit -m "docs: P44 池子批次抽卡（十连强制）实施计划——就绪"
```

- [ ] **Step 4: 版本号提升——2.1.0 → 2.2.0** <!-- REVIEW-R1-FIX: ISSUE-002 -->

> 本计划为功能新增（batch_size 贯穿 Pool/PoolConfig/PoolEntry 三级模型 + 策略 + 服务 + GUI），按 Pride Versioning 属于 MINOR 级别（DEFAULT）变更。需手动提升版本号。（C1 cron 自动同步到 `技术栈.md`，但 `_version.py` 自身不自动提升。）

```python
# gacha_simulator/_version.py —— 改前
__version__ = "2.1.0"
VERSION_DISPLAY = "v2.1.0"

# gacha_simulator/_version.py —— 改后
__version__ = "2.2.0"
VERSION_DISPLAY = "v2.2.0"
```

同时在 `VERSION_HISTORY` 表首行追加：

```python
("2.2.0", "2026-06-13", "DEFAULT — P44 池子批次抽卡（十连强制）"),
```

```bash
git add gacha_simulator/_version.py
git commit -m "chore: 版本号 2.1.0→2.2.0 (P44 批次抽卡为 MINOR 级变更)"
```

---

### Task 7a: GUI 配置面板——表格列基础设施 <!-- REVIEW-R1-FIX: ISSUE-001 -->

> **估计工时：** 45min | **依赖：** Task 1（PoolEntry.batch_size 已存在）
> 
> **职责：** 新增第 10 列（col 9）「批次大小」的表头、列宽、渲染、新增行默认值、所有背景色循环 range(8)→range(10)。
> 不涉及数据流路径（→7b）和模板/复制路径（→7c）。

**Files:**
- Modify: `gui/config_panel.py`

> **现状：** pool_table 共 9 列（0-8）：启用 | ID | 名称 | 类型 | 开始(天) | 持续(天) | 单抽消耗 | 备注 | 分布编辑
> **目标：** 在「分布编辑」列之后新增第 10 列（col 9）——「批次大小」

- [ ] **Step 1: setColumnCount 9→10**

```python
# gui/config_panel.py:549
self.pool_table.setColumnCount(10)
self.pool_table.setHorizontalHeaderLabels([
    "启用", "ID", "名称", "类型", "开始(天)", "持续(天)", "单抽消耗", "备注", "分布编辑", "批次大小"
])
# 新增列宽
self.pool_table.setColumnWidth(9, 60)
header.setSectionResizeMode(9, QHeaderView.ResizeMode.Fixed)
```

- [ ] **Step 2: _set_pool_table 渲染 batch_size**

```python
# gui/config_panel.py:815 附近（edit_item 之后）
batch_size = p.get('batch_size', 1)
self.pool_table.setItem(i, 9, QTableWidgetItem(str(batch_size)))
```

- [ ] **Step 3: _set_pool_table 背景色循环 range(8) → range(10)**

```python
# gui/config_panel.py:820
for col in range(10):  # 原来是 range(8)，现在覆盖 col 9（批次大小）
    item = self.pool_table.item(i, col)
```

- [ ] **Step 4: _add_pool 默认 batch_size=1**

```python
# gui/config_panel.py:867 附近（edit_item 之后）
batch_item = QTableWidgetItem("1")
self.pool_table.setItem(row, 9, batch_item)
```

- [ ] **Step 5: 手动验证——GUI 启动确认表格列显示正确**

```bash
python -m gacha_simulator.main
```
操作：Config 面板 → 池子 Tab → 确认第 10 列「批次大小」表头可见 / 新增池子行默认值显示 "1" / 所有背景色正常。

- [ ] **Step 6: 提交**

```bash
git add gui/config_panel.py
git commit -m "feat(7a): GUI 池子表格第 10 列「批次大小」基础设施——表头/列宽/渲染/背景色"
```

---

### Task 7b: GUI 配置面板——数据流路径 <!-- REVIEW-R1-FIX: ISSUE-001 -->

> **估计工时：** 45min | **依赖：** Task 7a（表格列基础设施已就位）
> 
> **职责：** `_refresh_from_store_impl()` pools_data 追加 batch_size、`apply_to_store()` 读取 + 上下界校验、`set_config()` 导入透传、`get_config()` 信号追加。
> 不涉及模板/复制路径（→7c）。

**Files:**
- Modify: `gui/config_panel.py`

- [ ] **Step 1: _refresh_from_store_impl() pools_data 中间 dict 追加 batch_size** <!-- REVIEW-R1-FIX: ISSUE-001 -->

> `_refresh_from_store_impl()`（第 2975-2988 行）从 `store.pools` 逐条构建 `pools_data` dict 列表传给 `_set_pool_table()` 渲染表格。当前 dict 不含 `batch_size` 字段，导致 Step 2 的 `p.get('batch_size', 1)` 始终回退为 1——UI 显示值与 Store 实际值断裂。`set_config()`（Step 3）导入配置 → `refresh_from_store()` → 此方法 → `_set_pool_table()` 链路中 batch_size 会静默丢失，且下次 `apply_to_store()`（Step 2）触发时以 UI 中的错误值 1 覆盖 Store 正确值，造成数据丢失。

```python
# gui/config_panel.py:2987 —— 'note' 行之后、')' 闭括号之前追加
pools_data.append({
    'enabled': p.enabled,
    'id': p.pool_id,
    'name': p.name,
    'type': pool_type,
    'start_day': p.start_day,
    'duration': p.end_day - p.start_day,
    'cost': p.cost,
    'note': '',
    'distribution': dist_list,
    'batch_size': getattr(p, 'batch_size', 1),  # ← 新增
})
```

- [ ] **Step 2: apply_to_store 读取批次列（含上下界校验）**

```python
# gui/config_panel.py:2835 附近（_item 函数体之后）
batch_size = 1
batch_text = _item(9, '1').strip()
if batch_text:
    try:
        batch_size = int(batch_text)
        if batch_size < 1:
            batch_size = 1
        elif batch_size > 1000:
            batch_size = 1000  # 防御性上界：防止 can_afford_batch O(N) 阻塞
    except ValueError:
        batch_size = 1

# gui/config_panel.py:2856 —— PoolEntry 构造加 batch_size
store.pools.append(PoolEntry(
    enabled=cb.isChecked() if cb else True,
    pool_id=pid,
    name=_item(2),
    start_day=start_day,
    end_day=start_day + duration,
    cost=cost_text,
    distribution_file=f"pools/{pid}.txt",
    bindings=bindings,
    distribution=distribution,
    batch_size=batch_size,  # ← 新增
))
```

- [ ] **Step 3: set_config（导入路径）同步更新**

```python
# gui/config_panel.py:2356 —— 第二个 PoolEntry 构造点（set_config 导入配置时使用）
store.pools.append(PoolEntry(
    enabled=p.get('enabled', True),
    pool_id=pid,
    name=p.get('name', ''),
    start_day=p.get('start_day', 0),
    end_day=p.get('start_day', 0) + p.get('duration', 21),
    cost=p.get('cost', 'draw_resource:160'),
    distribution_file=f"pools/{pid}.txt",
    bindings=bindings,
    distribution=distribution,
    batch_size=p.get('batch_size', 1),  # ← 新增
))
```

- [ ] **Step 4: get_config() 池子 dict 追加 batch_size** <!-- REVIEW-R1-FIX: ISSUE-007 -->

> `get_config()` 方法（第 2219-2237 行）通过 `config_changed` 信号发出池子预览 dict，当前不含 `batch_size` 字段。属防护性修复——虽然已知消费者（`MainWindow`）不依赖此字段，但未来下游可能依赖。

```python
# gui/config_panel.py:2228 (cost 之后) 追加一行
'batch_size': getattr(p, 'batch_size', 1),  # ← 新增
```

- [ ] **Step 5: 手动验证——编辑批次大小 → 保存 → 重载一致性**

```bash
python -m gacha_simulator.main
```
操作：Config 面板 → 池子 Tab → 双击批次大小单元格 → 输入 10 → 切换到其他 Tab 再切回 → 确认值保留 → 启动模拟验证 → 关闭重启 GUI → 确认值仍为 10。

- [ ] **Step 6: 提交**

```bash
git add gui/config_panel.py
git commit -m "feat(7b): GUI 批次大小数据流——refresh/apply/set_config/get_config 全链路透传"
```

---

### Task 7c: GUI 配置面板——模板与复制路径 <!-- REVIEW-R1-FIX: ISSUE-001 -->

> **估计工时：** 30min | **依赖：** Task 7a（表格列基础设施已就位）
> 
> **职责：** `_add_pools_from_template` 模板 dict 添加 batch_size + col 9 写入 + 背景色 range(10)，`_duplicate_pool` 复制 col range 8→10。

**Files:**
- Modify: `gui/config_panel.py`

- [ ] **Step 1: _add_pools_from_template 三处遗漏修复** <!-- REVIEW-R1-FIX: ISSUE-002 -->

> `_add_pools_from_template()` 方法（第 707-779 行）遗漏三处：1) 模板 dict 构造不含 `batch_size` 字段；2) 未写入 col 9 的 `QTableWidgetItem`；3) 背景色循环 `range(8)` 未覆盖 col 9。此为阻塞性遗漏——用户通过模板批量创建池子时批次大小列将显示空白且无背景色。

**1.1 模板 dict 添加 batch_size：**

```python
# gui/config_panel.py:735-745 —— 模板 dict 追加 batch_size
pool = {
    'enabled': True,
    'id': pid,
    'name': f'{name_prefix}{i+1}',
    'type': tmpl.get('type', '角色'),
    'start_day': start_day + i * interval,
    'duration': tmpl.get('duration', 21),
    'cost': tmpl.get('cost', 'draw_resource:160'),
    'note': '复刻池' if i > 0 and interval > 0 else '',
    'distribution': dist,
    'batch_size': 1,  # ← 新增
}
```

**1.2 模板生成循环中写入 col 9：**

```python
# gui/config_panel.py:765 附近（edit_item.setItem 之后）插入：
self.pool_table.setItem(r, 9, QTableWidgetItem('1'))
```

**1.3 背景色循环 range(8) → range(10)：**

```python
# gui/config_panel.py:771
for col in range(10):  # 原来是 range(8)，现在覆盖 col 9（批次大小）
    item = self.pool_table.item(r, col)
    if item:
        item.setBackground(row_bg)
```

- [ ] **Step 2: _duplicate_pool 复制 col range 8→10** <!-- REVIEW-R1-FIX: ISSUE-001 -->

```python
# gui/config_panel.py:885 —— col range 8→10
for col in range(10):  # 原来是 range(8)，现在覆盖 col 8（分布编辑）和 col 9（批次大小）
    if col == 0:
        ...
# 注意：range(10) 包含索引 0-9，col 8（分布编辑）和 col 9（批次大小）都会被 src_item.text() 复制
```

- [ ] **Step 3: 手动验证——模板创建 + 复制池子批次大小正确**

```bash
python -m gacha_simulator.main
```
操作：Config 面板 → 池子 Tab → 使用模板创建多个池子 → 确认每个新池子批次大小列显示 "1" 且有正常背景色 → 右键复制某个池子 → 确认复制行批次大小列值与原行一致。

- [ ] **Step 4: 提交**

```bash
git add gui/config_panel.py
git commit -m "feat(7c): GUI 批次大小模板批量创建 + 复制路径覆盖"
```

---

### Task 8: config_service.py JSON 导出/导入涵盖 batch_size <!-- REVIEW-R1-FIX: ISSUE-003 -->

**Files:**
- Modify: `service/config_service.py:25-60`

> `ConfigService.export_pool_to_config()`（第 25-42 行）和 `ConfigService.import_pool_from_config()`（第 44-60 行）逐字段手写序列化/反序列化，均未包含 `batch_size`。虽然当前项目主要使用 `schedule.txt` 文本格式配置，但若用户通过 JSON 路径导出池子配置再导入，`batch_size` 将回退为默认值 1，造成静默数据丢失。

- [ ] **Step 1: export_pool_to_config 添加 batch_size**

```python
# service/config_service.py:29-40 —— 导出 dict 追加 batch_size
{
    'id': p.id,
    'name': p.name,
    'cost': p.cost,
    'rewards': [...],
    'available_from': p.available_from,
    'available_until': p.available_until,
    'is_exchange': p.is_exchange,
    'batch_size': getattr(p, 'batch_size', 1),  # ← 新增
}
```

- [ ] **Step 2: import_pool_from_config 添加 batch_size**

```python
# service/config_service.py:51-59 —— Pool() 构造追加 batch_size
pools.append(Pool(
    id=p_data['id'],
    name=p_data['name'],
    cost=p_data.get('cost', {}),
    rewards=rewards,
    available_from=p_data.get('available_from'),
    available_until=p_data.get('available_until'),
    is_exchange=p_data.get('is_exchange', False),
    batch_size=p_data.get('batch_size', 1),  # ← 新增
))
```

- [ ] **Step 3: 提交**

```bash
git add service/config_service.py
git commit -m "feat: ConfigService JSON 导出/导入涵盖 batch_size 字段"
```

---

### Task 9: 文档同步——01-理论.md 池子属性表 <!-- REVIEW-R1-FIX: ISSUE-005 -->

**Files:**
- Modify: `docs/01-活跃/panels/配置/01-理论.md` (2.1 节)

> §2.1「池子维度」表列出了 7 个属性，不包含 `batch_size`。按该文档自身约定（「新列只能追加在末尾」），需在表末尾追加一行。

- [ ] **Step 1: 池子属性表追加 batch_size 行**

```markdown
# docs/01-活跃/panels/配置/01-理论.md §2.1 表末尾追加
| `batch_size` | 单次动作抽卡发数（十连=10，默认1） | `schedule.txt` 第 9 列（可选，省略默认 1） |
```

- [ ] **Step 2: 提交**

```bash
git add docs/01-活跃/panels/配置/01-理论.md
git commit -m "docs: 01-理论.md 池子属性表追加 batch_size"
```

---

## 四、边界情况覆盖

| 测试场景 | 覆盖任务 |
|---------|---------|
| batch_size 默认值 1 | Task 1 |
| schedule.txt 无第 9 列 → 默认 1 | Task 2 |
| schedule.txt 显式 10 → 解析正确 | Task 2 |
| 单一资源足够 N 发 | Task 3 |
| 单一资源不够 N 发 | Task 3 |
| free_draw → draw_resource 优先级切换 | Task 3 |
| 优先级切换后合计不够 | Task 3 |
| batch_size=1 退化到 can_afford | Task 3 |
| 策略 batch_size=1 行为不变 | Task 4 |
| 策略 can_afford_batch 使用 pool.batch_size（非硬编码 1） | Task 4 Step 2.5 | <!-- REVIEW-R1-FIX: ISSUE-006 -->
| 服务层 batch_size=1 退化到单发 | Task 5a |
| 服务层 batch_size=10 一次动作 10 发 | Task 5a |
| 服务层资源不足静默跳过 | Task 5a |
| 多资源逐发 spent 正确记录 | Task 5a |
| 保底 before_draw/after_draw 逐发调用（非缓存复用） | Task 5b |
| `_pending_wait_gains` 批次归因——仅首击含等待收益，streaming.py 累加不虚增 | Task 5c |
| `collector.on_draw` 逐发调用 batch_size 次（非每动作 1 次）——防止归因粒度退化 | Task 5c Step 1 |
| CLI 端到端无回归 | Task 6 |
| GUI 池子表格第 10 列渲染 | Task 7a |
| GUI 编辑批次大小后保存/重载一致 | Task 7b |
| _refresh_from_store_impl() pools_data 中间 dict 透传 batch_size——防止 set_config() 导入后 refresh 丢失 | Task 7b Step 1 | <!-- REVIEW-R1-FIX: ISSUE-001 -->
| GUI 批次大小非法输入 (≤0/非数字) → 回退到 1 | Task 7b Step 2 |
| schedule.txt batch_size < 1 → 静默修正为 1 | Task 2 |
| batch_size=0 或负数（防御） | Task 2 + Task 7b Step 2 |
| batch_size > 1000（防御性上界——静默截断为 1000） | Task 2 Step 6 + Task 7b Step 2 | <!-- REVIEW-R1-FIX: ISSUE-002 -->
| 批次中途获得抽卡资源——后续发自动可见 | Task 5 |
| 预检查失败（即使中途可能获得资源）→ 保守跳过 | Task 5 |
| load_config_from_directory() Pool 构造传递 batch_size | Task 1 Step 7.5 |
| 预置 schedule.txt 格式注释含 [batch_size] | Task 2 Step 6.5 |
| about_dialog.py 帮助文本含批次大小说明 | Task 2 Step 6.6 |
| worst_impact.py 虚拟 Pool 无需传递 batch_size（默认 1 正确） | Task 4 Step 3.5 |
| _add_pools_from_template 模板 dict + col 9 + 背景色 | Task 7c Step 1 |
| get_config() 信号 dict 含 batch_size | Task 7b Step 4 |
| config_service.py JSON 导出/导入含 batch_size | Task 8 |
| 01-理论.md 池子属性表含 batch_size 行 | Task 9 |

### 已确认无需修改的构造点 <!-- REVIEW-R1-FIX: ISSUE-008 -->

| 文件:行号 | 说明 | 理由 |
|-----------|------|------|
| `worst_impact.py:226-233` | `prepare_simulation_config()` 虚拟 `Pool` 构造无 `batch_size` | 分析用途不需要批次语义，默认 1 正确 |

### 已知行为（设计决策，非缺陷）

| 场景 | 行为 | 理由 |
|------|------|------|
| 批次中途获得目标卡 | 剩余发照常执行 | 批次原子性——与「付不起就不抽」对称，付得起就抽完 |
| 批次中途池子过期 | 剩余发照常执行 | 与单抽行为一致——池子可用性仅在迭代开始时检查 |
| 批次中途资源耗尽 | 不应发生——`can_afford_batch` 已预检查 | 极端情况（奖励扣减导致中途枯竭）时 `spent=None` 触发 `break` |
| 批次逐发保底概率计算 | 不使用 `ctx._pity_cache` 缓存 | 批次第 2 发起缓存已过期——直接调 `before_draw()` 获取新鲜概率 |
| StrategyContext.get_pity_probabilities() | 批次循环不调用 | 策略层每迭代构造新 `ctx`，不在批次内调用，无需修改 |
| 批次中途获得抽卡资源（如奖励返还 draw_resource） | 后续发自动可用——`resources` 是 `state.resources` 实时引用，非快照 | `spend()` 每次读取最新资源值；第 N 发的奖励在第 N+1 发 `spend()` 之前已入账 |
| 预检查失败但中途可能掉落资源 | 批次数跳过（保守策略） | 预检查用快照不依赖随机奖励——若依赖「可能掉落」来通过，等于赌博。资源不足就等下一轮迭代 |
| analysis_panel.py:1725 / worst_impact_panel.py:289 「单抽消耗」QLabel | 标签文本不随 batch_size 变化，仍显示「单抽消耗」 | 标签语义对应 per-draw cost，与 per-action cost 正交。不影响数值正确性。已知低优先级——随下次面板迭代自然更新为「每发消耗」 | <!-- REVIEW-R1-FIX: ISSUE-003 -->

---

## ⚠️ 自动化审查阻塞项

- **未解决问题数：** 0
- **详情：** []
- **原因：** 6 轮对抗循环未收敛 — Finder/Fixer/Verifier 三方僵持，无法达成一致，审查流程强制终止。该计划文件内的 `<!-- REVIEW-R1-FIX: ISSUE-XXX -->` 标记为对抗过程中已修复的发现项，其余未标记项（如有）尚未通过对抗验证闭环。

---

## 自动化审查记录

<details>
<summary>第 1 次审查（2026-06-11）——P38 工作流自动化</summary>

- 复杂度: complex | 变更性质: evolutionary
- 阶段 1 影响面: 18 个发现
- 阶段 2 对抗循环: 6 轮，熔断
- 阶段 3 门控: 6 项检查
- 阶段 4 代码审计: 已执行
- 累计问题: 16 个

</details>
