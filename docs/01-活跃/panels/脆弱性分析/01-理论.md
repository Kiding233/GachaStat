# 脆弱性分析面板 — 理论基础与局限性

> 建档日期：2026-06-08 | 版本：v4.1（ISSUE-046 修正——移除 v3 Sidak 回退设计残留，左界描述与代码一致）
> 理论审核来源：P15（9 项缺陷，A-C 阶段一已修复）+ P51（D/E/F/G/I 离散分箱框架一体化修复）
> 设计状态：✅ 方法缺陷全部修复；框架固有局限已明确标注；P51 引入的已知局限已登记

---

## 一、核心假设

| 假设 | 内容 | 成立条件 |
|------|------|---------|
| H1: 离散分箱充分性 | binsglm 分位数分箱（~J 箱）能够充分捕捉 θ(r) = P(失败\|资源=r) 在资源域上的变化 | J 足够大（IMSE 自动选择）；箱内 θ(r) 近似常数 |
| H2: 二元响应 | 每次模拟产出二值结局（成功/失败），由 GDR 阈值判定 | 阈值选择合理；GDR 指标恰当 |
| H3: 条件独立性 | 给定资源 r，各次模拟的失败判定独立 | 模拟间独立（不同 seed 保证）；但同一模拟内多池不独立 |
| H4: 单调递减（强制 + 可回退） | P(失败\|r) 随 r 递减——资源越多失败概率越低 | 经济直觉支撑；PAVA 在估计中强制执行；策略断点由诊断检测并局部回退 |
| H5: 设计点视为固定 | 将资源观测值视为固定设计点（design points），忽略其随机性 | 资源值本身是随机的（抽样变异），当前将其视为固定协变量，可能引入 Berkson 误差 |
| H6: 变更点存在性 | 存在唯一临界资源值 r*，使得 r < r* 时 θ(r) > α，r ≥ r* 时 θ(r) ≤ α | 单调性成立时自动满足；若不成立则分区间独立处理 |

> **与旧版（v2.0）的关键变化：** 旧 H1（局部平滑）被 H1（离散分箱充分性）取代——不再假设 θ(r) 局部线性。旧 H4（单调性未强制）升级为 H4（强制+可回退）。旧 H6（高斯核边界）因核回归完全移除而删除。

---

## 二、当前方法（P51 实施后）

### 三阶段管线

```
资源数据 (r_i, y_i)
       │
       ▼
① binsglm 分位数分箱（CCFF 2024a/b）
   │  dist='Binomial', ci=False, cb=False（只取 dots，不触发 SVD 路径）
   │  IMSE 准则自动选择箱数 J
   │  输出：x_left, x_right, x_center, p̂_j = K_j/N_j, N_j
   │
       ▼
② PAVA 保序估计（Barlow et al. 1972; Robertson et al. 1988）
   │  单调递减约束下 O(J) 合并逆向波动
   │  合并操作：K_pool / N_pool = 约束 MLE
   │  输出：θ̃_l (l=1…L ≤ J)、blocks、theta_per_bin
   │
   ├── 单调性诊断 ──── Wilson SE × 3σ 阈值检测策略断点
   │   若断点存在 → GUI 状态栏警告建议人工复核，不触发算法级回退
   │   （monotonicity_holds 仅存入返回字典供 retreat_panel.py 读取展示；
   │    变更点 CI 始终基于完整 PAVA 估计无条件执行）
   │
       ▼
③ Bootstrap 变更点推断
   │  ĵ* = max{j: θ̃_j > α}
   │  固定箱边界上非参数 Bootstrap（仅重新计数 + PAVA，不重跑 binsglm）
   │  95% 变更点 CI → [ĵ*_lo, ĵ*_hi] → [x_star_lo, x_star_hi]
   │  脆弱区间：左界 = x_left[0]（首个分箱左界，约等于数据最小值），右界 = x_star_hi（保守端）
   │
       ▼
   输出：脆弱区间 + 变更点 CI + 单调性诊断报告
```

### 设计细节与选择理由

#### binsreg cb/ci 不稳定性声明

binsglm 调用时传入 `ci=False, cb=False`，**仅取 dots（分箱均值点）**。CCFF (2024b) 提供的置信带（cb）和置信区间（ci）构造依赖 SVD 分解路径，在小 J（<20）或极端比例（p̂ ≈ 0 或 1）时数值不稳定——SVD 可能产生负的特征值导致 CI 宽度为 NaN。因此本管线放弃 binsreg 自带的 cb/ci，转用 Bootstrap 变更点 CI（步骤 ③）提供不确定性量化。dots（x_left, x_right, x_center, p̂_j, N_j）的提取本身是稳定的——仅涉及分位数切分和组内平均。

#### `_diagnose_monotonicity` 中 Wilson CI 半宽近似 SE 的选择理由

PAVA 输出的是约束 MLE（θ̃_l），其标准误不能直接从合并后的二项分布导出（合并操作引入了块间依赖）。本管线采用 **Wilson score CI 半宽作为 SE 的近似**：

```
SE_j ≈ (Wilson_CI_upper_j − Wilson_CI_lower_j) / (2 × z_{1−α/2})
```

Wilson CI 在 p̂→0 或 p̂→1 时不塌缩——与 Wald CI 不同——因此适合覆盖整个 [0,1] 区间，尤其脆弱性分析中资源极端区域的 p̂ 可能接近 0 或 1。

**3σ 阈值的 Sidak 校正依据：** 诊断检测策略断点时，对 L 个 PAVA 块（L ≤ J ≈ 15-30）同时检验单调递减假设。采用 3σ 阈值（|z| > 3）而非标准 2σ，等价于 Sidak 校正的近似：

```
α_adj = 1 − (1 − α)^(1/L)
z_{Sidak} ≈ z_{1−α_adj/2} ≈ 3.0   （当 L=15-30, α=0.05 时）
```

精确计算：L=20 时 Sidak 校正的临界值约 3.02σ；3σ 作为工程近似，保守性略高但避免了精确依赖 L 的计算开销。若诊断到断点，GUI 状态栏显示警告建议人工复核脆弱区间，不触发算法级回退——变更点 CI 始终基于完整 PAVA 估计无条件执行（monotonicity_holds 仅存入返回字典供 retreat_panel.py:347 读取展示，从未触发条件分支或算法切换）。

#### `_changepoint_ci` 接受预计算 PAVA 输出以消除重复计算

Bootstrap 变更点推断的每次重抽样中，箱边界（由 binsglm 确定）保持不变——仅对箱内 K_j 重新抽样（Binomial(N_j, p̂_j)）并重新运行 PAVA。由于 binsglm 分箱只需运行一次（箱边界固定），`_changepoint_ci` 直接接受步骤 ② 的 PAVA 输出（theta_per_bin、N_j、箱边界），避免在 `n_boot=500` 次迭代中重复计算 binsglm。这使得 Bootstrap 的总计算成本为：

```
T_total = T_binsglm + T_pava + 500 × T_pava_only
```

而非 `T_total = 500 × (T_binsglm + T_pava)`，节省了约 500 次 binsglm 调用的开销。

### 回退路径（完整三级回退）

```
binsglm 分位数分箱（首选）
    │
    ├─ 成功 → PAVA 保序估计 → Bootstrap CI → 完整脆弱区间（正常路径）
    │
    └─ 失败（ImportError / C 扩展编译问题 / 数值异常）
           │
           ▼
       `_logistic_ll_fit`（IRLS 局部逻辑回归，纯 Python）
           │
           ├─ 成功 → 曲线走查法（旧方法降级路径）
           │       │  按 r_grid 逐点估计 θ̂(r)
           │       │  脆弱区间 = {r: θ̂(r) > α} 的连续区域
           │       │  局限性：无多重性校正、无 CI、无单调性强制
           │       │
           │       └─ GUI 状态栏：「binsglm 不可用，已回退到局部逻辑回归」
           │
           └─ 失败（IRLS 不收敛 / 数据不足）
                  │
                  ▼
              等距分箱 + 朴素比率（最后手段）
                      │  等距分箱（FD 规则）→ 每箱失败率 = K_j/N_j
                      │  脆弱区间 = {r: 失败率 > α} 的连续区域
                      │
                      └─ GUI 状态栏：「统计方法已降级为简单分箱比率」
```

回退触发条件：
- `ImportError`：binsglm 包未安装或 C 扩展不兼容（P51-N4）
- `RuntimeError`：binsglm 数值异常（如全零/全一响应）
- IRLS 不收敛：局部逻辑回归在极端稀疏区迭代超限（>50 次）
- 数据不足：有效样本数 < 10（任何方法均不可靠，输出空结果）

### 与旧方法（v2.0 核回归）的本质区别

| | v2.0 核回归 | v3.0 离散分箱 + PAVA |
|---|---|---|
| 平滑假设 | θ(r) 局部近似线性（logit 变换后） | 无——箱内 θ(r) 视为常数 |
| 推断单位 | 200 个格点（连续） | J 个分箱（离散，~15-30） |
| 带宽选择 | Silverman 规则（核密度用，非回归用） | IMSE 自动选 J（数据驱动） |
| 多重性 | 200 次独立检验，无校正 | 1 次变更点估计 |
| 单调性 | 不强制（估计结果可能违反经济直觉） | 强制递减（PAVA），可诊断可回退 |
| 稀疏区处理 | 全局等带宽 → 方差大 | 分位数分箱 → 自动扩箱 |
| 噪声尖峰 | 需 persistence 过滤 | PAVA 自动与邻箱合并 |
| 置信区间 | 无 | Bootstrap 变更点 CI（95%） |

---

## 三、已知局限性

### A 层：方法缺陷（P15 + P51 已全面修复）

| # | 缺陷 | 严重度 | 旧方案 | P51 修复 | 状态 |
|---|------|--------|--------|---------|------|
| A | r_grid 边界硬限制 | 🔴 严重 | `[r_min, r_max]` | r_grid 扩展 10% 边距 | ✅ 已通过 P51 修复 |
| B | LLR 左边界偏差 | 🔴 严重 | 核右侧有数据左侧无 | 左边界向左扩展一个带宽 | ✅ 已通过 P51 修复 |
| C | 线性回归用于二元响应 | 🟡 中等 | LLR → 裁剪到 [0,1] | `_logistic_ll_fit` IRLS 实现 | ✅ 已通过 P51 修复 |
| D | Silverman 带宽非回归最优 | 🟡 中等 | 核密度估计规则 | binsglm IMSE 选 J——无带宽概念 | ✅ 已通过 P51 修复 |
| E | 200 格点无多重性校正 | 🟡 中等 | 逐点阈值 | 变更点 CI——J 次检验 → 1 次 | ✅ 已通过 P51 修复 |
| F | 无 persistence 过滤 | 🟡 中等 | 1-2 格点噪声判为脆弱 | PAVA 自动合并逆向箱 | ✅ 已通过 P51 修复 |
| G | 全局等带宽不适应密度 | 🟢 低等 | 稀疏区方差大 | 分位数分箱——每箱等样本量 | ✅ 已通过 P51 修复 |
| H | FD 分箱偏斜分布分辨率不足 | 🟢 低等 | FD 规则 | `compute_bins()` 统一入口；等距直方图仅展示用 | 🔵 改善 |
| I | 因果性声明隐式假设 | 🟢 低等 | 无说明 | 关于页面 + 本文档明确标注关联非因果 | ✅ 已实施因果性声明 |

### B 层：框架固有局限（任何方法均无法在当前框架内消除）

这些不是「方法选错了」，而是「分析框架本身」的约束。即使换方法（核回归、神经网络、贝叶斯），只要输入是「(资源, 成败) 点集 → 条件概率曲线 → 脆弱区间」，这些问题就存在。

| # | 局限 | 根源 | 影响 | 可能的根本解决方向 |
|----|------|------|------|------------------|
| L1 | **GDR 阈值二元化信息损失**（旧 H2） | 连续 GDR 值被截断为 0/1，丢失「接近成功」vs「差得很远」的区分 | 中间阈值（如 α=0.5）时大量「部分成功」样本被粗暴分类；阈值取极端值时损失更严重 | 序数 logistic 回归（3+ 有序类别）或直接建模 GDR 连续值分布 |
| L2 | **条件独立性违反——多池策略依赖**（旧 H3） | 同一模拟的各池结局共享底层运气因子（如早期池歪了 → 后续资源少 → 连锁失败），不是独立观测 | 方差估计偏小，置信带偏窄——对脆弱区间的确定性过度自信 | 多层模型（模拟级随机截距）；cluster-robust 标准误；GEE |
| L3 | **幸存者偏差**（旧 H4 的部分） | 极低资源区间的样本是选择性的——能活到低资源还没失败的模拟，其底层运气可能系统性偏优 | 左尾部 θ(r) 可能被低估——极低资源区间的失败概率看起来低于真实值 | 因果推断框架（do-calculus / 工具变量）；逆概率加权（IPW） |
| L4 | **资源作为固定设计点的 Berkson 误差**（旧 H5） | 将随机资源值当作固定协变量，忽略其抽样变异 → 相当于在 x 轴上有测量误差 | 回归曲线向均值收缩（attenuation bias）；脆弱区间边界估计偏保守 | Errors-in-variables 模型；SIMEX；贝叶斯测量误差模型 |
| L5 | **Bootstrap CI 的渐近有效性** | 变更点 Bootstrap CI 在大样本下名义覆盖率为 95%，有限样本（n < 1000）下实际覆盖率未知 | 小样本下 CI 可能偏窄或偏宽——名义 95% 可能实际只有 80% 或高达 99% | 模拟校准研究（Monte Carlo 评估实际覆盖率）；双 Bootstrap（计算成本高） |
| L6 | **r→0 边界无法估计** | 若数据中不存在资源接近 0 的模拟（大部分模拟在资源耗尽前就停止了），θ(r→0) 不可估计 | 脆弱区间左界固定为首个分箱左界 x_left[0]（≈ 数据最小值），更低资源区域的外推无法验证 | 外推需额外假设（如参数形式），但会引入新的模型风险 |
| L7 | **binsglm IMSE 选 J 是工程启发式** | IMSE 准则最小化积分均方误差，但无有限样本最优性理论保证 | J 的选取可能不是「最优」的（但通常「足够好」） | 交叉验证选 J；或多分辨率分箱 + 汇总（计算成本高） |

> **关键区分：** A 层缺陷是「方法在数学上站不住脚」——P51 修的就是这些。B 层局限是「问题本身的难度」——框架不变就无法消除，只能明确标注，让用户知情决策。

---

## 四、假设违反的后果速查

| 假设 | 违反情景 | 在新框架下的后果 |
|------|---------|----------------|
| H1: 离散分箱充分性 | 箱内 θ(r) 变化剧烈（如 J 太小导致箱太宽） | 箱内平均掩盖了真实结构——变更点偏移最多 1 箱宽；可增大 J 缓解 |
| H4: 单调递减 | 策略切换处 θ(r) 局部跃升（如 target_hunting 跨池时资源阈值效应） | 诊断检测为 breakpoint → GUI 状态栏警告建议人工复核；变更点 CI 无条件基于 PAVA 估计执行，断点不影响算法路径 |
| H6: 变更点存在性 | 脆弱/安全区间交替出现（如 M 形） | 变更点 ĵ* 仅捕获最后一个脆弱箱——遗漏后续的脆弱区域；需分区间独立分析 |
| — (旧 H2) | GDR 阈值取中间值 | 见 B 层 L1——二元化丢失信息，任何方法均受影响 |
| — (旧 H3) | 多池策略依赖 | 见 B 层 L2——方差低估，置信带偏窄 |
| — (旧 H4 幸存者偏差) | 极低资源区样本稀少+选择性强 | 见 B 层 L3——左尾部估计失真 |

> 旧 H1（局部平滑）和旧 H6（高斯核边界）已因核回归移除而不再适用。

---

## 五、理论改进所依据的文献

### 5.1 离散分箱与非参数二元回归

| 文献 | 关联 | 核心贡献 |
|------|------|---------|
| Cattaneo, Crump, Farrell & Feng (2024a), *binsreg: Binscatter Regressions in Python* | 管线 ① | binsreg Python 包——分位数分箱 + IMSE 选 J + 置信带构造 |
| Cattaneo, Crump, Farrell & Feng (2024b), *On Binscatter*, *American Economic Review*, 114(5): 1488-1514 | 管线 ① | binscatter 的理论基础——分箱作为非参数回归的离散化方法 |
| Signorini & Jones (2004), *JASA*, 99(465): 119-126 | 回退路径 | 系统比较核二元回归估计量，局部线性逻辑回归为最佳单带宽方法 |

### 5.2 保序回归与有序约束推断

| 文献 | 关联 | 核心贡献 |
|------|------|---------|
| Barlow, Bartholomew, Bremner & Brunk (1972), *Statistical Inference under Order Restrictions*, Wiley | 管线 ② | 保序回归经典专著——PAVA 算法 + 有序约束下的 MLE 理论 |
| Robertson, Wright & Dykstra (1988), *Order Restricted Statistical Inference*, Wiley | 管线 ② | 有序约束推断的权威参考——chi-bar-squared 零分布、似然比检验 |
| Ayer, Brunk, Ewing, Reid & Silverman (1955), *Annals of Mathematical Statistics*, 26: 641-647 | 管线 ② | PAVA 算法原始提出（针对二项比例的保序估计） |

### 5.3 变更点检测与 Bootstrap

| 文献 | 关联 | 核心贡献 |
|------|------|---------|
| Antoch & Hušková (2001), *Journal of Statistical Planning and Inference*, 91: 53-72 | 管线 ③ | 变更点估计量的 Bootstrap 近似——置换检验与重抽样方法 |
| Efron & Tibshirani (1993), *An Introduction to the Bootstrap*, Chapman & Hall/CRC | 管线 ③ | Bootstrap 方法论权威专著——百分位法 CI 的理论基础 |
| Wilson (1927), *JASA*, 22(158): 209-212 | 单调性诊断 | Wilson score 置信区间——不对 p̂=0 或 p̂=1 塌缩 |

### 5.4 直方图与密度估计（展示层，不参与推断）

| 文献 | 关联 | 核心贡献 |
|------|------|---------|
| Freedman & Diaconis (1981), *Z. Wahrscheinlichkeitstheorie*, 57: 453-476 | 等距直方图 | FD 分箱规则——`h = 2·IQR·n^(-1/3)` |

### 5.5 旧版核回归文献（已归档，保留供回退路径参考）

| 文献 | 关联（旧缺陷） | 核心贡献 |
|------|---------------|---------|
| Fan (1992), *JASA*, 87(420): 998-1004 | B | 局部线性回归的设计自适应性与边界行为 |
| Fan & Gijbels (1996), *Local Polynomial Modelling and Its Applications*, Chapman & Hall/CRC | B, D | 局部多项式回归权威专著 |
| Loader (1999), *Local Regression and Likelihood*, Springer | C | 局部似然框架 |
| Silverman (1986), *Density Estimation for Statistics and Data Analysis*, Chapman & Hall/CRC | D | Silverman 带宽规则原始出处 |

---

*理论文档 v4.1 完成。A 层 9 项缺陷全部修复（A-I，由 P15 阶段一与 P51 共同完成）。B 层 7 项框架固有局限已明确标注，无法在当前分析框架内消除——用户应知情决策。P51 引入的 4 项新问题（N1-N4）已在 `04-问题.md` 登记，均为已知局限或已缓解。三阶段管线设计细节（回退逻辑、Wilson CI 诊断、changepoint_ci 优化、binsreg 不稳定性声明）已补充至 §二。v4.1（ISSUE-046）：移除三处 v3 Sidak 回退设计残留（架构图箭头、正文引用、假设违反后果表）并修正左界描述——与 vulnerability.py _fit_vulnerability_pava 第438-448行的实际行为一致。*
