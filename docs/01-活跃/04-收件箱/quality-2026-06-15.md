# quality-2026-06-15

> C5 quality-reviewer | 2026-06-15（周一）| 审查范围：6/8–6/15

---

## 一、重复代码模式

### 已消除 ✅

| 模式 | 旧 | 新 | 效果 |
|------|-----|-----|------|
| _NoWheel* 子类 | 4 面板各自定义 `_NoWheelComboBox`/`_NoWheelSpinBox`/`_NoWheelDoubleSpinBox`（~36 行 ×4） | `wheel_blocker.py` GlobalWheelBlocker（32 行） | 语义去重 + 全局一致 |

P47 是教科书级的语义去重——从分散的 wheelEvent override 模式提升到 QApplication 级事件过滤器。

### 持续存在

- `_resolve_targets` / `_resolve_targets_for_pool`（`worst_impact.py:548-563` / `526-546`）：两个方法语义相同但参数不同——可抽取为带参数的单方法。低优先级（二者调用上下文不同，合并可能引入不必要的抽象）。

---

## 二、架构分层

### 通过 ✅

- **零 core→gui 反向引用**：`grep -rn "from gacha_simulator.gui" gacha_simulator/core/` 返回空
- **GUI→core 方向全部正常**：gui/ 文件从 core 导入 gdr/per_pool_analysis/retreat_search 等——标准依赖方向
- **新增 core/config_toml.py**：仅依赖 stdlib `tomllib`/`tomli_w` + `config_store`——无 GUI 依赖

---

## 三、过度耦合

### 大文件（>800 行阈值）

| 文件 | 行数 | 倍数 | 风险 |
|------|------|------|------|
| `gui/config_panel.py` | 3232 | 4.0× | 🟡 配置 Tab 含 6 个子 Tab——自然增长 |
| `gui/analysis_panel.py` | 2487 | 3.1× | 🟡 AnalysisWorker + AnalysisPanel 合体 |
| `gui/plan_search_panel.py` | 1373 | 1.7× | 🟡 PlanSearchPanel + Worker |
| `core/vulnerability.py` | 1172 | 1.5× | 🟢 P51 改造后适度 |

**缓解因素**：
- `config_panel.py` 的 3232 行分布在 6 个独立子 Tab 方法中，内聚性尚可
- `analysis_panel.py` 中 AnalysisWorker（~1000 行）和 AnalysisPanel（~1500 行）可拆分为独立文件
- P54（CLI/GUI 统一化）可能自然推动 `plan_search_panel.py` 的 Worker 提取

### 无多级 import 链

新增模块的 import 深度 ≤2：`config_toml → config_store`、`wheel_blocker → PyQt6`。

---

## 四、边界检查

### 通过 ✅

| 检查点 | 结果 |
|--------|------|
| `config_toml.py` `.get()` 调用 | 56 处——TOML 解析惯用模式 |
| `config_toml.py` try/except | 4 处——文件 I/O + TOML 格式错误 |
| `state.can_afford_batch()` | `batch_size≤1` 退化到 `can_afford()`——安全回退 |
| `pool.batch_size` 默认值 | `int = 1`——零破坏性变更 |
| GlobalWheelBlocker | `isinstance(obj, (QComboBox, QAbstractSpinBox))`——类型安全过滤 |

---

## 总结

| 维度 | 判定 |
|------|------|
| 重复代码 | ✅ 改善（P47 去重） |
| 架构分层 | ✅ 零违反 |
| 过度耦合 | ⚠️ 4 文件 >800 行（缓解因素充分） |
| 边界检查 | ✅ 安全 |

**整体评价**：本周变更质量良好。P47（wheel_blocker）是语义去重的典范。P53（TOML 迁移）干净——530 行新文件边界安全，56 处 `.get()` + 4 处异常处理。config_panel.py（3232 行）是技术债积累点，但拆分需谨慎（6 子 Tab 共享 ConfigStore 状态）。
