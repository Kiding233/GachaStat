# E1 Deep Evaluator 审查报告

**时间**：2026-06-13 01:25 UTC  
**审查范围**：`bb2b17f` — chore: 安全检查点  
**P38 交叉验证**：无 peer-review 文件

---

## 判定结果：ℹ️ REVIEW（2 WARN，0 FAIL）

本次为 checkpoint 提交（183 文件，+11704/-384），非功能 PR。以下按判定维度逐项分析。

---

## 判定表

| 维度 | 判定 | 说明 |
|------|------|------|
| 架构分层 | ✅ PASS | `core→gui` 反向引用 = 0 |
| PR 边界 | ✅ PASS | checkpoint 提交，含测试+pyproject，符合 chore 预期 |
| 测试覆盖 | ⚠️ WARN | `ResultStore.connect_datasets_changed` / `connect_current_changed` / `_emit_*` 信号方法零测试覆盖 |
| 文档同步 | ⚠️ WARN | `cli.py` 全文 API 重构（Pool/Reward/PityEngine → ConfigStore/SimulationEnvBuilder）无对应模块文档更新 |
| 副作用 | ℹ️ INFO | 多个 GUI 面板新增 `from batch_simulator import run_batch_parallel` ——新跨层调用模式，未声明 |
| 计划一致性 | ✅ PASS | 变更横跨 P19(策略比较)/P26(方案搜索)/P22(GDR)，均为活跃计划区域；P19 即本周聚焦 |

---

## 关键发现

### 1. cli.py 全文重构（WARN — 文档未同步）

旧导入体系 `Pool / Reward / GachaState / DrawAction / PityEngine` 被完全替换为 `ConfigStore / PoolEntry / SimulationEnvBuilder / AllPoolsEndCondition`。这是一个跨越旧版 API → 新版 ConfigStore 体系的关键迁移。**没有对应的模块文档或迁移说明记录这一变化。**

### 2. config_io.py 加载循环重构

`load_store_from_directory` 中 8 个独立 `_load_*` 调用改为 `for _name, _fn in [...]` 循环 + 每文件独立异常处理。逻辑等价但异常粒度更细——原版 8 个调用共享 3 个顶层 except，新版每文件独立 try/except。这是质量改进，风险低。

### 3. batch_simulator.py end_day 硬化和 logger 警告

```python
end_day = (pe.end_day if pe.end_day is not None and pe.end_day > start_day
           else (start_day + 21))
if pe.end_day is None:
    logger.warning("Pool '%s' end_day is None, defaulting to start_day+21=%d", ...)
```

防御性编程改进，低风险。

### 4. 🟢 历史遗留修复

`eval-2026-06-11-1712` 中的 WARN——`expand_gain_rules_to_schedule` 零测试覆盖——**已被本次 commit 修复**。新增 `tests/core/test_resource_gain_expand.py`（166 行，17 个测试引用）。该 WARN 从此消除。

---

## 上下文追踪

| # | 报告 | 判定 | 类型 |
|---|------|------|------|
| 1 | eval-2026-06-12-0507 | ✅ CLEAN | 无变更 |
| 2 | eval-2026-06-12-1148 | ✅ CLEAN | 无变更 |
| 3 | eval-2026-06-13-0108 | ✅ CLEAN | 无变更 |
| 4 | 本次 | ℹ️ REVIEW | 有变更（2 WARN） |

CLEAN 连续记录中断（变为 REVIEW），但无 FAIL 连续记录。不触发升级/降级。

---

## 未解决的遗留问题

| 来源 | 问题 | 状态 |
|------|------|------|
| eval-2026-06-11-1712 | `expand_gain_rules_to_schedule` 无测试 | ✅ 已修复（本次） |
| 本次 | `ResultStore` 信号方法无测试 | 🔴 新增 WARN |
| 本次 | `cli.py` 重构无文档 | 🔴 新增 WARN |

---

## 建议（非强制）

1. **ResultStore 信号**：在 `tests/core/test_result_store.py`（或新建）中为 `connect_datasets_changed` / `connect_current_changed` 加至少冒烟测试——验证信号发出/断连不抛异常。
2. **cli.py 迁移记录**：在 `gacha_simulator/cli.py` 模块 docstring 中加一行迁移注记，或更新对应的架构文档说明新 CLI 基于 ConfigStore 体系。
