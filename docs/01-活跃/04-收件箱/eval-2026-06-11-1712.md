# E1 深度评估 — 2026-06-11 17:12 UTC

> 审查范围：e7eed28..HEAD (6 commits，约 24h) · 上次 eval：无（首次运行）

---

## 判定结果：ℹ️ REVIEW

| 维度 | 判定 | 摘要 |
|------|------|------|
| 架构分层 | ✅ PASS | core/ 无 gui/ 反向引用 |
| PR 边界 | ✅ PASS | 变更含源码 + 测试 + 文档，无孤立 meta 文件提交 |
| 测试覆盖 | ⚠️ WARN | `expand_gain_rules_to_schedule()` 无测试 |
| 文档同步 | ℹ️ INFO | 新 API 仅在 P37 计划中记录，子系统模块文档待计划完成后更新 |
| 副作用 | ✅ PASS | 旧 helper 标记 [DEPRECATED]，调用方分批迁移完成 |
| 计划一致性 | ℹ️ INFO | 变更对齐 P37（配置面板，P2 债务性，矩阵状态 designing） |

---

## 各维度详述

### 1. 架构分层 ✅ PASS

```
grep -rn "from gacha_simulator.gui" gacha_simulator/core/ → 0 结果
```

`expand_gain_rules_to_schedule()` 位于 `core/resource_gain.py`，纯计算函数，无 GUI 依赖。config_panel 和 batch_simulator 通过显式 import 调用，方向正确（gui → core, service → core）。

### 2. PR 边界 ✅ PASS

审查范围内 6 个 commit：

| Commit | 类型 | 文件 |
|--------|------|------|
| a885770 | fix | `core/retreat_search.py` |
| f907a2a | feat | `core/config_store.py` + `service/batch_simulator.py` + hooks/docs |
| e3b49c3 | chore | 安全检查点（全工作树） |
| 5d7df19 | fix | `.claude/hooks/` |
| ac5d3f4 | feat | P37 全链路（`core/resource_gain.py` + `service/batch_simulator.py` + `gui/config_panel.py` + 测试 + 文档） |

源码变更均有对应测试变更（`tests/core/test_resource_gain.py` 同步修改）。注意：工作树中存在 `__pycache__/*.pyc`、`.coverage`、`results.json` ——这些未提交，但 `.gitignore` 应覆盖 `.pyc` 和 `.coverage`。建议确认 `results.json` 是否应加入 `.gitignore`。

### 3. 测试覆盖 ⚠️ WARN

新增公共接口：
- `expand_gain_rules_to_schedule(gain_rules, day_overrides, total_days, start_date) -> Dict[int, Dict[str, float]]` —— **无测试**
- `ConfigStore.sim_start_date` —— 无直接测试（通过 CLI/GUI 间接覆盖）
- `ConfigStore.desire_weights` / `miss_cost_weights` / `card_value_weights` (3 个 property) —— 无直接测试

`test_resource_gain.py` 未新增任何 `expand_gain_rules_to_schedule` 的测试用例。该函数包含 3 种规则类型（`every_n_days` / `weekly` / `monthly_day: 单数字` / `monthly_day: 月,日` / `monthly_week: 逗号` / `monthly_week: 减号`）+ 空 param 回退 + 范围校验 + `total_days <= 0` 边界 + 闰年行为 + `date.fromordinal` 依赖。这些路径全部未经测试覆盖。

**风险**：P37 核心逻辑变更（从固定 30 天月长改为真实公历）的正确性依赖 `expand_gain_rules_to_schedule` 的时间计算。没有测试意味着回归只能靠人工验收。

**建议**：在继续其他 P37 Phase 之前补测试。至少覆盖：正常路径（每种 rule_type）、空 param 回退、`total_days=0`、闰年 2-29、fromordinal 溢出。

### 4. 文档同步 ℹ️ INFO

新 API 在 P37 计划文件中有详细文档（函数签名、参数说明、调用链、类型转换职责），但 GDR 系统 / 模拟服务层的模块 `02-实施.md` 尚未更新。P37 计划状态为 `designing`，文档同步应在计划完成后执行。当前状态合理，无需告警。

注意：`ConfigStore` 新增 3 个权重 property 已在 `subsystems/GDR系统/05-笔记.md` 中记录（H4 自动追加），但非正式实施文档。

### 5. 副作用 ✅ PASS

变更影响面已明确：

| 变更 | 影响 | 处理 |
|------|------|------|
| 新增 `expand_gain_rules_to_schedule()` | `batch_simulator._build_resource_gain()` + `config_panel` 日历预览 | 两处均已迁移 |
| 标记 4 个 helper `[DEPRECATED]` | `_weekday_of_day` / `_month_of_day` / `_day_of_month` / `_week_of_month` | 保留定义，由新函数内联 |
| `_wk_init` 移除预导入 | 子进程启动行为变更 | 懒加载 + 错峰加载，降低内存峰值 |
| `run_batch_parallel` 重试逻辑 | 新增 3 次降并发重试 + 单进程兜底 | 向后兼容，返回值格式不变 |
| `ConfigStore` 新增 `sim_start_date` | `SimulationEnvBuilder` + `config_io` | 两处均完成适配 |

无破坏性变更。旧函数保留 `[DEPRECATED]` 标注但未删除——确认无外部依赖后可安全移除。

### 6. 计划一致性 ℹ️ INFO

变更核心路径 `core/resource_gain.py` → `service/batch_simulator.py` → `gui/config_panel.py` 对齐 P37（资源管理日历增强，归属模块 配置面板，P2 债务性）。矩阵中 P37 状态为 designing，在活跃计划索引中正确登记。

---

## P38 交叉验证

收件箱中无 `peer-review-*.md` 文件。P38 审查产出直接追加到计划文件末尾（`## 自动化审查记录`），未写入收件箱。当前无交叉验证可比对。

若未来 P38 产出 peer-review 报告文件，E1 将自动执行交叉验证。

---

## 升级标记

无历史 eval 报告 —— 升级标记不适用。基线已建立。

---

## 工作树状态备注

以下文件出现在工作树中但未提交，不影响本次判定：

| 文件 | 建议 |
|------|------|
| `results.json` | 看起来是模拟输出——建议加入 `.gitignore` |
| `.coverage` | pytest-cov 产物——应已在 `.gitignore` |
| `__pycache__/*.pyc` | Python 缓存——应已在 `.gitignore` |
| `benchmark_chart.py` | 独立脚本——确认是否有意提交 |
| `test_*.html` / `profile_*.py` | 临时文件——建议清理或加入 `.gitignore` |
