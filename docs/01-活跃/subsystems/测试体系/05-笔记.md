# 测试体系 -- 笔记

> 最后更新：2026-06-13

---

## 2026-06-13 | 首次建档

**触发：** 用户发起测试覆盖率提高专题计划 → 头脑风暴 → 方案设计 → P46 计划创建。

**动作：**
- 覆盖率现状调查：总体 29%，core/ 55%，425 用例，4.2s
- 与用户达成方案共识：方案 A+B 混合，聚焦 7 个核心模块，三阶段 7 周
- 创建 `subsystems/测试体系/` 子系统，含完整六文件骨架
- 创建 `P46 核心模块测试覆盖率提升计划.md`
- P46 注册到 `模块状态矩阵.md` P1 段 + 子系统概览表

**关键决策：**
- 分层目标：core/ 55%→70%+，总体 29%→38%+
- 按"谁依赖谁"排序：strategy → gdr → P0 模块 → 辅助
- GUI 层不设覆盖目标（PyQt6 不可测）
- 测试模式对齐现有风格：简洁 pytest，工厂函数，AAA，几乎不用 mock

**下一步：** 用户审阅计划文件 → 转入 writing-plans 技能生成实施计划

---

## 2026-06-13 | 扩展为实施计划

**触发：** 用户要求直接扩展 P46 为实施计划。

**动作：**
- 探查全部 7 个目标模块源码与现有测试，识别具体测试缺口
- P46 计划从 ~7KB 设计轮廓扩展为 ~20KB 可执行实施计划
- 每模块列出详细实施步骤、关键测试用例名称、预估用例数
- 新增逐阶段验收标准（M1/M2/M3）
- 更新模块状态矩阵：P46 📋设计 → 🟡进行中

**探查发现：**
- `strategy.py`：仅 2 用例覆盖 2 个策略类，5 个策略+工厂函数+StrategyContext 完全未测
- `gdr.py`：51 用例但 11 个 GDR 指标 compact 计算、20 个 history 函数、check_batch 等未覆盖
- `worst_impact.py`：实际 578 行（非 355），WorstImpactAnalyzer 含 12 个方法全部未测
- `retreat_search.py`：实际 780 行，search_min_resource（140行最复杂方法）未测
- `comparison_analyzer.py` + `streaming.py`：均无专用测试文件
- `resource_gain.py`：ScheduleResourceGain + StepResourceGain 完全未测

**预估上调：** ~210 → ~211 用例（各模块探查后精确估算）
