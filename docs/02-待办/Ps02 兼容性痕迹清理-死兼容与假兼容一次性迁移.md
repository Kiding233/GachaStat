<!-- META: Ps02 | module:模拟服务层 | status:待办 | last:2026-08-02 -->

# Ps02 兼容性痕迹清理：死兼容与假兼容一次性迁移

> 日期：2026-08-02 | 状态：待办
> 触发：兼容性痕迹扫描（子 agent，两轮）发现约 35 处「为向后兼容而保留」的设计；依据 CLAUDE.md「无历史包袱原则」应一次性清理，代码只保留新形态
> 性质：清理计划（分阶段，各阶段独立可运行、独立 commit）

## 一、问题

代码库存在三类兼容性痕迹，均与「无历史包袱原则」相悖：

1. **死兼容**：旧路径/旧接口无任何生产者，分支不可达（如 `create_behavior` 的 `hasattr(pdef, 'params')` 旧路径、`PityEngine` 旧签名）
2. **假兼容**：声称「向后兼容」实际旧格式会静默失效（如 `is_new_format` 检测只有 `if` 无 `else`，旧格式直接返回 None、保底被禁用）
3. **双路径/双写**：同一信息新旧两处存储（如 `pool_type` 同时写 `PoolEntry.pool_type` 和 `bindings['type']`）

这些痕迹增加理解成本、掩盖真实缺陷、妨碍未来迁移。项目未上线、config.toml 仅示例/测试用（CLAUDE.md「无历史包袱原则」），应一次性干净迁移。

**与 P61/P58 关系**：P61 聚焦「池子结构迁移」（Pool → Banner），P58 聚焦「里程碑引擎」。Ps02 聚焦「兼容性痕迹清理」（死代码/双路径/假兼容），与两者方向一致但独立。P61 已排期的项（如 `Pool.is_rerun`/`original_pool_id` 删除、`get_available_pools` 删除）不在 Ps02 重复排期。

## 二、目标

1. 删除所有无生产者的死兼容分支与死代码
2. 消除假兼容（`is_new_format` 等无 else 检测）
3. 消除双路径/双写（信息只保留新形态）
4. 不触碰活功能：语法糖（`_expand_soft_to_deltas`）、用户可见健壮性回退（旧 TOML 自动迁移）、活跃 API 别名

## 三、方案

### 阶段 A：直接可删（无调用方改动，约 145 行）

| # | 位置 | 内容 | 预估 |
|---|------|------|------|
| A1 | `pity.py:324-370` | `HardPityBehavior` 旧接口参数（`target_distribution`）+ `apply()` 旧接口桥接方法 + `_legacy_target_dist`/`_legacy_mode` + `PoolPitySpec.resolved_targets` 死字段（注释自认恒空） | ~35行 |
| A2 | `pity.py:848-862` | `create_behavior` 的 `hasattr(pdef, 'params')` 旧路径（新 `PityDef` 无 `params`，恒不可达）+ `_parse_deltas_string`（L906-920，仅此调用） | ~30行 |
| A3 | `pity.py:1176-1182` | `PityEngine` 旧签名检测（`isinstance(pity_defs, dict)`）+ `behaviors` 参数 + `_legacy_mode`（赋值后从未读取，write-only） | ~8行 |
| A4 | `pity.py:1044-1051` + `core/__init__.py:11,110` | `PityDefParsed` dataclass + 导出（无生产者）+ `_parse_target_distribution` 死函数（L1386-1400，定义后从未调用） | ~25行 |
| A5 | `batch_simulator.py:92-94` + `worst_impact.py:448-449` | `is_new_format` 假兼容检测（恒真、无 else；docstring 声称「旧格式向后兼容」实际旧格式导致 `pity_defs_list` 未绑定 **NameError 崩溃**而非静默，删除更有利）+ 误导性 docstring | ~6行 |
| A6 | `gdr.py:829-930` | `_build_legacy_registries` + `GDR_REGISTRY`/`COMPACT_GDR_REGISTRY` + `compute_all_gdr`/`compute_collection_rates`/`compute_weighted_gdr`/`compute_custom_weighted_gdr` 旧函数（被 `UNIFIED_GDR_REGISTRY` 取代）+ 导出；删除块到 `compute_custom_weighted_gdr`（L893）为止，辅助函数 `card_collection_rate` 等仍被 UNIFIED 历史计算使用、保留 | ~40行 |

### 阶段 B：先改少量调用方/测试再删

| # | 位置 | 内容 | 预估 |
|---|------|------|------|
| B1 | `config_service.py:7-63` + `service/__init__.py:2,5` | `ConfigService` 全类（无调用方；`export_pool_to_config` 读 P61 将删字段） | ~60行 |
| B2 | `gdr_analysis.py` + `generalized_drop_rate.py` + `analysis_service.py` + `core/__init__.py:41-49,136-137` | 旧 GDR 计算器家族（`LegacyGDRCalculator`/`SuccessProbabilityAnalyzer` 等，被 `core.gdr.GDRCalculator` 取代；`AnalysisService` 是 `generalized_drop_rate` 唯一消费者） | ~250行 |
| B3 | `strategy.py:155-180` + `core/__init__.py:25` | `CompositeStrategy`（DeprecationWarning 壳，无调用方，P69 已由 `PriorityChainStrategy` 取代） | ~25行 |
| B4 | `per_pool_analysis.py:184` + `core/__init__.py:60` | `compute_transition_matrices`（DeprecationWarning，被 `*_from_flags` 版取代，无调用方） | ~15行 |
| B5 | `streaming.py:273/292/532` + `gdr.py:1114-1121` | `pool_end_resource` 标量双写 + 旧快照回退分支（标量只被一处回退读取，纯冗余） | ~10行 |
| B6 | ~~config_toml.py:1070-1071 双写清理~~（**移除，由 P61 承接**） | P61 Ph4 删除 `PoolEntry.pool_type` 字段、消费者改读 `bindings['type']`（P61 AUDIT-BREAK-4 已排期），方向与「Ps02 让 pool_type 唯一源」相反；两计划重叠冲突，Ps02 不处理该字段，统一由 P61 落地 | 0行 |
| B7 | `config_panel.py:3469-3475` + `config_toml.py:974-983` | strategy `'key'`/`'name'` 双格式（`save_toml` 恒写 `'key'`） | ~5行 |
| B8 | `config_panel.py:4240-4250,4265-4270` | `rule_type` 冒号+参数旧格式（TOML 存分离格式，冒号串只来自手写 TOML） | ~11行 |
| B9 | `pity.py:1149-1152` + `tests/test_p60_pity_state.py` | `PityState.from_dict` `'counters'` 旧格式升级分支（Ps01 保留，确认全库无生产者后删）+ 改测试 mock | ~4行 |
| B10 | ~~state.py:89-90 get_available_pools~~（**移除，由 P61 Ph1b 承接**） | P61 Ph1b 已排期删该方法 + 同步删 `tests/core/test_state.py:73-81` 的 `test_get_available_pools`（该测试调用此方法，Ps02 若删方法不删测试必红）；Ps02 不重复 | 0行 |

### 阶段 C：废弃 GUI 面板归档（按项目文件删除规则移入 .recycle_bin/，非删除）

| # | 位置 | 内容 |
|---|------|------|
| C1 | `gui/strategy_panel.py`、`gui/retreat_search_panel.py`、`gui/resource_search_panel.py` | P8 已合并到 `plan_search_panel.py`，未被 main_window 注册；仅 tests/gui/test_gui_imports.py 验证可 import |
| C2 | `core/forward_backward.py:12-37` | `ForwardStep`/`BackwardStep`/`ForwardResult`/`BackwardResult`（仅被 C1 死面板用；`ResourceSearch*` 保留，被活跃 `retreat_search.py` 用） |
| C3 | `vulnerability.py:36-38` | `lp_r`/`lp_curve`/`lp_var` 死字段（P51 后从不写入） |

## 四、实施阶段

| 阶段 | 内容 | 预估 |
|------|------|------|
| R1 | 阶段 A 全部（A1-A6，纯删除，无调用方改动） | ~145行 |
| R2 | 阶段 B 的 B1-B4（整文件/整类死代码删除 + 导出清理） | ~350行 |
| R3 | 阶段 B 的 B5-B10（改少量消费方/测试后删） | ~35行 + 测试 |
| R4 | 阶段 C（废弃面板归档 + forward_backward 半死部分 + lp_* 字段） | 归档 |

各阶段独立可运行、独立 commit。R1-R4 互相独立，可并行。每阶段完成跑 `pytest -q` + `ruff check`。

## 五、验收标准

- [ ] 阶段 A 删后 `pytest -q` 全量通过（删除无破坏）
- [ ] `grep -rn "PityDefParsed\|is_new_format\|_legacy_mode\|compute_all_gdr\|GDR_REGISTRY\|COMPACT_GDR_REGISTRY\|CompositeStrategy\|compute_transition_matrices\|ConfigService\|ForwardStep\|BackwardStep\|lp_r\|lp_curve\|lp_var"` 除归档外零命中
- [ ] 阶段 B 删后相关消费方已迁移、测试适配，`pytest -q` 全量通过
- [ ] 阶段 C 面板移入 `.recycle_bin/`，`tests/gui/test_gui_imports.py` 对应项更新
- [ ] `ruff check` 全绿
- [ ] 无历史包袱原则下，删除的每个符号都有「无生产者」的事实依据（不误删活功能）

## 六、不清理项（C 类，记录不排期）

| 项 | 位置 | 保留理由 |
|----|------|---------|
| 旧 TOML 保底自动迁移 | `config_toml.py:822-921` `_is_legacy_format`/`_migrate_legacy_pity` + `config_store.py:152` + `cli.py --migrate` | 用户可见健壮性（错误格式自动纠正），删除属产品决策 |
| `retreat_search` 别名 | `retreat_search.py:63-64,710-711`（`RetreatSearchResult`/`RetreatSearchEngine`） | 保留无害（一行别名）；C1 归档 retreat_search_panel 后唯一生产调用方消失，仅剩 tests/test_retreat_search.py，如清理可随 C1 顺带 |
| `stop_condition` history 回退 | `stop_condition.py:63-66` | 功能兜底（`acquired` 为 0 时），非格式兼容 |
| `config_panel` get_card_defs/set_card_defs | `config_panel.py:2683` | 内部活跃 API（自用 13 处），仅注释措辞 |

## 七、备注

- 与 Ps01 的关系：Ps01 修两个运行时真实缺陷；Ps02 清兼容性痕迹。B9（from_dict `'counters'` 分支）是 Ps01 保留项，确认无生产者后在本计划删
- 与 P61 的关系：B10（get_available_pools）、Pool 字段删除已由 P61 排期，Ps02 不重复；B1（ConfigService）P61 只删其中两方法，Ps02 删整类更彻底，执行时注意协调顺序（P61 删字段可能导致 ConfigService 先崩，本计划应在 P61 前后择机）
