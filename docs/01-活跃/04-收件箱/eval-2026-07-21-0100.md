# E1 Deep Evaluator 审查报告

**时间**：2026-07-21 01:00 UTC  
**审查范围**：18 commits（`9164b27..1bd2020`），上次 eval (06-17) 后  
**P38 交叉验证**：无 peer-review 文件

---

## 变更概要（最近批次 7/19–7/20）

| 类型 | 变更 | 关键文件 |
|------|------|---------|
| P55 | 保底体系重构完成 | `pity.py` 重大重写 |
| P56 | 轮换保底rotating | `pity.py` |
| P60 | PityState重构+acquired修复 | `state.py`, `pity.py` |
| P62 | 可达目标卡筛选——GDR分母区分 | `gdr.py`, `streaming.py` |
| P65 | 卡片标签系统——CardDefEntry.tags | `config_store.py`, `config_panel.py` (+694行) |
| P67 | CLI编码修复+测试基础设施 | `cli.py` |
| 回归修复 | migrated_from_legacy私有化回退 | `config_store.py` |

---

## 判定表

| 维度 | 判定 | 说明 |
|------|------|------|
| 架构分层 | ✅ | 零 core→gui 反向引用 |
| PR 边界 | ✅ | tests/ 在变更中——正常测试更新 |
| 测试覆盖 | ⚠️ WARN | P60 `is_limited()` 方法已删除但测试未同步——7 FAIL |
| 文档同步 | ✅ | 计划文件 P55/P56/P62/P65/P67 已同步 |
| 副作用 | ⚠️ WARN | P55 保底重构波及 retreat_config/vulnerability——已适配 |
| 计划一致性 | ℹ️ INFO | P58/P62/P65 归档，P64/P66/P67 新计划 |

---

## 判定结果：⚠️ FAIL（7 FAIL，2 WARN）

### FAIL：test_p60_is_limited.py — 7 测试全部失败

- `ConfigStore.is_limited()` 方法已删除（P60 收尾回退 `_migrated_from_legacy` 私有化）
- P65 测试已正确适配（`assert not hasattr(s, 'is_limited')`）
- P60 测试未同步更新——仍调用已删除的 API
- 修复：将 `test_p60_is_limited.py` 改为 `assert not hasattr` 模式（同 P65），或直接删除（方法不存在且不打算恢复）

### WARN 1：P60 测试残留

7 个测试文件断言不存在的 API。P65 测试证明团队已知晓 `is_limited` 已删除——P60 测试是同步遗漏。

### WARN 2：config_panel.py 持续膨胀

`config_panel.py` 新增 694 行至 ~3900 行（近 5× 阈值）。P65 新增 QListWidget 详情面板——合法功能增长，但拆分紧迫性增加。

---

## 测试概况

```
762 passed, 7 failed, 1 skipped
```

全部 7 个失败集中在 `test_p60_is_limited.py`（`AttributeError: 'ConfigStore' object has no attribute 'is_limited'`）。

---

## 升级标记

| # | 报告（历史） | 判定 |
|---|------------|------|
| — | eval-2026-06-17-1700 | ✅ CLEAN |
| — | eval-2026-06-17-1000 | ✅ CLEAN |
| — | eval-2026-06-17-0300 | ✅ CLEAN |
| 4 | **本次** | **⚠️ FAIL** |

CLEAN→FAIL 中断。`test_p60_is_limited.py` 是新的 FAIL 文件——首次出现，不触发升级。
