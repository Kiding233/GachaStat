# 测试体系 -- 问题

> 创建日期：2026-06-13（首次建档）
> 来源：覆盖率现状调查（2026-06-13）
> 关联计划：`P46 核心模块测试覆盖率提升计划.md`

---

## 概述

| 优先级 | 数量 | 条目 |
|--------|------|------|
| P1（风险） | 1 | 缺陷 1：核心计算模块测试覆盖严重不足 |

---

## P1：风险

### 缺陷 1：核心计算模块测试覆盖严重不足

- **来源**：覆盖率现状调查（2026-06-13）
- **严重度**：高——影响 P0 阻塞项修改的安全性
- **位置**：`core/` 层 7 个关键模块
- **问题**：总体覆盖率 29%，core/ 层 55%，但分布极不均匀：
  - `worst_impact.py` 22% → P31 要改
  - `comparison_analyzer.py` 20% → P19 要改（本周聚焦）
  - `retreat_search.py` 29% → P26 要改
  - `gdr.py` 43% → P28 依赖
  - `strategy.py` 49% → 所有 P0 依赖
  - `resource_gain.py` 49%
  - `streaming.py` 51%
- **影响**：修改 P0 模块时无测试安全网，每次改动依赖人工审查
- **推荐方案**：P46 计划——三阶段渐进式覆盖提升，7 模块 ~210 新用例
- **当前状态**：P46 计划设计中，未启动

---

## 问题依赖图

```
P1: 缺陷 1 (核心覆盖不足) ── P46 计划承接 ── 7 模块按依赖顺序推进
```

---

*问题条目随审查和计划推进动态更新。*
