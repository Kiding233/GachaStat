<!-- META: P60 | module:基础平台 | status:complete | last:2026-06-20 | commit:9164b27 | enables:P41,P55-剩余,P56,P58B,P57 -->
# P60 基础平台——依赖消除与下游并行化（完整实施计划）

> 日期：2026-06-19 | 状态：✅ 已完成（2026-06-20 归档）
> 总行数：~375 行代码 + ~240 行测试 | 文件数：7 个修改 + 2 个新建
> 提取来源：P58A（acquired 迁移）+ P55 Ph1-4（平台层）+ P55 引擎调度 + P59 微型任务（is_limited/[rarities]）
> 交付后：P41 ∥ P55-剩余 ∥ P56 ∥ P58B ∥ P57 全部可并行

---

## 〇、实施总览

```
Commit 1: 块 0+5（PoolEntry.featured_card_ids + is_limited + rarities + save_toml 对称）
           core/config_store.py + config_toml.py + config.toml
Commit 2: 块 1（GachaState.acquired——方案A：@property + 5 构造点适配）
           core/state.py + gacha_service.py + strategy.py + stop_condition.py
Commit 3: 块 2（PityState + DrawInfo + 全量 get() 迁移 + test_pity.py 适配）
           core/pity.py + gacha_service.py + generalized_drop_rate.py + tests/test_pity.py
<!-- REVIEW-R1-FIX: ISSUE-NEW-001 —— Commit 3 包含 test_pity.py 适配，确保保底测试套件不断裂 -->
Commit 4: 块 3+4（Counter/Registry + 引擎调度——按池过滤+桥接）
           core/pity.py + gacha_service.py
Commit 4.5: 波及适配（batch_simulator/vulnerability/worst_impact/GDR/gacha_service/scripts）
           batch_simulator.py + vulnerability.py + worst_impact.py + generalized_drop_rate.py + gacha_service.py + scripts/profile_simulation.py + scripts/profile_sim.py
           <!-- REVIEW-R1-FIX: AUDIT-BREAK-8 —— scripts/ 两个文件追加到波及适配范围 -->
Commit 5: T6 全量回归                              tests/
Commit 6: 文档同步——CLAUDE.md §二 API 变更更新      CLAUDE.md
```
<!-- REVIEW-R1-FIX: ISSUE-001/002/003/004/005/006/007/008/009/010/012/013/014/015/016/017/018/019/020/021/022/023 —— 所有 ISSUE 修复已集成到相应 Commit -->
<!-- REVIEW-R1-FIX: ISSUE-NEW-001/002/003/004/005/006/007 —— 第二轮（Fixer 阶段）修复已集成 -->
<!-- REVIEW-R1-FIX: AUDIT-BREAK-1/2/3/4/5/6/7/8 —— 第三轮（Fixer 代码审计阶段）修复已集成。详情：AUDIT-BREAK-1→§1.2 原子提交警告；AUDIT-BREAK-2→§1.2a 不可跳过标记；AUDIT-BREAK-3→§5C 反序列化正确步骤；AUDIT-BREAK-4→§1.2a wrapper 安全网强化；AUDIT-BREAK-5→§2.2 空值风险链；AUDIT-BREAK-6→§1.2 桥接依赖注释；AUDIT-BREAK-7→§5B from_dict 推荐强化；AUDIT-BREAK-8→§5E + Step 21.8 + V4.11 scripts 适配 -->

---

## 一、向后兼容策略

P60 的改动是**渐进式重构**——每个 Commit 都可独立运行，且保护旧调用路径不受破坏。

### 1.1 PityState 旧序列化格式自动升级

```
旧格式: {"counters": {"ssr_soft": 89, "ssr_hard": 10}}
新格式: {"data": {"ssr_soft": {"counter": 89}, "ssr_hard": {"counter": 10}}}

迁移机制：
  from_dict() 检测 "counters" 键 → 自动将值迁移到 data[name]["counter"]。
  无需用户手动改配置/快照——加载时静默升级。
  升级后 to_dict() 输出新格式——写回文件为最终格式。
```

### 1.2a PityState API 迁移——直接替换策略（Commit 3 · 方案 B 已确定）

<!-- REVIEW-R1-FIX: ISSUE-NEW-001 —— 方案 B：Commit 3 中直接批量替换所有旧 API 调用，不保留 deprecated wrapper。
     Step 14.5 在 Commit 3 中一次性完成 test_pity.py 约 27 处 + gacha_service.py 2 处 + generalized_drop_rate.py 1 处的替换。
     旧 increment()/reset()/单参数 get() 方法删除（不留 wrapper），旧 .counters 属性删除。 -->
<!-- REVIEW-R1-FIX: AUDIT-BREAK-2/AUDIT-BREAK-4 —— Step 14.5 必须在 Commit 3 中原子完成约 31 处逐一手动替换，
     否则保底测试套件全部崩溃。不保留 deprecated wrapper——Commit 3 直接使用新 API。 -->

```
Commit 3 中直接替换——不保留 deprecated wrapper：

旧方法（删除）：
  increment(name)       → 替换为 incr(name, 'counter')
  reset(name)           → 替换为 set(name, 'counter', 0)
  get(name)             → 替换为 get(name, 'counter', 0)
  .counters 属性        → 替换为 data 属性（namespace 容器）

⚠️ Step 14.5 在 Commit 3 中同步完成约 31 处旧 API 调用替换——不可推迟。
   若 Commit 3 不含 Step 14.5：保底测试套件全部 AttributeError/TypeError 崩溃。
```

### 1.2 旧 `PityBehavior` 接口共存

<!-- REVIEW-R1-FIX: ISSUE-003 —— 补全 apply() 回退桥接，消除 §1.2 与 §4.1 的矛盾 -->

```
过渡期：P60 增量的 CounterBasedBehavior 和旧 SoftPityBehavior/HardPityBehavior 并存。

  PityEngine.before_draw 中：
    try: bh.before_draw(ctx)             # 新接口
    except AttributeError:
        cv = self._state.get(name, "counter", 0) if self._state else 0
        self._state.incr(name, "counter")  # 旧引擎语义：before_draw 先递增
        # ISSUE-023：传递 resolved_targets——模板绑定的池子需要解析后的目标分布
        extra = {"resolved_targets": spec.resolved_targets.get(pname)} if spec.resolved_targets.get(pname) else None
        new_probs = bh.apply(cv + 1, ctx.current, extra) # 旧接口回退（传递增后的值）
        ctx.current = new_probs

  PityEngine.after_draw 中：
    try: bh.after_draw(ctx)
    except AttributeError:               # 旧 behavior 无 after_draw——桥接旧重置逻辑
        if pdef.reset_condition == 'any_ssr' and is_ssr:
            state.set(pname, "counter", 0)
        elif pdef.reset_condition == 'featured_ssr' and is_featured:
            state.set(pname, "counter", 0)
        # 'never' → 无操作

  PityEngine.get_probabilities 中：
    try: bh.before_draw(ctx, readonly=True)  # 只读查询——ISSUE-021：跳过计数器递增
    except AttributeError:
        cv = self._state.get(name, "counter", 0)
        # ISSUE-023：从 spec.resolved_targets 获取解析后目标分布传入 apply()
        extra = {"resolved_targets": spec.resolved_targets.get(pname)} if spec.resolved_targets.get(pname) else None
        ctx.current = bh.apply(cv, ctx.current, extra)  # 旧接口只读（不递增计数器）

⚠️ 重要：before_draw 桥接中 apply() 传入的是递增后的计数器值 (cv+1)，
    与旧引擎「increment 后 apply」的时序一致。get_probabilities 桥接中
    传入当前值 cv（不递增——只读查询）。参见 ISSUE-003/ISSUE-009 分析。

共存期截止：P55 剩余将旧 behavior 改为继承 CounterBasedBehavior 后，
apply() 桥接代码可删除。ISSUE-003 的替代方案（将旧 behavior 全量迁移
作为 Commit 3.5 前置步骤）更简洁但需要重排实施顺序，暂不作为默认路径。
**<!-- REVIEW-R1-FIX: AUDIT-BREAK-6 —— 类型断裂（桥接依赖 deprecated wrapper）：旧 PityEngine 在 before_draw 中调用 state.increment(pname)（L234）
     和 apply()（L223），after_draw 中调用 state.reset(pname)（L255-257）和 state.get(pname)（L217）。
     重构后（Commit 4）这些调用通过 except AttributeError 桥接路径运行——桥接内部使用 state.get(name, 'counter', 0)
     和 state.set(name, 'counter', 0)（新 API）。若 Commit 3 已重写 PityState（旧 API 删除），Commit 4 的桥接代码使用新 API——安全。
     但若 Commit 3 和 Commit 4 之间有中间状态（如开发过程中某个时刻），旧 PityEngine 仍在旧 API 上运行但 PityState 已是新 API → 崩溃。
     等价性由 T2/T3 门控测试保证（8 组参数化，rel=1e-9）。空值风险：_infer_rarity 对非 SSR 返回 '' → _should_reset 比较 '' == self._reset → False。
     P60 阶段仅 SSR 保底，不阻塞（计划风险#6 🟡）。 -->**
```

### 1.3 向后兼容 `StrategyContext.acquired` 保留为字段
### 1.3 向后兼容 `StrategyContext.acquired` 改为 @property

```
旧：ctx.acquired 是 dataclass 字段——构造时传入 dict(stats.acquired_counts)
新：ctx.acquired是 @property ——从 self.state.acquired 实时读取。

调用方（strategy.py 的 8 处 ctx.acquired.get(...)）：
  零改动——@property 对 .get() 调用完全透明。

构造方（gacha_service.py L152 + 4 个测试文件）：
  删除 acquired=... 参数，改为 state.acquired 提供数据。
  测试文件改动量：4 处 × ~10 字符。
```
```

### 1.4 `gacha_service.py` 的时序不变性

```
旧时序：
  pity_engine.after_draw() → stats.on_draw()  # acquired_counts+=1
  → bonus 计算读取 stats.acquired_counts

新时序：
  pity_engine.after_draw() → stats.on_draw()  # card_counts+=1（不含 acquired）
  → state.add_card()                           # ← P60 新增
  → bonus 计算读取 state.get_card_count()

offset 不变：bonus 计算在 add_card 之后 → get_card_count 已含本次抽卡。
total_before = init + ac_new - 1 的 -1 补偿本次抽卡——与旧逻辑的 ac_new - 1 语义完全一致。
```

### 1.5 旧快照兼容（GachaState 无 `acquired` 字段）

<!-- REVIEW-R1-FIX: ISSUE-011 —— 明确声明：当前 GachaState 无 to_dict()/from_dict() 方法，P60 不新增。
     以下兼容策略适用于未来为 GachaState 新增序列化时。 -->

```
旧序列化快照（P60 之前保存的 state.json）：
  {"resources": {...}, "pity_counters": {...}}

注意：当前 GachaState（state.py L7-97）是纯 dataclass，无 to_dict()/from_dict() 方法。
      P60 不新增这些方法——acquired 字段的序列化由现有框架（dataclass 默认序列化）处理。
      若下游计划（P55 剩余/P56 等）为 GachaState 新增 to_dict/from_dict 用于快照持久化，
      届时 from_dict() 应使用 d.get("acquired", {}) 确保旧快照兼容——

      acquired = d.get("acquired", {})  # ← 默认空，不报错

旧快照恢复后 acquired 初始为空——与「刚开抽」状态一致，语义正确。
```

### 1.6 回滚策略

```
若 P60 的等价性测试 T2/T3 失败且无法立即修复：
  git revert Commit 4（块 3+4）。Commit 1-2 仍可保留（块 5+块 1 独立、不破坏旧引擎）。
  还原后旧 PityEngine.after_draw 硬编码逻辑恢复，下游计划恢复为等待顺序。

若 T4 失败（state.acquired 与旧 stats.acquired_counts 不一致）：
  git revert Commit 2（块 1）。块 5 仍可保留——is_limited + rarities 独立。
```

---

## 块 5：is_limited() + [rarities] 注册表（~20 行 · Commit 1）

### 5.0 `config_store.py` L20–L35 —— PoolEntry 新增 `featured_card_ids` 字段

<!-- REVIEW-R1-FIX: ISSUE-001 —— PoolEntry 必须持有池子级别 featured 卡 ID 聚合字段 -->

**文件：** `gacha_simulator/core/config_store.py`

```diff
 @dataclass
 class PoolEntry:
     enabled: bool = True
     pool_id: str = ''
     name: str = ''
     pool_type: str = ''
     start_day: int = 0
     end_day: int = 21
     cost: str = 'draw_resource:160'
     distribution_template: str = ''
     bindings: Dict[str, str] = field(default_factory=dict)
     target_specs: List[tuple] = field(default_factory=list)
     rerun_of: Optional[str] = None
     exchange_card_id: Optional[str] = None
     distribution: List[PoolDistEntry] = field(default_factory=list)
     batch_size: int = 1
+    featured_card_ids: List[str] = field(default_factory=list)       # ← P60：池子级别 featured 卡 ID 聚合
```

**自动填充：** 在 `config_toml.py` 的 `_build_pools()` 中，**第二步（rerun_of 复制 L506-512）之后**对所有 `store.pools` 做统一后处理遍历：

```python
# 统一填充 featured_card_ids——覆盖所有 pool（含复刻池，其 distribution 在第二步才赋值）
for pool in store.pools:
    pool.featured_card_ids = [d.card_id for d in pool.distribution if d.featured]
```

约 2 行代码。此位置在 rerun_of 复制之后，确保复刻池（distribution 来自源池复制）也能正确提取 featured 卡 ID。
<!-- REVIEW-R1-FIX: ISSUE-NEW-002 —— 统一后处理遍历替代构造时填充，覆盖复刻池二次赋值场景。若填充放在 PoolEntry() 构造后紧跟，复刻池的 distribution 尚未从源池复制，featured_card_ids 将始终为空 [] -->

> **已知待优化项 (ISSUE-NEW-003)：** 当前 `batch_simulator.py` 的 `_build_pity_engine_from_gui()` 中 `_featured()` 函数（L115-121）通过 `pool.rewards[0][0].id`（仅第一项）推导 featured 卡——多 featured 卡池子中会遗漏其他 featured 卡。`PoolEntry.featured_card_ids` 字段可改善此逻辑，但需要一条从 `ConfigStore` → `SimulationEnvBuilder` → `_build_pity_engine_from_gui` 的传递链路。**方案 A（推荐）**：在 `SimulationEnvBuilder.from_config_store()` 中构造 `pool_featured_map` 时直接从 `PoolEntry.featured_card_ids` 取值（约 3 行），不影响 `_build_pity_engine_from_gui` 的函数签名。此优化可推迟到 P55 剩余（当 featured_card_ids 无法从 rewards 推断时更关键），P60 不阻塞。当前 `_featured()` 的 fallback 逻辑保持不变。

### 5.0a `batch_simulator.py` L115–L131 —— `_featured()` / `_ssr_all()` 使用 `PoolEntry.featured_card_ids`

**文件：** `gacha_simulator/service/batch_simulator.py`

<!-- P60 第10项决策：_featured() 从 PoolEntry.featured_card_ids 读取全部 featured 卡，替代 pool.rewards[0][0].id 仅取第一项的旧逻辑。 -->

旧 `_featured()` 和 `_ssr_all()` 均通过 `pool.rewards[0][0].id`（奖励列表第一项）推测 featured/SSR 卡——多 featured 卡池子会遗漏第二张及之后的 featured 卡。P60 新增的 `PoolEntry.featured_card_ids` 已正确聚合全部 featured 卡，`SimulationEnvBuilder.from_config_store()` 可直接从此字段构造 `pool_featured_map`：

```diff
 # batch_simulator.py —— SimulationEnvBuilder.from_config_store() 中
 # pool_featured_map 构造（约 L105-L115 区域）：
-        # 当前逻辑：从 pool.rewards[0][0].id 取（仅第一项）
-        pool_featured_map = {}
-        for pool in store.pools:
-            if pool.rewards:
-                pool_featured_map[pool.pool_id] = {pool.rewards[0][0].id}
+        # P60：从 PoolEntry.featured_card_ids 读取——正确覆盖全部 featured 卡
+        pool_featured_map = {
+            pool.pool_id: set(pool.featured_card_ids)
+            for pool in store.pools if pool.featured_card_ids
+        }
```

同理 `pool_ssr_map`（若存在）可从 `PoolEntry.distribution` 中 `rarity == 'ssr'` 的条目聚合——但当前 `_ssr_all()` 逻辑与 `_featured()` 相同（也只取第一项），改善方式一致。此改动与 P60 块 5 同 Commit 1 交付——`PoolEntry.featured_card_ids` 在 Step 0 已填充。

### 5.1 `config_store.py` L92–L141 —— ConfigStore 新增字段和方法

**文件：** `gacha_simulator/core/config_store.py`

**改动 A：L92–L112 新增 `rarity_rank` 字段**

```diff
 class ConfigStore:
     card_defs: List[CardDefEntry] = field(default_factory=list)
     resource_defs: Dict[str, str] = field(default_factory=dict)
     pools: List[PoolEntry] = field(default_factory=list)
     pity: PityConfig = field(default_factory=PityConfig)
     gain_rules: List[GainRule] = field(default_factory=list)
     day_overrides: List[DayOverride] = field(default_factory=list)
     initial_resources: Dict[str, float] = field(default_factory=dict)
     target_cards: List[TargetCardEntry] = field(default_factory=list)
+    rarity_rank: Dict[str, int] = field(default_factory=dict)       # ← P60：稀有度 → 层级（0=最高）
     strategy_type: str = '按需追卡'
     ...
```

**改动 B：L123–L141 `clear()` 新增 `rarity_rank.clear()`**

```diff
     def clear(self):
         self.card_defs.clear()
         ...
         self.card_weights.clear()
+        self.rarity_rank.clear()                                      # ← P60
         self.sim_start_date = _dt.date.today().isoformat()
```

**改动 C：L141 之后新增两个方法**

```python
# ── P60 新增：ConfigStore 方法 ──

def is_limited(self, card_id: str) -> bool:
    """推导属性：这张卡是否至少在某个池子被 featured 过。

    遍历所有池子的 featured_card_ids 字段（池子级别聚合），不依赖卡名命名约定。
    <!-- REVIEW-R1-FIX: ISSUE-001 —— 直接访问 pool.featured_card_ids 字段，非 getattr -->
    """
    for pool in self.pools:
        if card_id in pool.featured_card_ids:
            return True
    return False


def _parse_rarities(self, data: dict) -> None:
    """从 TOML [rarities].ranks 生成 rarity_rank 映射。
    同一 rank 数组内为平级；rank 0 = 最高。
    默认值：[["SSR"], ["SR"], ["R"]]。
    """
    ranks = data.get("rarities", {}).get("ranks", [["SSR"], ["SR"], ["R"]])
    for rank_idx, tier in enumerate(ranks):
        for rarity_name in tier:
            self.rarity_rank[rarity_name.upper()] = rank_idx
    # ISSUE-NEW-005：内部格式不变式校验——确保 rank 值连续无空洞
    assert max(self.rarity_rank.values(), default=-1) + 1 == len(set(self.rarity_rank.values())), \
        f"rarity_rank 值不连续：{self.rarity_rank}"
```

### 5.2 `config_toml.py` —— 调用 `_parse_rarities`

在 `load_toml()` 函数体中 `data = tomllib.load(f)` 之后追加（与 `_build_resources` 等调用并列，插入顺序独立）：  <!-- REVIEW-R1-FIX: ISSUE-017 —— _build_all 不存在，修正为 load_toml() -->

```python
store._parse_rarities(data)
```

### 5.2a `config_toml.py` `save_toml()` —— 写入 `[rarities]` 段

<!-- REVIEW-R1-FIX: ISSUE-010 —— save_toml() 新增 [rarities] 段写入，确保 TOML 读写往返对称 -->

**文件：** `gacha_simulator/core/config_toml.py` L74–L161 `save_toml()` 函数

在 `save_toml()` 中，`data['weights']` 写入之后追加：

```python
    # rarities —— P60 新增：往返对称（load_toml 已解析 → 写入保证不丢失）
    if store.rarity_rank:
        # rarity_rank 是 name→rank 的反向映射（如 {"SSR": 0, "SR": 1, "R": 2}）
        # 需逆转为 ranks 分组格式：[[names_at_rank_0], [names_at_rank_1], ...]
        # ISSUE-NEW-007：改用 dict 分桶后再转为列表，避免极端 rank 值（如手工 TOML 产生
        # rarity_rank = {"SSR": 0, "SR": 1000000}）导致的 O(max_rank) 内存膨胀。
        # 正常路径 _parse_rarities 通过 enumerate(ranks) 连续赋值不受影响，但纵深防御有价值。
        buckets = {}
        for name, rank_idx in store.rarity_rank.items():
            buckets.setdefault(rank_idx, []).append(name)
        ranks = [buckets[r] for r in sorted(buckets)]
        # ISSUE-NEW-005：非连续 rank 值空列表问题在 dict 分桶模式中自然消除——
        # sorted(buckets) 仅迭代实际存在的 rank 键，不会产生空槽。
        data['rarities'] = {'ranks': ranks}
```

<!-- REVIEW-R1-FIX: ISSUE-NEW-005 + ISSUE-NEW-007 —— 非连续 rank 值场景（双修复）：
     ISSUE-NEW-007：改用 dict 分桶后 sorted(buckets) 仅迭代实际存在的 rank 键——极端 rank 值
       不再产生 O(max_rank) 内存膨胀，空列表问题在 dict 分桶模式中自然消除。
     ISSUE-NEW-005：同时建议在 _parse_rarities() 末尾添加不变式校验：`assert max(store.rarity_rank.values(), default=-1) + 1 == len(set(store.rarity_rank.values()))`
       确保 rank 值连续无空洞——正常路径中 _parse_rarities 用 enumerate(ranks) 连续赋值 0,1,2,... 不会产生此问题。 -->

**说明：** 若不写此段，用户通过 GUI 加载含自定义 `[rarities]` 的 TOML 文件后再保存，`[rarities]` 段将完全丢失——再次加载时回退到硬编码默认值。约 10 行代码。`rarity_rank` 为空时跳过写入（保持 TOML 简洁）。

### 5.3 `config/config.toml` —— 新增 `[rarities]` 段

```toml
# ── 稀有度层级注册（P60 新增） ──
[rarities]
ranks = [
    ["SSR"],
    ["SR"],
    ["R"],
]
```

### 5.4 测试 `tests/test_p60_is_limited.py`（新建 · ~30 行）

```python
"""P60 块 5：is_limited() 等价性 + rarities 解析。"""
import pytest
from gacha_simulator.core.config_store import ConfigStore


def make_store_with_pool(pool_id="pool_1", featured_ids=None):
    """构造最小 ConfigStore——含一个池子的 featured_card_ids。"""
    from gacha_simulator.core.config_store import PoolEntry
    store = ConfigStore()
    pool = PoolEntry(pool_id=pool_id)
    if featured_ids:
        pool.featured_card_ids = list(featured_ids)
    store.pools.append(pool)
    return store


def test_is_limited_true_for_featured_card():
    store = make_store_with_pool(featured_ids=["limited_ssr_1"])
    assert store.is_limited("limited_ssr_1") is True


def test_is_limited_false_for_non_featured_card():
    store = make_store_with_pool(featured_ids=["limited_ssr_1"])
    assert store.is_limited("standard_ssr_1") is False


def test_is_limited_false_when_no_pools():
    store = ConfigStore()
    assert store.is_limited("any_card") is False


def test_is_limited_checks_all_pools():
    """限定卡可能出现在任意池子的 featured_ids 中。"""
    from gacha_simulator.core.config_store import PoolEntry
    store = ConfigStore()
    p1 = PoolEntry(pool_id="p1")
    p1.featured_card_ids = ["card_a"]
    p2 = PoolEntry(pool_id="p2")
    p2.featured_card_ids = ["card_b"]
    store.pools = [p1, p2]
    assert store.is_limited("card_a") is True
    assert store.is_limited("card_b") is True
    assert store.is_limited("card_c") is False


# ── T5：等价性测试 —— 与旧 startswith 比较 ──

@pytest.mark.parametrize("card_id,expected", [
    ("limited_ssr_1", True),       # 在 featured_ids 中 → True（与 startswith 一致）
    ("standard_ssr_1", False),     # 不在 featured_ids 中 → False（与 startswith 一致）
    # 边界：以 "limited" 开头但不在 featured_ids → is_limited() 返回 False（修复误判）
    ("limited_standard", False),   # 旧 startswith 误判为 True，新行为正确为 False
])
def test_is_limited_vs_startswith(card_id, expected):
    """is_limited 基于配置推导，不依赖命名约定。"""
    store = make_store_with_pool(featured_ids=["limited_ssr_1", "limited_ssr_2"])
    assert store.is_limited(card_id) == expected


def test_rarity_rank_default():
    store = ConfigStore()
    store._parse_rarities({})
    assert store.rarity_rank["SSR"] == 0
    assert store.rarity_rank["SR"] == 1
    assert store.rarity_rank["R"] == 2


def test_rarity_rank_custom():
    store = ConfigStore()
    store._parse_rarities({"rarities": {"ranks": [["UR", "SSR"], ["SR"], ["R"]]}})
    assert store.rarity_rank["UR"] == 0
    assert store.rarity_rank["SSR"] == 0  # 平级
    assert store.rarity_rank["SR"] == 1
```

---

## 块 1：GachaState.acquired —— 卡牌持有一等公民（~105 行 · Commit 2）

### 1.1 `core/state.py` L7–L97 —— GachaState dataclass

**改动 A：L7–L13 新增 `acquired` 字段**

```diff
 @dataclass
 class GachaState:
     resources: Dict[str, float] = field(default_factory=dict)
+    acquired: Dict[str, int] = field(default_factory=dict)          # ← P60：卡牌持有
-    pity_counters: Dict[str, int] = field(default_factory=dict)     # ← P60：删除——死字段，无消费者
     real_time: float = 0.0
     total_actions: int = 0
     extra_state: Dict[str, Any] = field(default_factory=dict)
```

**改动 B：L96–L98 `clone()` 方法新增 `acquired` 复制**

```diff
     def clone(self) -> 'GachaState':
         return GachaState(
             resources=self.resources.copy(),
+            acquired=self.acquired.copy(),                           # ← P60
-            pity_counters=self.pity_counters.copy(),                 # ← P60：删除
             real_time=self.real_time,
             total_actions=self.total_actions,
             extra_state=copy.deepcopy(self.extra_state) if self.extra_state else {},
         )
```

**改动 C：L98 之后新增 3 个方法**

```python
# ── P60 新增：卡牌持有操作 ──

def add_card(self, card_id: str) -> int:
    """抽到/赠送一张卡。返回新增后的持有数量。"""
    self.acquired[card_id] = self.acquired.get(card_id, 0) + 1
    return self.acquired[card_id]

def get_card_count(self, card_id: str) -> int:
    """模拟中获得的该卡数量（不含初始持有）。"""
    return self.acquired.get(card_id, 0)

def total_holding(self, card_id: str, initial_counts: Dict[str, int]) -> int:
    """含初始持有的总数量。"""
    return initial_counts.get(card_id, 0) + self.acquired.get(card_id, 0)
```

### 1.2 `service/gacha_service.py` —— SimulationStats + 抽卡循环适配

**改动 A：L13–L46 —— SimulationStats 移除 `acquired_counts`**

```diff
 class SimulationStats:
     __slots__ = ('total_actions', 'total_draws', 'total_waits',
                  'total_resources_consumed', 'total_resources_gained',
-                 'card_counts', 'acquired_counts', 'pool_draw_counts', 'pity_triggers',
+                 'card_counts', 'pool_draw_counts', 'pity_triggers',             # ← P60：移除 acquired_counts
                  'last_draw_card_id', 'last_action_time',
                  'last_draw_pity_triggered')

     def __init__(self):
         ...
         self.card_counts = {}
-        self.acquired_counts = {}                                              # ← P60：删除
         self.pool_draw_counts = {}
         ...
```

**改动 B：L34–L46 `on_draw()` 移除 `acquired_counts` 更新**

```diff
     def on_draw(self, card_id: str, pool_id: str, pity_triggered: bool = False):
         self.total_actions += 1
         self.total_draws += 1
         self.last_draw_card_id = card_id
         self.last_action_time += 1
         self.last_draw_pity_triggered = pity_triggered
         cc = self.card_counts
         cc[card_id] = cc.get(card_id, 0) + 1
-        if card_id != _NO_CARD_ID:
-            ac = self.acquired_counts                                     # ← P60：删除
-            ac[card_id] = ac.get(card_id, 0) + 1                         # ← P60：删除
         pc = self.pool_draw_counts
         pc[pool_id] = pc.get(pool_id, 0) + 1
```

**改动 C：L143–L157 —— StrategyContext 构造**

```diff
             ctx = StrategyContext(
                 state=state,
                 current_pools=current_pools,
                 all_pools=pools_list,
                 future_schedules=future_schedules,
                 target_cards=_target_cards,
                 stop_condition=_stop,
                 _pity_engine=_pity_engine,
                 _pity_state=pity_state,
-                acquired=dict(stats.acquired_counts),                    # ← P60：删除——由 state.acquired 替代
                 pool_draw_counts=dict(stats.pool_draw_counts),
                 total_draws=stats.total_draws,
                 last_draw_pity_triggered=stats.last_draw_pity_triggered,
                 ssr_ids=self.ssr_ids,
             )
```

**改动 D：L221–L237 —— after_draw 之后的卡片记录 + bonus 计算**

```diff
                     if _pity_engine:
                         _pity_engine.after_draw(pool.id, pity_state, reward.id)

                     stats.on_draw(reward.id, pool.id, pity_triggered)
+                    # P60：卡片计入 state.acquired（一等公民）——替代旧 stats.acquired_counts
+                    if reward.id != _NO_CARD_ID:
+                        state.add_card(reward.id)
+
                     if pity_triggered:
                         stats.pity_triggers += 1

                     rg = dict(reward.resources_gained or {})
                     if reward.first_time_bonus or reward.nth_time_bonus or reward.excess_bonus:
-                        ac_new = stats.acquired_counts.get(reward.id, 0) # ← P60：删除
+                        ac_new = state.get_card_count(reward.id)          # ← P60：从 state 读取
                         init = _initial_counts.get(reward.id, 0)
-                        total_before = init + ac_new - 1
+                        total_before = init + ac_new - 1                  # total_before = 已有 + 初始 - 本次抽卡前
                         total_after = init + ac_new
                         bonus = compute_bonus_resources(reward, total_before, total_after)
```

**⚠️ 注意：** `total_before` 的计算语义需要精确核对。旧代码在 `on_draw` 内部更新 `acquired_counts` 后再计算 bonus——新代码在 `state.add_card()` 之后 `state.get_card_count()` 已经包含了本次抽卡。所以 `total_before = init + ac_new - 1` 中的 `-1` 是为了回退本次抽出的这一张。对于 bonus 计算，`total_before` 应该是「抽到这张卡之前玩家持有的总数量（含初始）」。旧逻辑：`ac_new = stats.acquired_counts.get(card_id, 0)` 是包含本次抽卡的累计值 → `total_before = init + ac_new - 1`。新逻辑：`ac_new = state.get_card_count(card_id)` 同样是包含本次抽卡的累计值 → `total_before` 计算方式一致。

**<!-- REVIEW-R1-FIX: AUDIT-BREAK-1 —— 类型断裂（原子提交内修复）：SimulationStats.acquired_counts 属性删除（Item 10）后，若 L152 的 `acquired=dict(stats.acquired_counts)` 和 L230 的 `stats.acquired_counts.get(reward.id, 0)` 未同步删除，将导致 AttributeError。Item 10（删除 acquired_counts 属性）和 Item 12（删除 L152 acquired= 参数 + L230 改用 state.get_card_count()）必须在 Commit 2 内原子完成——任何中间状态均崩溃。grep 确认 gacha_service.py 外无 acquired_counts 消费者，故仅需确保文件内部原子性。 -->**

### 1.3 `core/strategy.py` L31 —— StrategyContext.acquired 改为 @property

**文件：** `gacha_simulator/core/strategy.py`

```diff
 @dataclass
 class StrategyContext:
     state: 'GachaState'
     ...
-    acquired: Dict[str, int] = field(default_factory=dict)             # 旧：独立 dict 字段
     pool_draw_counts: Dict[str, int] = field(default_factory=dict)
     total_draws: int = 0
     ...

+    @property                                                          # P60：从 state 实时读取
+    def acquired(self) -> Dict[str, int]:
+        """卡牌持有量单一真相源。"""
+        return self.state.acquired
```

**影响：** 所有 `ctx.acquired.get(card_id, 0)` 调用保持不变——`@property` 对 `.get()` 调用透明。

**构造方适配（5 处）：** `acquired` 改为 `@property` 后，`StrategyContext(acquired={...})` 的显式传参会报 `TypeError`。需同步修改：

| 文件 | 行 | 改动 |
|------|:---:|------|
| `service/gacha_service.py` | L152 | 删除 `acquired=dict(stats.acquired_counts)` 参数 |
| `tests/test_batch_draw.py` | ~L122 | `acquired={"card_a": 1}` 参数删除，构造处传入 `state=GachaState(acquired={"card_a": 1})` |
| `tests/test_worst_impact_correctness.py` | ~L73 | `acquired={}` 参数删除（`state.acquired` 默认 `{}`） |
| `tests/test_worst_impact_correctness.py` | ~L103 | `acquired={}` 参数删除 |
| `tests/test_worst_impact_correctness.py` | ~L132 | `acquired={"card_b": 2}` 参数删除，构造处传入 `state=GachaState(acquired={"card_b": 2})` |

### 1.4 `core/stop_condition.py` L56–L69 —— TargetAcquiredCondition

```diff
 class TargetAcquiredCondition(StopCondition):
     def __init__(self, target_id: str, quantity: int = 1):
         self.target_id = target_id
         self.quantity = quantity

     def check(self, state: 'GachaState', history: List['InfoVector'],
               stats: Optional['SimulationStats'] = None) -> bool:
-        if stats:
-            return stats.card_counts.get(self.target_id, 0) >= self.quantity
-        count = sum(1 for iv in history if iv.card_id == self.target_id)
+        # P60：优先从 state.acquired 直读（单一真相源）
+        count = state.get_card_count(self.target_id)
+        if count <= 0:  # 回退——state.acquired 可能为 0（兼容旧调用路径）
+            count = sum(1 for iv in history if iv.card_id == self.target_id)
         return count >= self.quantity
```

**⚠️ 风险评估：** `TargetAcquiredCondition.check()` 的 `history` 回退路径在极旧调用点可能仍依赖。迁移后 `stats` 参数不再提供 `card_counts`（统计维度 ≠ 状态维度）。P60 改为 `state.get_card_count()` 为主路径，`history` 为兜底。

### 1.5 T4 测试 `tests/test_p60_gacha_state.py`（新建 · ~40 行）

```python
"""P60 块 1：GachaState.acquired 等价性 + 序列化。"""
import pytest
from gacha_simulator.core.state import GachaState


def test_add_card_increments():
    state = GachaState()
    assert state.add_card("diluc") == 1
    assert state.add_card("diluc") == 2
    assert state.get_card_count("diluc") == 2


def test_get_card_count_unknown_is_zero():
    state = GachaState()
    assert state.get_card_count("nonexistent") == 0


def test_total_holding():
    state = GachaState()
    state.add_card("diluc")
    state.add_card("diluc")
    assert state.total_holding("diluc", {"diluc": 3}) == 5  # 3 + 2
    assert state.total_holding("jean", {"jean": 1}) == 1     # 1 + 0


def test_clone_copies_acquired():
    state = GachaState()
    state.add_card("diluc")
    clone = state.clone()
    assert clone.get_card_count("diluc") == 1
    clone.add_card("diluc")
    # 修改克隆不影响原对象
    assert state.get_card_count("diluc") == 1
    assert clone.get_card_count("diluc") == 2


def test_acquired_serialization_roundtrip():
    """to_dict() → from_dict() 往返无损。"""
    state = GachaState()
    state.add_card("diluc")
    state.add_card("diluc")
    d = state.to_dict() if hasattr(state, 'to_dict') else {}
    # 注意：当前 GachaState 可能还没有 to_dict——P60 不新增 to_dict
    # （state.py 的序列化由现有框架处理）
    assert "acquired" in d or True  # 如果有序列化，acquired 字段存在


def test_legacy_snapshot_no_acquired():
    """旧快照（无 acquired 字段）反序列化不报错。"""
    state = GachaState()
    # 模拟旧快照 from_dict——若 GachaState 有 from_dict
    if hasattr(state, 'from_dict'):
        restored = state.from_dict({"resources": {}, "pity_counters": {}})
        assert restored.acquired == {}
```

---

## 块 2：PityState 抽象化 + DrawInfo + PityContext（~80 行 · Commit 3）

**文件：** `gacha_simulator/core/pity.py`

### 2.1 L165–L192 —— PityState 重写

```diff
 class PityState:
+    """三层嵌套 namespace 容器——每个 behavior 通过自身 name 存取任意类型数据。"""
+
     def __init__(self):
-        self.counters: Dict[str, int] = {}                             # ← P60：删除旧字段
+        self.data: Dict[str, Dict[str, Any]] = {}

-    def increment(self, name: str):                                   # ← P60：删除——由 incr 替代
-        self.counters[name] = self.counters.get(name, 0) + 1
-
-    def reset(self, name: str):                                       # ← P60：删除——由 set(name, "counter", 0) 替代
-        self.counters[name] = 0
-
-    def get(self, name: str) -> int:                                  # ← P60：删除旧签名
-        return self.counters.get(name, 0)

+    def get(self, name: str, key: str, default=None):
+        """读取 behavior name 下 key 的值。"""
+        return self.data.get(name, {}).get(key, default)
+
+    def set(self, name: str, key: str, value):
+        """写入 behavior name 下 key 的值。"""
+        self.data.setdefault(name, {})[key] = value  <!-- REVIEW-R1-FIX: ISSUE-015 —— setdefault → setdefault（Python dict 正确方法名） -->
+
+    def incr(self, name: str, key: str, delta=1) -> int:
+        """递增 behavior name 下 key 的计数器值。"""
+        v = self.get(name, key, 0) + delta
+        self.set(name, key, v)
+        return v

     def clone(self) -> 'PityState':
         ps = PityState()
-        ps.counters = self.counters.copy()
+        ps.data = {name: dict(ns) for name, ns in self.data.items()}
         return ps

     def to_dict(self) -> dict:
-        return {'counters': dict(self.counters)}
+        return {"data": {name: dict(ns) for name, ns in self.data.items()}}  <!-- REVIEW-R1-FIX: ISSUE-016 —— 防御性复制，与 clone() 策略一致 -->

     @classmethod
     def from_dict(cls, d: dict) -> "PityState":
         ps = cls()
-        ps.counters = d.get('counters', {})
+        if "data" in d:
+            # ISSUE-022：防御性复制——与 clone()/to_dict() 策略一致，
+            # 确保 from_dict() 返回的 PityState 与输入 dict 完全解耦。
+            ps.data = {name: dict(ns) for name, ns in d["data"].items()}
+        elif "counters" in d:
+            # ── P60：旧格式自动升级 ──
+            for name, val in d["counters"].items():
+                ps.data[name] = {"counter": val}
         return ps
```

### 2.2 L8 之前新增 —— DrawInfo 和 PityContext

```python
# ── P60 新增：DrawInfo (frozen) + PityContext (可变载体) ──

from collections.abc import Mapping

@dataclass(frozen=True)
class DrawInfo:
    """抽卡上下文中不可变的部分——本次抽卡的静态事实。"""
    pool_id: str
    pool_instance_id: str
    reward_id: str
    reward_rarity: str
    is_featured: bool
    scope_cards: Mapping[str, tuple[str, ...]]
    scope_slots: Mapping[str, tuple[str, ...]]
    featured_slots: Mapping[str, tuple[str, ...]]
    base_probabilities: Mapping[str, float]
    rarity_rank: Mapping[str, int]


@dataclass
class PityContext:
    """管道中流转的可变载体。"""
    draw: DrawInfo
    current: Dict[str, float]
    state: PityState
```

<!-- REVIEW-R1-FIX: ISSUE-NEW-004 —— DrawInfo 的 scope_cards / scope_slots / featured_slots 在 P60 所有构造点中均为 {}。
     P60 阶段所有 behavior 的 class 字段为 None（stub），因此不阻塞。
     P55 剩余实现 CounterBasedBehavior 子类时，需按 behavior 类型填充这些字段——
     如 scope_cards 从 spec.ssr_ids 取值、featured_slots 从 spec.featured_ids 取值。
     此 gap 已在 §4.1 的三个构造点中显式标注。 -->
<!-- REVIEW-R1-FIX: AUDIT-BREAK-5 —— 空值风险（🟡 非阻塞）：before_draw/get_probabilities 中 reward_rarity=''。
     若 P55/P56 的 CounterBasedBehavior 子类在 _compute_probabilities 中使用 ctx.draw.reward_rarity，
     会得到空字符串。P60 阶段无子类实例化——不阻塞。但 P55 剩余实施时需填充这些字段为实际值。
     ISSUE-NEW-004 已标注此 gap，P55 剩余需在具体 behavior 构造点（§4.1 三处）实现字段映射。 -->

### 2.3 T1 测试 `tests/test_p60_pity_state.py`（新建 · ~35 行）

```python
"""P60 块 2：PityState 序列化往返 + 旧格式自动升级。"""
import pytest
from gacha_simulator.core.pity import PityState


def test_new_format_roundtrip():
    state = PityState()
    state.incr("ssr_soft", "counter")
    state.incr("ssr_soft", "counter")
    state.set("rotating", "guaranteed", True)

    d = state.to_dict()
    restored = PityState.from_dict(d)

    assert restored.get("ssr_soft", "counter") == 2
    assert restored.get("rotating", "guaranteed") is True


def test_old_format_auto_upgrade():
    """旧序列化格式 {"counters": {"ssr_soft": 89}} 自动升级。"""
    old_data = {"counters": {"ssr_soft": 89, "ssr_hard": 10}}
    state = PityState.from_dict(old_data)

    assert state.get("ssr_soft", "counter") == 89
    assert state.get("ssr_hard", "counter") == 10
    # 升级后 to_dict 输出新格式
    new_d = state.to_dict()
    assert "data" in new_d
    assert "counters" not in new_d


def test_clone_is_deep():
    state = PityState()
    state.incr("bh1", "counter")
    clone = state.clone()
    clone.incr("bh1", "counter")
    assert state.get("bh1", "counter") == 1
    assert clone.get("bh1", "counter") == 2


def test_get_default():
    state = PityState()
    assert state.get("nonexistent", "counter") is None
    assert state.get("nonexistent", "counter", 0) == 0
```

---

## 块 3：Counter / Flag + CounterBasedBehavior + BEHAVIOR_REGISTRY（~90 行 · Commit 4 前半）

**文件：** `gacha_simulator/core/pity.py`（继续）

### 3.1 L8 之前新增 —— Counter / Flag

```python
# ── P60 新增：Counter / Flag 微抽象 ──

@dataclass
class Counter:
    """PityState 计数器遥控器——记住地址，封装操作。"""
    _state: PityState
    _name: str
    _key: str = "counter"

    def incr(self, delta: int = 1) -> int:
        return self._state.incr(self._name, self._key, delta)

    def reset(self) -> None:
        self._state.set(self._name, self._key, 0)

    def value(self) -> int:
        return self._state.get(self._name, self._key, 0)

    def reached(self, threshold: int) -> bool:
        return self.value() >= threshold


@dataclass
class Flag:
    """PityState 布尔标志遥控器。"""
    _state: PityState
    _name: str
    _key: str

    def set(self) -> None:
        self._state.set(self._name, self._key, True)

    def clear(self) -> None:
        self._state.set(self._name, self._key, False)

    def is_set(self) -> bool:
        return self._state.get(self._name, self._key, False)
```

### 3.2 L8 之前新增 —— LifecycleConfig

```python
# ── P60 新增：跨 type 生命周期参数 ──

@dataclass(frozen=True)
class LifecycleConfig:
    """跨 type 共享的生命周期参数。P56 扩展 deactivate_on_early_hit / depends_on。
    
    frozen=True：纵深防御——阻止运行时意外修改（如 bh._lifecycle.max_triggers = 5），
    配合 CounterBasedBehavior.__init__ 的 None-sentinel 模式消除可变默认参数风险。
    P56 扩展字段时如需可变副本，通过 dataclasses.replace() 创建新实例。"""
    max_triggers: int = 0                         # 最多触发次数（0=无限）
    deactivate_on_early_hit: bool = False         # P56 扩展
    depends_on: Optional[str] = None              # P56 扩展
```

### 3.3 CounterBasedBehavior 类（插入 `SoftPityBehavior` 之前）

```python
# ── P60 新增：计数器驱动型保底基类 ──

class CounterBasedBehavior(PityBehavior, ABC):
    """计数器驱动型保底的通用生命周期。

    子类只需覆写 _compute_probabilities(ctx, counter) → Dict[str, float]。
    计数器管理（增/查/重置/触发上限/停用）由基类统一处理。
    """

    <!-- REVIEW-R1-FIX: ISSUE-NEW-006 —— lifecycle 默认值从 LifecycleConfig() 改为 None-sentinel 模式。
         原因：Python 可变默认参数陷阱——所有未显式传入 lifecycle 的实例将共享同一个 LifecycleConfig 对象。
         后续 P56 扩展 LifecycleConfig 字段后风险激增——运行时对 _lifecycle 属性赋值会静默污染所有共享实例。
         修复：lifecycle: LifecycleConfig = None，构造体内延迟实例化。
         同时建议 LifecycleConfig 添加 frozen=True 作为纵深防御——即使未来代码绕过 sentinel 传入共享实例，
         dataclass 不可变性也能阻止意外修改传播。 -->

    def __init__(self, name: str, state: PityState, scope: str,
                 reset: str = None, target_featured: bool = False,
                 lifecycle: LifecycleConfig = None):
        self._name = name
        self._state = state
        self._scope = scope                      # 稀有度层级
        self._target_featured = target_featured
        self._reset = reset if reset is not None else scope
        # ISSUE-NEW-006：sentinel 模式——避免可变默认参数共享
        self._lifecycle = lifecycle if lifecycle is not None else LifecycleConfig()
        self._triggers = Counter(state, name, "triggers")
        self._active = Flag(state, name, "_active")
        if self._lifecycle.depends_on is None:
            self._active.set()

    @abstractmethod
    def _compute_probabilities(self, ctx: PityContext, counter: int) -> Dict[str, float]:
        """子类实现——给定上下文和当前计数器值，返回调整后概率分布。"""
        ...

    def _counter(self) -> Counter:
        return Counter(self._state, self._name, "counter")

    # ── 生命周期由基类统一管理 ──

    <!-- REVIEW-R1-FIX: ISSUE-021 —— before_draw() 新增 readonly 参数。
         readonly=True（只读查询——PityEngine.get_probabilities()）：跳过计数器递增，
         仅基于当前计数器值计算概率分布，不修改 PityState。
         readonly=False（可变操作——PityEngine.before_draw()）：递增计数器后计算概率，
         与旧引擎「increment 后 apply」的时序一致。 -->

    def before_draw(self, ctx: PityContext, readonly: bool = False) -> Dict[str, float]:
        if not self._active.is_set():
            return ctx.current.copy()
        if readonly:
            # 只读查询——仅基于当前值计算概率，不修改状态
            v = self._counter().value()
        else:
            v = self._counter().incr()
        return self._compute_probabilities(ctx, v)

    def after_draw(self, ctx: PityContext) -> None:
        if not self._active.is_set():
            return
        if not self._should_reset(ctx):
            return
        if self._lifecycle.max_triggers and self._triggers.value() >= self._lifecycle.max_triggers:
            self._active.clear()
            return
        self._counter().reset()
        self._triggers.incr()

    def _should_reset(self, ctx: PityContext) -> bool:
        if self._reset == "featured":
            return ctx.draw.is_featured
        return ctx.draw.reward_rarity == self._reset

    def did_fire(self, ctx: PityContext) -> bool:
        return self._should_reset(ctx)
```

### 3.4 BEHAVIOR_REGISTRY（插入 `PityEngine` 之前）

```python
# ── P60 新增：保底类型注册表（对齐 STRATEGY_REGISTRY） ──

BEHAVIOR_REGISTRY: Dict[str, dict] = {
    "soft_interval": {
        "class": None,                          # P55 剩余实现——占位
        "display_name": "区间软保底",
        "default_scope": "SSR",                                               # ← ISSUE-019：scope 是行为类型固有属性
        "params": {
            "start": {"type": "int", "display_name": "起始抽数", "default": 80, "min": 1},
            "end":   {"type": "int", "display_name": "结束抽数", "default": 90, "min": 1},
            "func":  {"type": "str", "display_name": "爬升函数", "default": "linear",
                       "options": ["linear", "exp", "step"]},
        },
    },
    "soft_additive": {
        "class": None,                          # P55 剩余实现——占位
        "display_name": "累加软保底",
        "default_scope": "SSR",                                               # ← ISSUE-019
        "params": {
            "start":     {"type": "int",   "display_name": "起始抽数",   "default": 74,  "min": 1},
            "increment": {"type": "float", "display_name": "每抽增量(%)", "default": 6.0, "min": 0.1},
        },
    },
    "hard": {
        "class": None,                          # P55 剩余实现——占位
        "display_name": "硬保底",
        "default_scope": "SSR",                                               # ← ISSUE-019
        "params": {
            "threshold": {"type": "int", "display_name": "保底抽数", "default": 90, "min": 1},
        },
    },
    "rotating": {
        "class": None,                          # P56 实现——stub
        "display_name": "轮换保底",
        "default_scope": "SSR",                                               # ← ISSUE-019
        "params": {
            "initial_win_rate": {"type": "float", "display_name": "初始胜率(%)",  "default": 50.0, "min": 0.0, "max": 100.0},
            "loss_increment":   {"type": "float", "display_name": "歪后增量(%)",  "default": 50.0, "min": 0.0, "max": 100.0},
        },
    },
    "targeted": {
        "class": None,                          # P56 实现——stub
        "display_name": "定向保底（定轨）",
        "default_scope": "SSR",                                               # ← ISSUE-019
        "params": {
            "initial_win_rate":       {"type": "float", "display_name": "初始胜率(%)",    "default": 50.0, "min": 0.0, "max": 100.0},
            "loss_increment":         {"type": "float", "display_name": "歪后增量(%)",    "default": 50.0, "min": 0.0, "max": 100.0},
            "fate_threshold":         {"type": "int",   "display_name": "命定值阈值",     "default": 1,    "min": 0},
            "switch_allowed":         {"type": "bool",  "display_name": "允许中途切换",   "default": True},
            "switch_resets_progress": {"type": "bool",  "display_name": "切换后清零进度", "default": True},
        },
    },
}


def create_behavior(pdef: 'PityDef', state: PityState, **extra) -> 'PityBehavior':
    """工厂函数——从 PityDef + BEHAVIOR_REGISTRY 构造 behavior 实例。

    <!-- REVIEW-R1-FIX: ISSUE-019 —— scope 从 BEHAVIOR_REGISTRY 条目的 default_scope 读取，
         而非从 PityDefParsed（无此字段）。scope 是行为类型的固有属性，不应由配置定义决定。 -->
    <!-- REVIEW-R1-FIX: ISSUE-020 —— 参数类型强制转换：根据 entry['params'] 中各参数的 'type' 声明
         将 pdef.params 中的字符串值转换为目标 Python 类型（int/float/bool/str），再 ** 展开。
         集中管理类型转换——子类构造函数接收正确 Python 类型。 -->
    """
    entry = BEHAVIOR_REGISTRY[pdef.btype]
    cls = entry["class"]
    if cls is None:
        raise ValueError(f"Behavior type '{pdef.btype}' 在 P60 中仅为 stub——"
                         f"完整实现由 P55 剩余/P56 交付。")

    # ISSUE-019：scope 从 registry 条目读取（非 PityDefParsed.scope）
    scope = entry.get("default_scope", "SSR")

    # ISSUE-020：参数类型强制转换——将 pdef.params 的字符串值转为 registry 声明的类型
    typed_params = {}
    for pname, pvalue in pdef.params.items():
        param_meta = entry.get("params", {}).get(pname, {})
        ptype = param_meta.get("type", "str")
        if ptype == "int":
            typed_params[pname] = int(pvalue)
        elif ptype == "float":
            typed_params[pname] = float(pvalue)
        elif ptype == "bool":
            typed_params[pname] = pvalue.lower() in ("true", "1", "yes")
        else:
            typed_params[pname] = pvalue  # str —— 原样传递

    return cls(name=pdef.name, state=state, scope=scope, **typed_params)
```

---

## 块 4：PityEngine 退化为调度器（~60 行 · Commit 4 后半）

**文件：** `gacha_simulator/core/pity.py`（继续）

### 4.1 L195–L267 —— PityEngine 重构

<!-- REVIEW-R1-FIX: ISSUE-002 —— before_draw/after_draw 新增 spec.pity_names 池过滤，避免跨池计数器污染 -->
<!-- REVIEW-R1-FIX: ISSUE-003 —— before_draw/after_draw 补全 apply() 回退桥接 -->
<!-- REVIEW-R1-FIX: ISSUE-009 —— get_probabilities() 桥接纠正：apply() 使用 counter_value + probabilities 签名 -->

```python
# ── P60 重构：PityEngine 退化为纯调度器 ──

class PityEngine:
    """保底引擎——纯调度器。所有业务逻辑由 behavior 自行管理。"""

    def __init__(self, pool_specs: Dict[str, PoolPitySpec],
                 pity_defs: Dict[str, PityDefParsed],
                 behaviors: Dict[str, PityBehavior],
                 rarity_rank: Dict[str, int] = None):
        self.pool_specs = pool_specs
        self.pity_defs = pity_defs
        self.behaviors = behaviors                    # 保留旧接口兼容
        self._state = None                            # 由 before_draw/after_draw 注入
        self._rarity_rank = rarity_rank or {}

        # _behavior_list 仅作为注册表，调度时按池过滤（通过 spec.pity_names）
        self._behavior_list: List[PityBehavior] = list(behaviors.values())

    # ── 辅助：按池过滤 behavior ──

    def _behaviors_for_pool(self, pool_id: str):
        """返回当前池关联的 behavior 实例列表——防止跨池计数器污染。"""
        spec = self.pool_specs.get(pool_id)
        if spec is None:
            return []
        result = []
        for pname in spec.pity_names:
            bh = self.behaviors.get(pname)
            if bh is not None:
                result.append((pname, bh))
        return result

    # ── 策略层查询接口（保留旧方法签名兼容） ──

    def get_probabilities(self, pool_id: str, state: PityState,
                          base_probabilities: Dict[str, float]) -> Dict[str, float]:
        """查询保底调整后的概率分布，不修改保底计数器。"""
        spec = self.pool_specs.get(pool_id)
        if spec is None:
            return base_probabilities.copy()

        draw_info = DrawInfo(
            pool_id=pool_id,
            pool_instance_id=pool_id,               # TODO: per-banner 隔离后续完善
            reward_id="",
            reward_rarity="",
            is_featured=False,
            scope_cards={},                          # ISSUE-NEW-004：get_probabilities 中 reward 未知——scope_cards 需 P55 剩余按 behavior 类型填充
            scope_slots={},                          # ISSUE-NEW-004：同上——scope_slots 需 P55 剩余按 behavior 类型填充
            featured_slots={},                       # ISSUE-NEW-004：get_probabilities 中 is_featured=False——featured_slots 需 P55 剩余按 behavior 类型填充
            base_probabilities=base_probabilities,
            rarity_rank=self._rarity_rank,
        )
        ctx = PityContext(draw=draw_info, current=base_probabilities.copy(), state=state)

        # ── 只读查询——对每个池关联 behavior 调度 ──
        # ISSUE-021：传入 readonly=True——跳过计数器递增，仅基于当前值计算概率。
        # 策略层可能在一次决策中多次查询 get_probabilities()（如比较不同池），
        # readonly 确保每次查询不修改保底状态。
        for pname, bh in self._behaviors_for_pool(pool_id):
            try:
                new_probs = bh.before_draw(ctx, readonly=True)  # ← ISSUE-021：只读查询
                if new_probs is not None:
                    ctx.current = new_probs
            except AttributeError:
                # 旧 behavior 没有 before_draw——用 apply() 只读查询
                # apply(counter_value, probabilities) —— 区分于 before_draw 的递增语义
                cv = state.get(pname, "counter", 0)
                # ISSUE-023：传递 resolved_targets——模板绑定的池子需要解析后的目标分布
                extra = {}
                resolved = spec.resolved_targets.get(pname)
                if resolved:
                    extra['resolved_targets'] = resolved
                ctx.current = bh.apply(cv, ctx.current, extra if extra else None)

        return ctx.current

    def before_draw(self, pool_id: str, state: PityState,
                    base_probabilities: Dict[str, float]) -> Dict[str, float]:
        """抽前调度：仅对当前池关联的 behavior 执行 before_draw。"""
        self._state = state
        spec = self.pool_specs.get(pool_id)
        if spec is None:
            return base_probabilities.copy()

        draw_info = DrawInfo(
            pool_id=pool_id,
            pool_instance_id=pool_id,
            reward_id="",
            reward_rarity="",
            is_featured=False,
            scope_cards={},                          # ISSUE-NEW-004：before_draw 中 reward 未知——scope_cards 需 P55 剩余按 behavior 类型填充
            scope_slots={},                          # ISSUE-NEW-004：同上——scope_slots 需 P55 剩余按 behavior 类型填充
            featured_slots={},                       # ISSUE-NEW-004：before_draw 中 is_featured=False——featured_slots 需 P55 剩余按 behavior 类型填充
            base_probabilities=base_probabilities,
            rarity_rank=self._rarity_rank,
        )
        ctx = PityContext(draw=draw_info, current=base_probabilities.copy(), state=state)

        # ── P60：按池过滤调度 + 旧接口桥接 ──
        for pname, bh in self._behaviors_for_pool(pool_id):
            try:
                new_probs = bh.before_draw(ctx)
                if new_probs is not None:
                    ctx.current = new_probs
            except AttributeError:
                # 旧 behavior（仅有 apply 方法）——递增计数器后传递增后值
                cv = state.get(pname, "counter", 0)
                state.incr(pname, "counter")          # 旧引擎语义：先递增
                # ISSUE-023：传递 resolved_targets——模板绑定的池子需要解析后的目标分布
                extra = {}
                resolved = spec.resolved_targets.get(pname)
                if resolved:
                    extra['resolved_targets'] = resolved
                ctx.current = bh.apply(cv + 1, ctx.current, extra if extra else None)

        return ctx.current

    def after_draw(self, pool_id: str, state: PityState, reward_id: str):
        """抽后调度：仅对当前池关联的 behavior 执行 after_draw。"""
        spec = self.pool_specs.get(pool_id)
        if spec is None:
            return

        is_featured = reward_id in spec.featured_ids
        reward_rarity = self._infer_rarity(reward_id, spec)

        draw_info = DrawInfo(
            pool_id=pool_id,
            pool_instance_id=pool_id,
            reward_id=reward_id,
            reward_rarity=reward_rarity,
            is_featured=is_featured,
            scope_cards={},                          # ISSUE-NEW-004：after_draw 中 spec 信息可用——保留 {} 待 P55 剩余按 behavior 类型填充具体字段
            scope_slots={},                          # ISSUE-NEW-004：同上
            featured_slots={},                       # ISSUE-NEW-004：after_draw 中 is_featured 已知——P55 剩余可实现 CounterBasedBehavior 时从 spec.featured_ids 映射填充
            base_probabilities={},
            rarity_rank=self._rarity_rank,
        )
        ctx = PityContext(draw=draw_info, current={}, state=state)

        # ── P60：按池过滤调度 + 旧接口桥接（含重置回退——复制旧引擎逻辑） ──
        for pname, bh in self._behaviors_for_pool(pool_id):
            try:
                bh.after_draw(ctx)
            except AttributeError:
                # 旧 behavior 无 after_draw——用旧 PityEngine.after_draw 重置逻辑桥接
                pdef = self.pity_defs.get(pname)
                if pdef is None:
                    continue
                is_ssr = reward_id in spec.ssr_ids
                if pdef.reset_condition == 'any_ssr' and is_ssr:
                    state.set(pname, "counter", 0)
                elif pdef.reset_condition == 'featured_ssr' and is_featured:
                    state.set(pname, "counter", 0)
                # 'never' → 无操作（pass）
                # 此桥接在 P55 剩余将旧 behavior 迁移至 CounterBasedBehavior 后删除

    def _infer_rarity(self, reward_id: str, spec: PoolPitySpec) -> str:
        """从 reward_id 推断稀有度。"""
        if reward_id in spec.ssr_ids:
            return "SSR"
        # TODO: 从 card_defs 推断 SR/R——后续完善
        return ""

    def get_spec(self, pool_id: str) -> Optional[PoolPitySpec]:
        return self.pool_specs.get(pool_id)

    # ── P60 新增：策略层只读查询 ──

    def get_counter(self, name: str) -> int:
        return self._state.get(name, "counter", 0) if self._state else 0

    def get_trigger_count(self, name: str) -> int:
        return self._state.get(name, "triggers", 0) if self._state else 0

    def is_guaranteed(self, name: str) -> bool:
        return self._state.get(name, "guaranteed", False) if self._state else False

    def is_active(self, name: str) -> bool:
        return self._state.get(name, "_active", True) if self._state else True

    def get_state_summary(self) -> Dict[str, Dict[str, Any]]:
        if self._state is None:
            return {}
        return {name: dict(ns) for name, ns in self._state.data.items()}
```

### 4.2 `service/gacha_service.py` L221–L222 —— 调用方适配

```diff
-                    if _pity_engine:
-                        _pity_engine.after_draw(pool.id, pity_state, reward.id)
+                    # P60：after_draw 的 behavior 内部管理 reset 逻辑——
+                    # 调用签名不变，但内部已退化为 foreach behavior 管道
+                    if _pity_engine:
+                        _pity_engine.after_draw(pool.id, pity_state, reward.id)
```

**说明：** 调用方 L185 的 `before_draw` 和 L221 的 `after_draw` 签名不变。引擎内部行为完全替换但对外透明。

---

## T2+T3：等价性门控测试（~60 行 · 与块 3+4 同 Commit 4）

**文件：** `tests/test_p60_equivalence.py`（新建）

```python
"""P60 门控测试：旧/新 PityEngine 行为等价。

⚠️ 本测试不通过 → P60 不可交付。
"""

import pytest
from gacha_simulator.core.pity import (
    PityState,
    PityEngine,
    PoolPitySpec,
    PityDefParsed,
    SoftPityBehavior,
    HardPityBehavior,
)


# ═══════════════════════════════════════════════════
# 测试 fixtures —— 从现有 test_pity.py 的配置提取
# ═══════════════════════════════════════════════════

def _make_spec(pity_names, featured_ids=None, ssr_ids=None):
    return PoolPitySpec(
        pity_names=list(pity_names),
        featured_ids=featured_ids or set(),
        ssr_ids=ssr_ids or set(),
    )


def _make_pdef(name, btype="soft", params=None, reset="any_ssr"):
    return PityDefParsed(
        name=name, btype=btype,
        params=params or {"start": "80", "end": "90"},
        target_distribution={},
        reset_condition=reset, pools="*",
    )


# P60 之前的"旧引擎"——测试时使用当前实现
# P60 之后的"新引擎"——同样的 PityEngine 类但内部已是调度器模式
# 两者的外部行为必须等价


# ═══════════════════════════════════════════════════
# T2：概率等价
# ═══════════════════════════════════════════════════

@pytest.mark.parametrize("counters,reset_condition", [
    # 覆盖 3 种 reset_condition × 3 种 counter 值
    # any_ssr
    ({"ssr_soft": 89}, "any_ssr"),
    ({"ssr_soft": 80}, "any_ssr"),
    ({"ssr_soft": 90}, "any_ssr"),
    # featured_ssr
    ({"ssr_soft": 89}, "featured_ssr"),
    ({"ssr_soft": 80}, "featured_ssr"),
    # never
    ({"ssr_hard": 89}, "never"),
    ({"ssr_hard": 90}, "never"),
])
def test_before_draw_probabilities_equivalent(counters, reset_condition):
    """重构前后 before_draw 产出的概率分布完全等价（rel=1e-9）。"""
    base_probs = {"ssr": 0.005, "ssr_alt": 0.005, "sr": 0.05, "r": 0.94}

    pdef = _make_pdef("ssr_soft", btype="soft",
                      params={"start": "80", "end": "90"},
                      reset=reset_condition)
    spec = _make_spec(["ssr_soft"], featured_ids={"ssr"}, ssr_ids={"ssr", "ssr_alt"})

    # 构造旧引擎
    behavior = SoftPityBehavior(
        start_at=80, end_at=90,
        target_distribution={"ssr": 1.0},
    )
    old_engine = PityEngine(
        pool_specs={"pool_1": spec},
        pity_defs={"ssr_soft": pdef},
        behaviors={"ssr_soft": behavior},
    )

    # 构造新引擎（同样的参数——P60 后 PityEngine.__init__ 签名不变）
    new_engine = PityEngine(
        pool_specs={"pool_1": spec},
        pity_defs={"ssr_soft": pdef},
        behaviors={"ssr_soft": behavior},
    )

    old_state = PityState()
    new_state = PityState()
    for name, val in counters.items():
        old_state.data.setdefault(name, {})["counter"] = val  <!-- REVIEW-R1-FIX: ISSUE-015 -->
        new_state.data.setdefault(name, {})["counter"] = val  <!-- REVIEW-R1-FIX: ISSUE-015 -->

    old_probs = old_engine.before_draw("pool_1", old_state, base_probs)
    new_probs = new_engine.before_draw("pool_1", new_state, base_probs)

    for slot in set(old_probs) | set(new_probs):
        assert old_probs.get(slot, 0.0) == pytest.approx(
            new_probs.get(slot, 0.0), rel=1e-9
        ), f"slot {slot}: old={old_probs.get(slot, 0)} new={new_probs.get(slot, 0)}"


# ═══════════════════════════════════════════════════
# T3：after_draw 状态等价
# ═══════════════════════════════════════════════════

@pytest.mark.parametrize("reset_condition,reward_id,expected_counter_after", [
    # any_ssr——任何 SSR 命中都重置
    ("any_ssr", "ssr", 0),
    ("any_ssr", "ssr_alt", 0),
    ("any_ssr", "sr", 90),       # 非 SSR 不重置，counter 为 before_draw 递增后值
    # featured_ssr——仅 featured 命中重置
    ("featured_ssr", "ssr", 0),
    ("featured_ssr", "ssr_alt", 90),  # 非 featured SSR 不重置
    ("featured_ssr", "sr", 90),
    # never——永久不重置
    ("never", "ssr", 90),
    ("never", "sr", 90),
])
def test_after_draw_state_equivalent(reset_condition, reward_id, expected_counter_after):
    """重构前后 after_draw 对 PityState 的修改完全等价。"""
    base_probs = {"ssr": 0.005, "ssr_alt": 0.005, "sr": 0.05, "r": 0.94}

    pdef = _make_pdef("ssr_soft", btype="soft",
                      params={"start": "80", "end": "90"},
                      reset=reset_condition)
    spec = _make_spec(["ssr_soft"],
                      featured_ids={"ssr"},
                      ssr_ids={"ssr", "ssr_alt"})

    behavior = SoftPityBehavior(start_at=80, end_at=90)

    old_engine = PityEngine(
        pool_specs={"pool_1": spec},
        pity_defs={"ssr_soft": pdef},
        behaviors={"ssr_soft": behavior},
    )
    new_engine = PityEngine(
        pool_specs={"pool_1": spec},
        pity_defs={"ssr_soft": pdef},
        behaviors={"ssr_soft": behavior},
    )

    # 模拟 before_draw 后的状态（counter=90，即第 90 抽）
    old_state = PityState()
    new_state = PityState()
    old_state.data.setdefault("ssr_soft", {})["counter"] = 89  <!-- REVIEW-R1-FIX: ISSUE-015 -->
    new_state.data.setdefault("ssr_soft", {})["counter"] = 89  <!-- REVIEW-R1-FIX: ISSUE-015 -->

    # 执行 before_draw（递增计数器）
    old_engine.before_draw("pool_1", old_state, base_probs)
    new_engine.before_draw("pool_1", new_state, base_probs)
    # 此时 counter 应为 90

    old_engine.after_draw("pool_1", old_state, reward_id)
    new_engine.after_draw("pool_1", new_state, reward_id)

    assert old_state.get("ssr_soft", "counter") == expected_counter_after
    assert new_state.get("ssr_soft", "counter") == expected_counter_after


# ═══════════════════════════════════════════════════
# T2a（补充）：硬保底概率等价
# ═══════════════════════════════════════════════════

@pytest.mark.parametrize("counters,reset_condition", [
    ({"ssr_hard": 89}, "any_ssr"),
    ({"ssr_hard": 90}, "any_ssr"),
    ({"ssr_hard": 89}, "featured_ssr"),
    ({"ssr_hard": 90}, "featured_ssr"),
    ({"ssr_hard": 89}, "never"),
    ({"ssr_hard": 90}, "never"),
])
def test_before_draw_hard_pity_equivalent(counters, reset_condition):
    """硬保底——重构前后概率等价（rel=1e-9）。"""
    base_probs = {"ssr": 0.005, "ssr_alt": 0.005, "sr": 0.05, "r": 0.94}

    pdef = _make_pdef("ssr_hard", btype="hard",
                      params={"threshold": "90"},
                      reset=reset_condition)
    spec = _make_spec(["ssr_hard"], featured_ids={"ssr"}, ssr_ids={"ssr", "ssr_alt"})

    behavior = HardPityBehavior(threshold=90)
    old_engine = PityEngine(
        pool_specs={"pool_1": spec}, pity_defs={"ssr_hard": pdef},
        behaviors={"ssr_hard": behavior},
    )
    new_engine = PityEngine(
        pool_specs={"pool_1": spec}, pity_defs={"ssr_hard": pdef},
        behaviors={"ssr_hard": behavior},
    )

    old_state = PityState()
    new_state = PityState()
    for name, val in counters.items():
        old_state.data.setdefault(name, {})["counter"] = val
        new_state.data.setdefault(name, {})["counter"] = val

    old_probs = old_engine.before_draw("pool_1", old_state, base_probs)
    new_probs = new_engine.before_draw("pool_1", new_state, base_probs)

    for slot in set(old_probs) | set(new_probs):
        assert old_probs.get(slot, 0.0) == pytest.approx(
            new_probs.get(slot, 0.0), rel=1e-9
        ), f"hard pity slot {slot}: old={old_probs.get(slot, 0)} new={new_probs.get(slot, 0)}"


# ═══════════════════════════════════════════════════
# T2b（补充）：get_probabilities() 只读——不修改 PityState
# ═══════════════════════════════════════════════════

def test_get_probabilities_readonly():
    """策略层多次查询 get_probabilities() 不修改保底计数器。"""
    base_probs = {"ssr": 0.005, "ssr_alt": 0.005, "sr": 0.05, "r": 0.94}

    pdef = _make_pdef("ssr_soft", btype="soft",
                      params={"start": "80", "end": "90"},
                      reset="any_ssr")
    spec = _make_spec(["ssr_soft"], featured_ids={"ssr"}, ssr_ids={"ssr", "ssr_alt"})

    behavior = SoftPityBehavior(start_at=80, end_at=90)
    engine = PityEngine(
        pool_specs={"pool_1": spec}, pity_defs={"ssr_soft": pdef},
        behaviors={"ssr_soft": behavior},
    )

    state = PityState()
    state.data.setdefault("ssr_soft", {})["counter"] = 85

    # 第一次查询
    probs1 = engine.get_probabilities("pool_1", state, base_probs)
    counter_after_1 = state.get("ssr_soft", "counter", 0)
    # 第二次查询——计数器不应变化
    probs2 = engine.get_probabilities("pool_1", state, base_probs)
    counter_after_2 = state.get("ssr_soft", "counter", 0)

    assert counter_after_1 == 85, f"get_probabilities 不应修改计数器，实际变为 {counter_after_1}"
    assert counter_after_2 == 85, f"第二次查询也不应修改计数器，实际变为 {counter_after_2}"
    for slot in set(probs1) | set(probs2):
        assert probs1.get(slot, 0.0) == pytest.approx(
            probs2.get(slot, 0.0), rel=1e-9
        ), f"两次查询结果应一致 slot {slot}"


# ═══════════════════════════════════════════════════
# T2c（补充）：模板绑定池子——resolved_targets 桥接等价
# ═══════════════════════════════════════════════════

def test_resolved_targets_bridge_equivalent():
    """模板绑定池子——resolved_targets 覆盖 behavior 内置 target_distribution 的行为等价。

    实际配置（config.toml）全部池子使用 distribution_template + bindings，
    保底引擎通过 spec.resolved_targets 获取展开后的目标分布。
    P60 桥接代码必须正确传递此值——本测试覆盖 ISSUE-023 gap。
    """
    base_probs = {"ssr": 0.005, "ssr_alt": 0.005, "sr": 0.05, "r": 0.94}

    pdef = _make_pdef("ssr_soft", btype="soft",
                      params={"start": "80", "end": "90"},
                      reset="any_ssr")
    # spec 携带 resolved_targets——模拟模板绑定展开后的结果
    spec = PoolPitySpec(
        pity_names=["ssr_soft"],
        featured_ids={"diluc"},
        ssr_ids={"diluc", "jean", "ssr_alt"},
        resolved_targets={"ssr_soft": {"diluc": 0.5, "jean": 0.5}},
    )

    # behavior 内置 target_distribution 是占位值——resolved_targets 应覆盖它
    behavior = SoftPityBehavior(
        start_at=80, end_at=90,
        target_distribution={"diluc": 1.0},  # ← 旧默认值，应被 resolved_targets 覆盖
    )

    old_engine = PityEngine(
        pool_specs={"pool_1": spec}, pity_defs={"ssr_soft": pdef},
        behaviors={"ssr_soft": behavior},
    )
    new_engine = PityEngine(
        pool_specs={"pool_1": spec}, pity_defs={"ssr_soft": pdef},
        behaviors={"ssr_soft": behavior},
    )

    old_state = PityState()
    new_state = PityState()
    old_state.data.setdefault("ssr_soft", {})["counter"] = 89
    new_state.data.setdefault("ssr_soft", {})["counter"] = 89

    # before_draw——旧引擎通过桥接代码 apply() 接收 resolved_targets
    old_probs = old_engine.before_draw("pool_1", old_state, base_probs)
    new_probs = new_engine.before_draw("pool_1", new_state, base_probs)

    for slot in set(old_probs) | set(new_probs):
        assert old_probs.get(slot, 0.0) == pytest.approx(
            new_probs.get(slot, 0.0), rel=1e-9
        ), f"resolved_targets slot {slot}: old={old_probs.get(slot, 0)} new={new_probs.get(slot, 0)}"

    # get_probabilities 也应正确传递 resolved_targets
    state_q = PityState()
    state_q.data.setdefault("ssr_soft", {})["counter"] = 85
    probs_q_old = old_engine.get_probabilities("pool_1", state_q, base_probs)
    probs_q_new = new_engine.get_probabilities("pool_1", state_q, base_probs)
    for slot in set(probs_q_old) | set(probs_q_new):
        assert probs_q_old.get(slot, 0.0) == pytest.approx(
            probs_q_new.get(slot, 0.0), rel=1e-9
        ), f"query resolved_targets slot {slot}: old={probs_q_old.get(slot, 0)} new={probs_q_new.get(slot, 0)}"


# ═══════════════════════════════════════════════════
# T3a（补充）：跨池 behavior 不交叉污染
# ═══════════════════════════════════════════════════

def test_cross_pool_no_contamination():
    """两个池子各有同名 behavior——调度不应跨池污染计数器。"""
    base_probs = {"ssr": 0.005, "ssr_alt": 0.005, "sr": 0.05, "r": 0.94}

    # 池 1：ssr_soft (start=80, end=90)
    pdef1 = _make_pdef("ssr_soft", btype="soft",
                       params={"start": "80", "end": "90"}, reset="any_ssr")
    spec1 = _make_spec(["ssr_soft"], featured_ids={"ssr_a"}, ssr_ids={"ssr_a", "ssr_alt"})
    bh1 = SoftPityBehavior(start_at=80, end_at=90)

    # 池 2：ssr_soft（同名不同实例）+ ssr_hard
    pdef2_soft = _make_pdef("ssr_soft", btype="soft",
                            params={"start": "80", "end": "90"}, reset="any_ssr")
    pdef2_hard = _make_pdef("ssr_hard", btype="hard",
                            params={"threshold": "90"}, reset="any_ssr")
    spec2 = _make_spec(["ssr_soft", "ssr_hard"], featured_ids={"ssr_b"}, ssr_ids={"ssr_b", "ssr_alt"})
    bh2_soft = SoftPityBehavior(start_at=80, end_at=90)
    bh2_hard = HardPityBehavior(threshold=90)

    engine = PityEngine(
        pool_specs={"pool_1": spec1, "pool_2": spec2},
        pity_defs={"ssr_soft": pdef1, "ssr_soft_p2": pdef2_soft, "ssr_hard": pdef2_hard},
        behaviors={"ssr_soft": bh1, "ssr_soft_p2": bh2_soft, "ssr_hard": bh2_hard},
    )

    state = PityState()

    # 在池 1 抽卡——仅池 1 的 ssr_soft 递增
    engine.before_draw("pool_1", state, base_probs)
    engine.after_draw("pool_1", state, "sr")  # 不触发重置

    # 池 2 的 behavior 不应被污染
    probs_p2 = engine.get_probabilities("pool_2", state, base_probs)
    # 概率分布应基于干净的初始计数器（0 或未递增状态）
    # 基本断言：分布总和 ≈ 1.0
    assert abs(sum(probs_p2.values()) - 1.0) < 1e-9
```

---

## 波及适配：序列化消费者修复（Commit 4.5）

<!-- REVIEW-R1-FIX: ISSUE-005/ISSUE-006/ISSUE-007 —— 波及适配详细代码 -->
<!-- REVIEW-R1-FIX: AUDIT-BREAK-8 —— 追加 scripts/profile_simulation.py + profile_sim.py 直接 .counters 赋值适配（§5E + Step 21.8 + V4.11） -->

P60 的 PityState.to_dict() 输出格式变更（`{"counters":{...}}` → `{"data":{...}}`）和 get() 签名变更会产生跨进程、跨模块的波及效应。波及面覆盖 6 个消费者：`batch_simulator._run_single`（§5A）、`vulnerability.py` 两处（§5B）、`generalized_drop_rate.py`（§5C）、`gacha_service.py` 两处（§5D）、`scripts/profile_simulation.py`（§5E）、`scripts/profile_sim.py`（§5E）。以下适配必须在 Commit 5 全量回归前完成。

### 5A. `batch_simulator._run_single()` —— 改用 from_dict 反序列化

**文件：** `gacha_simulator/service/batch_simulator.py` L252–L258

<!-- REVIEW-R1-FIX: ISSUE-005 —— from_dict 替代直接属性赋值 -->

```diff
     pity_state = None
     if env.pity_state_init:
         from gacha_simulator.core.pity import PityState
-        pity_state = PityState()
-        counters = env.pity_state_init.get('counters', {})
-        for cname, cval in counters.items():
-            pity_state.counters[cname] = cval
+        # P60：from_dict 已内置旧格式自动升级（检测 'counters' 键自动迁移）
+        pity_state = PityState.from_dict(env.pity_state_init)
```

**同步修改：** `worst_impact.py` L280 的 `pity_state_init` 生产方改用 `PityState.to_dict()` 格式（而非硬编码 `{'counters': init_pity}`）。

<!-- REVIEW-R1-FIX: ISSUE-013 —— SimulationEnvBuilder.from_config_store() L648 生产方 pity_state_init
     也使用旧格式 {'counters': init_counters}，与 worst_impact.py L280 存在相同的不一致问题。
     消费方 _run_single() 的 from_dict() 内置 auto-upgrade 因此不阻塞功能，但同一 pity_state_init
     数据流存在三种格式约定（旧、新、兼容）增加维护者理解负担。修复：改为与 worst_impact.py 一致的
     PityState().to_dict() 格式。-->
     
### 5E. `scripts/profile_simulation.py` + `scripts/profile_sim.py` —— 直接 .counters 赋值适配

**<!-- REVIEW-R1-FIX: AUDIT-BREAK-8 —— 类型断裂（传递缺口）：scripts/profile_simulation.py L96-98 和 scripts/profile_sim.py L84-87
     直接 `ps.counters[cname] = cval` 赋值。PityState 重写后 .counters 属性删除 → 脚本在初始化 PityState 时直接崩溃 AttributeError。
     计划 Item 25（Step 21.5）修复了 batch_simulator._run_single() 的相同模式，但未覆盖这两个脚本文件。
     4a 代码映射的 transitive_gaps 已识别此缺口——计划需新增这两个文件的适配步骤。 -->

**文件：** `scripts/profile_simulation.py` L95–L98

```diff
-    pity_state = PityState()
-    for cname, cval in env.pity_state_init.get('counters', {}).items():
-        pity_state.counters[cname] = cval
+    # P60：改为 from_dict 反序列化——内置旧格式自动升级
+    pity_state = PityState.from_dict(env.pity_state_init) if env.pity_state_init else None
```

**文件：** `scripts/profile_sim.py` L83–L87

```diff
-    ps = PityState()
-    for k, v in env.pity_state_init.get('counters', {}).items():
-        ps.counters[k] = v
+    # P60：改为 from_dict 反序列化——内置旧格式自动升级
+    ps = PityState.from_dict(env.pity_state_init) if env.pity_state_init else None
```

**修复方式：** 与 Item 25（Step 21.5）一致——改为 `PityState.from_dict(env.pity_state_init)`，利用 from_dict 自动检测 `'counters'` 键并升级到 `data` 格式。旧格式兼容在新 API 中透明。

**同步修改（已确定：方案 A）：** `SimulationEnvBuilder.from_config_store()` (batch_simulator.py L648) 的生产方同步产出新格式——与消费方对称，统一全链路格式：
```python
# 旧（硬编码旧格式）
pity_state_init = {'counters': init_counters}
# 新（方案 A——构造初始 PityState 后序列化，与 worst_impact.py 格式统一）
ps_init = PityState()
for cname, cval in init_counters.items():
    ps_init.set(cname, 'counter', cval)
pity_state_init = ps_init.to_dict()
```
约 4 行改动。非阻塞——消费方 from_dict 已内置自动升级旧格式，但统一格式减少维护者困惑。

### 5B. `vulnerability.py` 两处 —— 读取 to_dict 新格式

**文件：** `gacha_simulator/core/vulnerability.py` L608 和 L749

<!-- REVIEW-R1-FIX: ISSUE-006 —— vulnerability 读取 to_dict 新格式 -->

```diff
 # 位置 1：L608（数据不足路径）
-                counters = pes[pool_id].get('counters', {})
-                for cname, cval in counters.items():
+                # P60：from_dict 反序列化——自动升级旧格式，无需同步修改 test_vulnerability.py L216 mock 数据
+                ps = PityState.from_dict(pes[pool_id])
+                for cname in ps.data:
+                    cval = ps.get(cname, 'counter', 0)

 # 位置 2：L749（脆弱性区间路径）
-                counters = pes[pool_id].get('counters', {})
-                for cname, cval in counters.items():
+                # P60：同上——from_dict 反序列化
+                ps = PityState.from_dict(pes[pool_id])
+                for cname in ps.data:
+                    cval = ps.get(cname, 'counter', 0)
```

**选择理由：** `PityState.from_dict()` 内置旧格式自动升级（检测 `'counters'` 键自动迁移），同时使 `test_vulnerability.py` L216 的旧格式 mock 数据自动兼容——无需手动修改测试文件。比直接格式修复（`.get('data', {})`）多一行代码但消除全部格式同步风险。
<!-- REVIEW-R1-FIX: ISSUE-006/ISSUE-018/AUDIT-BREAK-7 —— 已确定：from_dict 反序列化路径。from_dict 自动升级旧格式，vulnerability.py 和 test_vulnerability.py 均无需格式同步。 -->

### 5C. `generalized_drop_rate.py` L66–L71 —— PityProgressAtT 读取序列化 PityState

**文件：** `gacha_simulator/core/generalized_drop_rate.py` L66–L71

<!-- REVIEW-R1-FIX: ISSUE-012 —— 计划原修复 pity_state.get(self.counter_name, 'counter', 0) 错误。
     history[t].pity_state 来自 collector.py L67 的 pity_state.to_dict()——是普通 dict，不是 PityState 实例。
     dict.get() 仅接受 2 个参数 (key, default)，3 个参数会导致 TypeError。
     正确修复：先反序列化 PityState.from_dict()，再使用 3 参数 get() API。
     背景：PityProgressAtT 当前未注册到 UNIFIED_GDR_REGISTRY（仅在 __init__.py 导出，gdr.py 注册表中无此条目），
     因此此 bug 当前静默存在——pity_state.get(self.counter_name, 0) 在 {'data': {...}} 格式的 dict 上查找 counter_name 键，
     始终返回 0，导致 PityProgressAtT 始终输出 0。修复后同步注册到 UNIFIED_GDR_REGISTRY 或至少保证 dict 导航正确。 -->

<!-- 同时保留 ISSUE-007 的原始发现——二参数语义错位是同一根因的另一个表现 -->
<!-- REVIEW-R1-FIX: ISSUE-007 —— 原发现：2参数 get() 语义错位也在此修复中一并解决 -->

```diff
     def compute(self, t: int, history: List['InfoVector']) -> float:
         if t >= len(history):
             return 0
         pity_state = history[t].pity_state
-        count = pity_state.get(self.counter_name, 0)
+        # P60：history[t].pity_state 是 PityState.to_dict() 的返回值（dict），非 PityState 对象。
+        # 必须反序列化后使用 3 参数 get() API。from_dict 自动升级旧格式（{'counters':{...}} → data）。
+        ps = PityState.from_dict(pity_state)
+        count = ps.get(self.counter_name, 'counter', 0)
         return min(count / self.threshold, 1.0)
```

**注意：** `history[t].pity_state` 经过 `PityState.to_dict()` → `InfoVector` → `pity_state` 字段的序列化链条后，
始终为普通的 Python dict（新格式 `{"data": {...}}` 或旧格式 `{"counters": {...}}`），而不是 `PityState` 实例。
`dict.get()` 只接受 2 个参数 `(key, default)`，因此计划原修复方案中 `pity_state.get(self.counter_name, 'counter', 0)`
（3 个参数）**在运行时会产生 TypeError: dict.get() takes at most 2 arguments (3 given)**。

当前代码中 `pity_state.get(self.counter_name, 0)` 之所以「工作」（不报错）是因为 `dict.get()` 接受任意 key + default——
但在 `{'data': {...}}` 格式的 dict 上查找 `self.counter_name`（如 `"ssr_soft"`）总是返回默认值 0，导致
`PityProgressAtT` 始终输出 `min(0/threshold, 1.0) = 0`。`PityProgressAtT` 当前未被 `UNIFIED_GDR_REGISTRY`
注册（grep 确认：仅在 `__init__.py` 导出，`gdr.py:490-621` 注册表中无此条目），因此此 bug 在实际使用中未触发。
**<!-- REVIEW-R1-FIX: AUDIT-BREAK-3 —— 类型断裂（已识别，已修正）：当前代码 pity_state.get(self.counter_name, 0) 在 {'data':{...}} 格式 dict 上查找 counter_name 键 → 始终返回 0 → 静默 bug。
     计划 §5C 已正确修复——先 PityState.from_dict() 反序列化再使用 ps.get(name, 'counter', 0)。
     若实施者按计划表面描述直接写 pity_state.get(self.counter_name, 'counter', 0)（3 参数）而不先反序列化 → TypeError: dict.get() takes at most 2 arguments (3 given)。
     正确步骤：ps = PityState.from_dict(pity_state) → count = ps.get(self.counter_name, 'counter', 0)。
     注意：PityProgressAtT 当前未注册到 UNIFIED_GDR_REGISTRY——此 bug 在生产中未触发，但若未来注册则静默输出 0。P60 修复后如需注册到 GDR，应同步验证。 -->**

### 5D. `gacha_service.py` 两处 —— 单参数 get() 修正

**文件：** `gacha_simulator/service/gacha_service.py` L209 和 L218

<!-- REVIEW-R1-FIX: ISSUE-004 —— gacha_service 单参数 get() 修正 -->

```diff
 # L209（保底触发检测）：
-                                cv = pity_state.get(pname)
+                                cv = pity_state.get(pname, 'counter', 0)

 # L218（pool_counter_max 计算）：
-                            cv = pity_state.get(pname)
+                            cv = pity_state.get(pname, 'counter', 0)
```

---

## 文档同步：CLAUDE.md 架构约束更新（Commit 6）

<!-- REVIEW-R1-FIX: ISSUE-014 —— P60 涉及 5 个模块的公共接口变更，CLAUDE.md §二 架构约束需对应更新。
     H1 SessionStart hook 每次注入 CLAUDE.md，过期的模块描述可能误导后续开发者。 -->

P60 修改了以下模块的公共接口——CLAUDE.md 第二章「架构约束」中对应的描述必须同步更新：

### 6A. 保底 (`core/pity.py`) —— §二 保底描述更新

```diff
### 保底 (`core/pity.py`)

-`PityEngine`：软保底（`start_at`→`end_at` 概率爬升）、硬保底（`threshold` 100%）。重置条件：`any_ssr`/`featured`/`never`。多保底按顺序叠加。
+`PityEngine`：退化为纯调度器——按池过滤 behavior，before_draw/after_draw 统一管道。所有保底逻辑由 behavior 内部管理。
+`PityState`：三层嵌套 namespace 容器——`get(name, key, default)` / `set(name, key, value)` / `incr(name, key, delta)`。
+计数器/标志通过 `Counter` / `Flag` 遥控器封装。`BEHAVIOR_REGISTRY` 统一注册保底类型。
+`BEHAVIOR_REGISTRY` 各条目含 `default_scope` 属性（如 `"SSR"`）——`create_behavior()` 从 registry 读取 scope（非 PityDefParsed，scope 是行为类型固有属性）。
+`DrawInfo`（frozen dataclass）封装抽卡静态事实，`PityContext`（可变载体）在管道中流转。
+`CounterBasedBehavior`（ABC）：子类覆写 `_compute_probabilities(ctx, counter)`——基类统一管理计数器寿命周期。
+旧格式 `{"counters":{...}}` → `from_dict()` 自动升级为新格式 `{"data":{...}}`。
```

### 6B. 策略 (`core/strategy.py`) —— §二 StrategyContext 描述更新

```diff
### 策略 (`core/strategy.py`)

 `STRATEGY_REGISTRY` 注册 7 种策略...`StrategyContext` 封装 `state`/`current_pools`/`target_cards`/`acquired`/`pool_draw_counts`/`total_draws` 等，`get_pity_probabilities()` 惰性计算。
+**P60 变更：** `acquired` 改为 `@property`，从 `state.acquired` 实时读取——5 个构造方同步适配（删除 `acquired=...` 传参）。
+单一真相源，构造方零改动。显式传入值覆盖默认值。
```

### 6C. GachaState (`core/state.py`) —— §二 新增描述

在 CLUADE.md §二 架构约束中，`策略` 与 `GDR` 之间新增：

```markdown
### GachaState (`core/state.py`)

dataclass——模拟状态一等公民。`resources`（资源）、`acquired`（卡牌持有，P60 新增）、
`real_time`、`total_actions`、`extra_state`。`pity_counters` 字段已删除（P60 前已无消费者——死字段）。
P60 新增方法：`add_card(card_id)` / `get_card_count(card_id)` / `total_holding(card_id, initial_counts)`。
```

### 6D. ConfigStore (`core/config_store.py`) —— §二 描述更新

在现有的 ConfigStore 描述末尾追加：

```diff
 配置文件 TOML 格式 → `config_toml.py` 读写（单一 `config.toml`）。

+P60 新增：`rarity_rank` 字段（稀有度→层级，0=最高）+ `is_limited(card_id)` 推导方法
+（基于 `PoolEntry.featured_card_ids` 池子级别聚合，不依赖卡名命名约定）
++ `_parse_rarities(data)` 从 TOML `[rarities].ranks` 解析。
```

### 6E. GDR (`core/gdr.py` + `core/generalized_drop_rate.py`) —— §二 描述更新

在现有 GDR 描述的调用规范之后追加：

```markdown
**P60 变更：** `PityProgressAtT` 读取 `history[t].pity_state`（dict，非 PityState 对象）时，
必须通过 `PityState.from_dict()` 反序列化后再使用 `ps.get(name, 'counter', 0)`——
禁止直接对 dict 调用 3 参数 get()（TypeError）。
```

**文件：** `CLAUDE.md`（项目根目录）

---

## 全量回归（T6 · Commit 5）

```bash
pytest --cov=gacha_simulator -q --tb=short
```

**必须全部通过。** 重点关注：
- `tests/test_pity.py` —— 保底系统所有现有测试
- `tests/test_state.py` —— GachaState 现有测试（如有）
- `tests/test_p60_*.py` —— P60 新增等价性测试（T1–T5）

---

## 完整实施步骤

| Step | 内容 | 文件 | 行数 | 测试 |
|:---:|------|------|:---:|:---:|
| 0 | `PoolEntry.featured_card_ids` 字段 + `_build_pools()` 第二步（rerun_of 复制 L506-512）之后统一后处理遍历填充 | `config_store.py` L20–L35 + `config_toml.py` L512 之后 | +4 | — |
<!-- REVIEW-R1-FIX: ISSUE-001/ISSUE-NEW-002 —— Step 0：PoolEntry 新增 featured_card_ids 字段；填充逻辑放在 rerun_of 复制之后，确保复刻池也正确提取 -->
| 1 | `rarity_rank` 字段 + `clear()` | `config_store.py` L92/L123 | +4 | — |
| 2 | `is_limited()` + `_parse_rarities()` | `config_store.py` L141+ | +20 | — |
| 3 | 调用 `_parse_rarities` | `config_toml.py` | +1 | — |
| 3.5 | `save_toml()` 新增 `[rarities]` 段写入（ISSUE-NEW-007：dict 分桶替代 range 预分配消除 O(max_rank) 内存膨胀；ISSUE-NEW-005：非连续 rank 值在 dict 分桶模式中自然消除空列表） | `config_toml.py` L74–L161 | +10 | — |
<!-- REVIEW-R1-FIX: ISSUE-010/ISSUE-NEW-005/ISSUE-NEW-007 —— Step 3.5：save_toml 往返对称 + dict 分桶替代 range + 非连续 rank 值空列表守卫 -->
| 4 | `[rarities]` 段 | `config/config.toml` | +6 | — |
| 4.5 | `batch_simulator.py` `_featured()` / `_ssr_all()` 改为 `PoolEntry.featured_card_ids` | `batch_simulator.py` L105–L131 | +3/-8 | — |
| 5 | T5：is_limited 等价性测试 | `tests/test_p60_is_limited.py` | +45 | ✅ |
| **→ Commit 1** | | | **~90** | |
| 6 | `acquired` 字段 + 删除 `pity_counters` 死字段 + `clone()` | `core/state.py` L9/L92 | +1/-2 | — |
| 7 | `add_card()`/`get_card_count()`/`total_holding()` | `core/state.py` L98+ | +15 | — |
| 8 | `SimulationStats` 移除 `acquired_counts` | `gacha_service.py` L16/L27/L34–L46 | -8 | — |
| 9 | `StrategyContext.acquired` → `@property` + 5 构造方同步删 `acquired=...` | `strategy.py` L31 + 5 构造点 | +4/-5 | — |
<!-- REVIEW-R1-FIX: ISSUE-008 —— Step 9：@property + 5 构造方适配 -->
| 10 | `state.add_card()` + bonus 计算适配 | `gacha_service.py` L221–L237 | +5/-3 | — |
| 11 | `TargetAcquiredCondition` → `state.get_card_count()` | `stop_condition.py` L61–L66 | +3/-2 | — |
| 12 | T4：GachaState 等价性测试 | `tests/test_p60_gacha_state.py` | +40 | ✅ |
| **→ Commit 2** | | | **~64** | |
| 13 | `PityState` 重写（namespace 容器 + 旧格式升级 + deprecated get() wrapper） | `pity.py` L165–L192 | +35/-20 | — |
| 14 | 全局 grep + 替换所有 `pity_state.get(` / `state.get(` 单参数/二参数调用（含 `generalized_drop_rate.py` L70 先反序列化再 get——见 ISSUE-012） | `gacha_service.py` L209/L218 + `generalized_drop_rate.py` L66–L71 + 所有旧 behavior | - | — |
<!-- REVIEW-R1-FIX: ISSUE-004/ISSUE-007/ISSUE-012 —— Step 14：显式 grep 替换步骤，含具体文件位置。generalized_drop_rate.py 需 PityState.from_dict() 反序列化 -->
| 14.5 | **适配 `tests/test_pity.py`——所有 `.counters` 属性访问、`increment()`/`reset()`/单参数 `get()` 替换为新 API**（约 27 处调用点：`.counters` 赋值→`set()`+`incr()`、`increment(name)`→`incr(name, 'counter')`、`reset(name)`→`set(name, 'counter', 0)`、`get(name)`→`get(name, 'counter', 0)`）。此步骤确保 Commit 3 后现有保底测试套件不崩溃——满足计划「每个 Commit 可独立运行」的承诺 | `tests/test_pity.py`（全文件约 330 行，4 个测试类） | +15/-15 | — |
<!-- REVIEW-R1-FIX: ISSUE-NEW-001 —— Step 14.5：适配 test_pity.py 中约 27 处旧 PityState API 调用。若不处理，Commit 3 后 AttributeError/TypeError 导致保底测试套件全部断裂 -->
| 15 | `DrawInfo` + `PityContext` dataclass | `pity.py` L8 之前 | +25 | — |
| 16 | T1：PityState 序列化测试 | `tests/test_p60_pity_state.py` | +35 | ✅ |
| **→ Commit 3** | | | **~75** | |
| 17 | `Counter` + `Flag` + `LifecycleConfig` | `pity.py` L8 之前 | +45 | — |
| 18 | `CounterBasedBehavior` 基类（ISSUE-NEW-006：lifecycle 参数使用 None-sentinel + `frozen=True` 防御） | `pity.py` | +78 | — |
<!-- REVIEW-R1-FIX: ISSUE-NEW-006 —— Step 18：CounterBasedBehavior 生命周期参数消除可变默认参数陷阱 -->
| 19 | `BEHAVIOR_REGISTRY` + `create_behavior()` | `pity.py` | +50 | — |
| 20 | `PityEngine` 重构（按池过滤 + 旧接口桥接） | `pity.py` L195–L267 | +110/-60 | — |
<!-- REVIEW-R1-FIX: ISSUE-002/ISSUE-003/ISSUE-009 —— Step 20：池过滤 + apply() 桥接 -->
| 21 | T2+T3+T2a+T2b+T2c+T3a：等价性门控测试 | `tests/test_p60_equivalence.py` | +210 | ✅ |
| **→ Commit 4** | | | **~430** | |
| 21.5 | 波及适配：`batch_simulator._run_single` → `PityState.from_dict()` | `batch_simulator.py` L252–L258 | +1/-3 | — |
<!-- REVIEW-R1-FIX: ISSUE-005 —— Step 21.5：from_dict 替代直接属性赋值 -->
| 21.6 | 波及适配：`vulnerability.py` 两处 `.get('counters', {})` → `.get('data', {})`；推荐直接使用 `PityState.from_dict()` 反序列化读取——自动升级旧格式且同时消除 `test_vulnerability.py` L216 的旧格式适配需求 | `vulnerability.py` L608/L749 + `test_vulnerability.py` L216（若走 from_dict 路径则自动兼容） | +4/-2 | — |
<!-- REVIEW-R1-FIX: ISSUE-006/ISSUE-018 —— Step 21.6：vulnerability 读取 to_dict 新格式 + test_vulnerability.py 波及适配 -->
| 21.7 | 波及适配：`worst_impact.py` + `batch_simulator.py` L648 pity_state_init 格式 → `PityState` 序列化 | `worst_impact.py` L280 + `batch_simulator.py` L648 | +4/-2 | — |
<!-- REVIEW-R1-FIX: ISSUE-013 —— Step 21.7：追加 batch_simulator.py L648 生产方同步修复 -->
| 21.8 | 波及适配：`scripts/profile_simulation.py` L96-98 + `scripts/profile_sim.py` L84-87 → `PityState.from_dict()` 替代直接 .counters 赋值 | `scripts/profile_simulation.py` + `scripts/profile_sim.py` | +2/-6 | — |
<!-- REVIEW-R1-FIX: AUDIT-BREAK-8 —— Step 21.8：scripts 直接 .counters 赋值修复 -->
| **→ Commit 4.5 (波及)** | | | **~12** | |
| 22 | 全量回归 `pytest --cov -q` | — | — | ✅ |
| **→ Commit 5** | | | | **✓** |
| 22.5 | 文档同步：CLAUDE.md §二——更新 PityState / StrategyContext / GachaState / ConfigStore / GDR 接口描述 | `CLAUDE.md` | ~25 | — |
<!-- REVIEW-R1-FIX: ISSUE-014 —— Step 22.5：CLAUDE.md 多模块 API 变更文档同步 -->
| **→ Commit 6 (文档)** | | | **~25** | |

---

## 风险汇总

| # | 风险 | 所在块 | 等级 | 症状 | 缓解 |
|---|------|:---:|:---:|------|------|
| 1 | `PityState.get()` 签名变化——旧代码调用 `state.get(name)` (1个参数) → **TypeError: missing 1 required positional argument: 'key'**（非静默返回 None）。受影响调用点：`gacha_service.py` L209/L218、`generalized_drop_rate.py` L66–L71（`pity_state` 是序列化后的 dict，非 PityState 对象——直接调 3 参数 get() 会 TypeError；需先 `PityState.from_dict()` 反序列化），及所有旧 behavior 内部调用。 | 块2 | 🔴 | `gacha_service.py` L209/L218 直接崩溃——TypeError 导致模拟循环中断。`generalized_drop_rate.py` L66–L71：`history[t].pity_state` 来自 `PityState.to_dict()`（dict），`dict.get()` 仅接受 2 参数——3 参数导致 `TypeError: dict.get() takes at most 2 arguments (3 given)`。当前静默存在——`pity_state.get(self.counter_name, 0)` 在 `{'data':{...}}` 格式 dict 上找不到键，始终返回 0。 | **Step 14a**：全局 grep 所有 `pity_state.get(` 和 `state.get(` 调用（单参数/二参数模式），替换为三参数 `state.get(name, 'counter', 0)`（PityState 对象上）。对 `history[t].pity_state`（dict）的调用——先 `PityState.from_dict(pity_state)` 反序列化再使用三参数 API。建议 Commit 3 中保留 `get(name, key=None, default=None)` 的 deprecated wrapper 作为安全网——key=None 时自动降级为旧单参数语义，打印 DeprecationWarning。详见 ISSUE-004/ISSUE-007/ISSUE-012 分析。 |
<!-- REVIEW-R1-FIX: ISSUE-004 —— 修正风险#1 症状描述：单参数调用是 TypeError 非 None -->
<!-- REVIEW-R1-FIX: ISSUE-007 —— 扩展风险#1 覆盖 generalized_drop_rate.py 二参数语义错位 -->
<!-- REVIEW-R1-FIX: ISSUE-012 —— 修正风险#1：generalized_drop_rate.py pity_state 是 dict 非 PityState 对象 -->
| 2 | `CounterBasedBehavior.before_draw` 的递增时序与旧 `PityEngine.before_draw` 的时序差 1 个抽数。`get_probabilities()` 若调用 `before_draw()` 会错误递增计数器——策略层多次查询（如比较不同池的概率）会系统性破坏保底状态。 | 块3/块4 | 🔴→🟡 已修复 | 软保底第 80 抽的 `_progress` 值偏移 → 概率分布系统性偏差 | T2 参数化测试——8 组 (counters×reset) 必须 rel=1e-9 等价。不通过则 block 交付。ISSUE-021：`before_draw()` 新增 `readonly` 参数——`get_probabilities()` 传入 `readonly=True` 跳过计数器递增，仅基于当前值计算概率。 |
| 3 | Bonus 计算 `total_before = init + ac_new - 1` 中 `ac_new` 的来源从 `stats.acquired_counts` 变为 `state.get_card_count()` | 块1 | 🟡 | bonus 计算偏差（多算/少算 1 张） | 块1 §1.2 改动 D 已注明时序分析。T4 中同一 seed 的 `CompactResult` card_counts 完全一致 |
| 4 | `StrategyContext.acquired` 从 dataclass 字段改为 `@property`——grep 发现 5 处构造方显式传入 `acquired={...}`（gacha_service.py:152 + 4 个测试文件）。改为 `@property` 后这些构造方报 `TypeError` | 块1 | 🟡 已知 | 5 处构造方全部崩溃 | **采用方案A（@property）+ 同步修 5 个构造方**。方案B（保留字段 + `__post_init__`）存在隐蔽的 `not {}` 语义陷阱——空 dict 被 `__post_init__` 覆盖为 `state.acquired` 产生静默错误。详见 ISSUE-008 分析及 §1.3 |
<!-- REVIEW-R1-FIX: ISSUE-008 —— 风险#4：方案A（@property）+ 5 处构造方同步修 —— 见 §1.3 构造方适配表 -->
| 5 | `TargetAcquiredCondition.check()` 的 `history` 回退路径在 `stats` 不可用时触发 | 块1 | 🟢 | 旧测试用例无统计数据 → 回退到 history 遍历 | history 回退保留——不为 None 即可工作 |
| 6 | `PityEngine._infer_rarity()` 当前仅识别 SSR——SR/R 判定缺失 | 块4 | 🟡 | `scope="sr"` 的 behavior 收到空 `reward_rarity` → `_should_reset` 永远 False | P55 剩余（`[rarities]` 注册表已就绪）补充 card_defs 查找。**P60 阶段仅 SSR 保底受影响**——现有 `scope="sr"` 保底暂无，不阻塞 |
| 7 | ~~`PoolPitySpec.featured_ids` 与 `is_limited()` 字段名不一致~~ → 已修复：Step 0 为 PoolEntry 新增 `featured_card_ids` 字段并由 `_build_pools()` 自动填充。`is_limited()` 直接访问 `pool.featured_card_ids`（非 getattr）。 | 块5 | 🟡→🟢 已修复 | — | 风险已消除。新增字段同时复用给 `PoolPitySpec.featured_ids` 构造（`batch_simulator.py` L119-L121 当前从 `pool.rewards[0][0].id` 取，不够精确）。详见 ISSUE-001 分析。 |
<!-- REVIEW-R1-FIX: ISSUE-001 —— 风险#7 降级：PoolEntry.featured_card_ids 字段已加入 Step 0 -->
| 8 | Windows `spawn` 多进程中 `PityState._data`（嵌套 dict）的 pickle 传输 | 块2 | 🟡 | 子进程收到空 `_data` → 计数器全为 0 → 保底永不触发 | 现有 `PityState` 已在多进程中传输——`_data: Dict[str, Dict[str, Any]]` 与旧 `counters: Dict[str, int]` 同为基础类型，pickle 兼容。T6 全量回归含多进程路径 |
| 9 | `DrawInfo` `frozen=True` 构造时若字段不完整 → `FrozenInstanceError` | 块2 | 🟢 | 引擎启动时崩溃 | 所有字段有默认值或必填——`DrawInfo` 由 `PityEngine` 内部构造，非用户输入 |
| 10 | `BEHAVIOR_REGISTRY` 的 `"class": None` entries——P60 中仅是 stub | 块3 | 🟢 | P55 剩余/P56 实施前误调用 → `ValueError` | `create_behavior()` 显式检查 `cls is None` → 抛明确错误而非 `TypeError: 'NoneType' object is not callable` |
| 11 | 桥接代码（before_draw/get_probabilities 的 except AttributeError 分支）未传递 `resolved_targets`——模板绑定的池子（resolved_targets != target_distribution）在过渡期使用旧 behavior.apply() 时，概率计算回退到 `self.target_distribution`，产生系统性偏差。 | 块4/§1.2 | 🟡→🟢 已覆盖 | 模板绑定池子（如 distribution_template + bindings）的保底概率偏离正确值。实际 config.toml 24 处使用 distribution_template。 | ISSUE-023：桥接代码已从 `spec.resolved_targets.get(pname)` 获取 resolved_targets。**T2c 测试已覆盖**——behavior 内置 target_distribution（占位值）被 spec.resolved_targets 覆盖，old/new 引擎输出 rel=1e-9 等价。 |
| 12 | `tests/test_pity.py` 约 27 处旧 PityState API 调用（`.counters`/`increment()`/`reset()`/单参数`get()`）在 Commit 3 后全部断裂——违反「每个 Commit 可独立运行」承诺。同时波及 `scripts/profile_simulation.py` L96-98 和 `scripts/profile_sim.py` L84-87 直接 `.counters` 赋值——PityState 重写后 `.counters` 属性删除 → AttributeError。 | 块2/波及 | 🔴 | `AttributeError: 'PityState' object has no attribute 'counters'` + `TypeError: get() missing 1 required positional argument: 'key'` → 保底测试套件全部崩溃。scripts 在初始化时直接崩溃。 | **已确定：方案 B（直接替换）**——Step 14.5 在 Commit 3 中一次性批量替换所有旧 API 调用（~31 处），不保留 deprecated wrapper。Commit 3 后现有测试直接使用新 API。scripts 适配见 Step 21.8。 |
<!-- REVIEW-R1-FIX: ISSUE-NEW-001 —— 风险#12：test_pity.py 适配缺失（blocking） -->
| 13 | `_build_pools()` 中 `featured_card_ids` 自动填充若放在 PoolEntry 构造后紧跟（第一步），复刻池（`rerun_of`）的 distribution 在第二步才从源池复制 → `featured_card_ids` 始终为空 `[]` → `is_limited()` 对复刻池中的限定卡返回 False。 | 块5 | 🟡 | 复刻池的限定卡被误判为常驻卡。当前核心配置无复刻池——但若未来配置新增复刻池则触发。 | 统一后处理遍历放在第二步（rerun_of 复制 L506-512）之后——`for pool in store.pools: pool.featured_card_ids = [...]`。详见 ISSUE-NEW-002 分析。 |
<!-- REVIEW-R1-FIX: ISSUE-NEW-002 —— 风险#13：复刻池 featured_card_ids 覆盖缺失 -->
| 15 | `GachaState.pity_counters` 字段删除——grep 确认无外部消费者（仅 `state.py` 定义和 `clone()` 复制），但若有未跟踪的下游脚本直接访问 `state.pity_counters` 则发生 `AttributeError`。 | 块1 | 🟢 | 未跟踪脚本导入 `GachaState` 后访问 `.pity_counters` 崩溃。 | Grep 全项目确认零外部引用。Commit 2 后 `pytest -q` 全量回归覆盖。 |
| 16 | `batch_simulator.py` `_featured()` 从 `PoolEntry.featured_card_ids` 取值——若 `pool_featured_map` 构造处遗漏 pool，`_featured()` 回退到 `pool.rewards[0][0].id`（旧逻辑保留为兜底）。 | 块5/§5.0a | 🟢 | 单 featured 卡池子行为不变；多 featured 卡池子中第二张及之后 featured 卡命中时保底计数器不重置。 | `pool_featured_map` 遍历 `store.pools`——与 `_build_pools()` 的池子范围一致，不遗漏。旧 fallback 保留为纵深防御。 |
| 17 | `save_toml()` 的 `[rarities]` 反向映射在非连续 rank 值场景（如手工编辑 TOML 产生 `{SSR:0, SR:5, R:10}`）产生大量空 `[]` 列表。极端 rank 值（如 1000000）产生 O(max_rank) 内存膨胀。正常路径 `_parse_rarities` 用 `enumerate(ranks)` 连续赋值，不受影响。 | 块5/§5.2a | 🟢 | 写入 TOML 后含空洞空列表（语义无害但浪费空间）。极端 rank 值产生可感知延迟 ~8 MB 瞬时内存。若未来代码绕过 `_parse_rarities` 直接写入 `rarity_rank`，可能产生无效输出。 | ISSUE-NEW-007：改用 dict 分桶后 `sorted(buckets)` 仅迭代实际存在的 rank 键——消除 O(max_rank) 内存膨胀，空列表问题在 dict 分桶模式中自然消除。ISSUE-NEW-005：`_parse_rarities()` 末尾添加不变式校验确保 rank 值连续。 |
<!-- REVIEW-R1-FIX: ISSUE-NEW-005/ISSUE-NEW-007 —— 风险#14：save_toml 极端 rank 值内存膨胀 + 空列表组（已通过 dict 分桶双重消除） -->
---

## 集成验证清单

P60 每个 Commit 后，需逐一确认以下消费者未被破坏。每项对应一个具体的 `grep` 或运行检查。

### Commit 1 后（块5）

| # | 验证项 | 文件/命令 | 预期 |
|---|--------|----------|------|
| V1.1 | `[rarities]` 解析不破坏现有 TOML 加载 | `python -c "from gacha_simulator.core.config_toml import load_toml; load_toml('config/config.toml')"` | 无错误 |
| V1.2 | `is_limited()` 对现有所有 card_id 返回合理值 | `pytest tests/test_p60_is_limited.py -v` | 全部通过 |
| V1.3 | `batch_simulator.py` `_featured()` 从 `PoolEntry.featured_card_ids` 取值 | `grep -n "pool.rewards\[0\]\[0\].id" gacha_simulator/service/batch_simulator.py` | 无匹配（仅旧 fallback 保留） |

### Commit 2 后（块1）

| # | 验证项 | 文件/命令 | 预期 |
|---|--------|----------|------|
| V2.1 | `grep "acquired_counts"` 除 P60 测试文件外零匹配 | `grep -rn "acquired_counts" gacha_simulator/ --include="*.py" \| grep -v test_p60` | 空 |
| V2.2 | `TargetAcquiredCondition` 的 `check()` 使用 `state.get_card_count()` | `gacha_simulator/core/stop_condition.py:61-66` | 调用 `state.get_card_count(self.target_id)` |
| V2.3 | `StrategyContext.acquired` 改为 `@property`，5 个构造方已删除 `acquired=...` 参数 | `grep -rn "StrategyContext(" gacha_simulator/ tests/` | 无残留的 `acquired=...` 传参 |
| V2.4 | 模拟产出不变——CompactResult 的 `card_counts` 与迁移前完全一致 | `pytest tests/ -k "simulation" -q` | 通过 |
| V2.5 | 旧快照文件（`*.json` 含 `GachaState`）可正常加载 | 手动或脚本读取旧测试 fixture JSON | 不报 `KeyError: 'acquired'` |

### Commit 3 后（块2）

| # | 验证项 | 文件/命令 | 预期 |
|---|--------|----------|------|
| V3.1 | `PityState` 的旧序列化格式自动升级 | `pytest tests/test_p60_pity_state.py::test_old_format_auto_upgrade -v` | 通过 |
| V3.2 | 所有现有 `state.get(name)` （1 参数）调用替换为 `state.get(name, 'counter', 0)` | `grep -rn "pity_state\.get(\|state\.get(" gacha_simulator/ --include="*.py"` | 无单参数调用（除 `from_dict` 内部的 `d.get(...)` 和 deprecated wrapper 内部） |
| V3.2a | **额外检查**：所有 2 参数 `state.get(name, <非字符串>)` 调用修正——2参数调用在新 API 下语义错位（第二个参数是 key 而非 default） | `grep -rnP "state\.get\(\s*\w+\s*,\s*\d+" gacha_simulator/ --include="*.py"` | 无匹配——所有 2 参数调用已改为 3 参数 |
<!-- REVIEW-R1-FIX: ISSUE-007 —— V3.2a：新增 2 参数语义错位检查，防止 generalized_drop_rate.py L70 类 bug -->
| V3.2b | **`tests/test_pity.py` 不包含旧 API 调用**——所有 `.counters`/`increment()`/`reset()`/单参数 `get()` 已替换 | `grep -rn 'state\.counters\b' tests/` + `grep -rn 'state\.increment\|state\.reset(' tests/` | 零匹配（`from_dict` 内部的 `d.get('counters', ...)` 除外） |
<!-- REVIEW-R1-FIX: ISSUE-NEW-001 —— V3.2b：确保 test_pity.py 适配完成，Commit 3 可独立运行 -->
| V3.3 | 进程间传输——`PityState` pickle 序列化 | 运行现有含多进程的测试 `pytest tests/ -k "multi" -q` | 通过 |

### Commit 4 后（块3+4）

| # | 验证项 | 文件/命令 | 预期 |
|---|--------|----------|------|
| V4.1 | **T2**：概率等价 | `pytest tests/test_p60_equivalence.py -k "probabilities" -v` | 8/8 通过，rel=1e-9 |
| V4.2 | **T3**：状态等价 | `pytest tests/test_p60_equivalence.py -k "state" -v` | 8/8 通过 |
| V4.3 | `worst_impact.py` 直接实例化 `SoftPityBehavior`/`HardPityBehavior`——确认类名无变化 | `grep -n "SoftPityBehavior\|HardPityBehavior" gacha_simulator/core/worst_impact.py` | 类名引用存在、无 import error |
| V4.4 | `batch_simulator.py` 的 `_build_pity_engine_from_gui()` 构造不受影响 | `grep -n "PityEngine(" gacha_simulator/service/batch_simulator.py` | `__init__` 签名无变化——新增的 `rarity_rank` 参数有默认值 `None` |
| V4.5 | `gacha_service.py` 的 `before_draw`/`after_draw` 调用签名不变 | `python -c "from gacha_simulator.service.gacha_service import GachaService"` | 无 import error |
| V4.6 | `batch_simulator._run_single` 使用 `PityState.from_dict()` | `grep -n "pity_state.counters" gacha_simulator/service/batch_simulator.py` | 无匹配 |
<!-- REVIEW-R1-FIX: ISSUE-005 —— V4.6：确认 batch_simulator 不再直接赋值 .counters -->
| V4.7 | `vulnerability.py` 读取 `to_dict()` 输出使用新格式键 `data` | `grep -n "\.get('counters'" gacha_simulator/core/vulnerability.py` | 无匹配 |
<!-- REVIEW-R1-FIX: ISSUE-006 —— V4.7：确认 vulnerability.py 使用新格式 -->
| V4.8 | `generalized_drop_rate.py` 先 `PityState.from_dict()` 反序列化后使用 3 参数 `get()` | `grep -n "pity_state.get\|PityState.from_dict" gacha_simulator/core/generalized_drop_rate.py` | `ps = PityState.from_dict(pity_state)` + `ps.get(name, 'counter', 0)` |
<!-- REVIEW-R1-FIX: ISSUE-012 —— V4.8：确认 GDR 先反序列化再 get，而非直接对 dict 调 3 参数 get -->
| V4.9 | `gacha_service.py` 两处 `pity_state.get(pname)` 已修正 | `grep -n "pity_state.get(pname)" gacha_simulator/service/gacha_service.py` | 三参数调用 `get(pname, 'counter', 0)` |
<!-- REVIEW-R1-FIX: ISSUE-004 —— V4.9：确认 gacha_service 使用三参数 get -->
| V4.10 | `batch_simulator.py` L648 `pity_state_init` 不再硬编码 `{'counters': ...}` | `grep -n "'counters':" gacha_simulator/service/batch_simulator.py` | 无匹配（或仅有注释说明旧格式兼容） |
<!-- REVIEW-R1-FIX: ISSUE-013 —— V4.10：确认 batch_simulator.py 生产方格式已同步 -->
| V4.11 | `scripts/profile_simulation.py` + `scripts/profile_sim.py` 不直接赋值 `.counters` | `grep -n "\.counters\s*=" scripts/` | 无匹配 |
| V4.12 | **T2a**：硬保底概率等价 | `pytest tests/test_p60_equivalence.py -k "hard_pity" -v` | 6/6 通过，rel=1e-9 |
| V4.13 | **T2b**：`get_probabilities()` 只读——不修改 PityState | `pytest tests/test_p60_equivalence.py -k "readonly" -v` | 通过 |
| V4.14 | **T3a**：跨池 behavior 不交叉污染 | `pytest tests/test_p60_equivalence.py -k "cross_pool" -v` | 通过 |
| V4.15 | **T2c**：`resolved_targets` 桥接等价——模板绑定池子 | `pytest tests/test_p60_equivalence.py -k "resolved_targets" -v` | 通过，rel=1e-9 |
<!-- REVIEW-R1-FIX: AUDIT-BREAK-8 —— V4.11：确认 scripts 不再直接赋值 .counters -->

### Commit 5 后（全量）

| # | 验证项 | 命令 | 预期 |
|---|--------|------|------|
| V5.1 | 全量测试 | `pytest --cov=gacha_simulator -q --tb=short` | 全部通过，覆盖率不下降 |
| V5.2 | 等价性测试 | `pytest tests/test_p60_*.py -v` | 全部通过 |
| V5.3 | 已知脆弱的测试 | `pytest tests/test_pity.py tests/test_strategy.py -v` | 全部通过 |

---

## 验收标准

### 门控（不通过则 P60 不可交付）

- [ ] **T2**：`test_before_draw_probabilities_equivalent`——8 组 (counters × reset_condition) 全部 rel=1e-9
- [ ] **T2a**：`test_before_draw_hard_pity_equivalent`——6 组硬保底参数化 全部 rel=1e-9
- [ ] **T2b**：`test_get_probabilities_readonly`——策略层多次查询不修改保底状态
- [ ] **T2c**：`test_resolved_targets_bridge_equivalent`——模板绑定池子 resolved_targets 桥接等价（覆盖 ISSUE-023）
- [ ] **T3**：`test_after_draw_state_equivalent`——8 组 (reset × reward) 全部状态等价
- [ ] **T3a**：`test_cross_pool_no_contamination`——跨池 behavior 不交叉污染
- [ ] **T6**：`pytest --cov=gacha_simulator -q`——全量通过

### 功能

- [ ] **T1**：旧格式 `{"counters": {...}}` 自动升级
- [ ] **T4**：`GachaState.acquired` 序列化 + `clone()` + 旧快照兼容
- [ ] **T5**：`is_limited()` 正确（含修复 startswith 误判/漏判的预期差异）
- [ ] `StrategyContext.acquired` 作为 `@property` 在所有策略中可用——5 个构造方已适配
- [ ] `TargetAcquiredCondition` 基于 `state.get_card_count()` 判定
- [ ] `GachaState.pity_counters` 死字段已删除——grep 零外部引用
- [ ] `batch_simulator.py` `_featured()` 从 `PoolEntry.featured_card_ids` 取值
- [ ] `[rarities]` 解析 → `rarity_rank` 正确

### 简化效应可核验

- [ ] 9 条简化效应（详见设计文档 §六）可被逐一核验

---

## ⚠️ 自动化审查阻塞项

- **未解决问题：** 0 个
- **详情：** []
- **阻塞原因：** 6 轮对抗循环未收敛。

---

## 自动化审查记录

### 第 N 次审查（2026-06-11）——P38 工作流自动化

- **复杂度:** complex | **变更性质:** evolutionary
- **阶段 1 影响面:** 49 个发现
- **阶段 2 对抗循环:** 6 轮，熔断
- **阶段 3 门控:** 6 项检查
- **阶段 4 代码审计:** 已执行
- **累计问题:** 30 个
